# native 模块日志埋点全面改造路线图

> 目标：构建覆盖率接近 100% 的原生扩展（C 模块）与 Python 包装层日志体系，确保任何异常都能被统一日志系统捕获，并在启动、运行阶段清晰可观测。

## 阶段 1（核心）——NativeLogBridge 基础设施搭建
- **负责人**：Cascade（本阶段我负责完成）
- **目标**：
  1. 设计并实现 C ↔ Python 日志桥接框架（NativeLogBridge），提供 `NATIVE_LOG_DEBUG/INFO/WARNING/ERROR` 宏。
  2. 在 Python 侧实现 `logging_bridge.py`，注册统一回调，按 `scenario/log_type` 路由到 LoggingHub。
  3. 为关键包装层提供 `native_call_guard`（同步/异步）装饰器，兜底捕获异常并补充上下文。
- **产出**：
  - C 层公共头文件 & 源文件（如 `backend/infrastructure/native/native_log_bridge/native_log_bridge.c/h`）。
  - Python 侧桥接模块与文档，新增单元测试覆盖桥接回调。
  - 迁移 `native_ipc` 顶层包以验证桥接可用性。

## 阶段 2 —— native_ipc / native_iocp 通信栈
- **目标**：将 `native_ipc`、`native_iocp` C 扩展及其 Python 包装层全部接入阶段 1 的桥接体系。
- **要点**：
  - 所有 IOCP 读写、连接、等待操作输出关键生命周期日志。
  - 对超时/重试场景写入结构化告警（含管道名、等待时长、重试次数）。

## 阶段 3 —— 并发执行栈（native_threadpool / native_scheduler / native_async / native_queue / native_load_balancer）
- **目标**：线程池、调度器及其衍生组件在任务崩溃、饱和、阻塞时及时报警。
- **要点**：
  - C 层 worker 调度失败统计输出；
  - Python 包装层记录任务提交/回收状态与重试策略；
  - `native_async`、`native_queue`、`native_load_balancer` 的队列/分发异常都能结构化呈现。

## 阶段 4 —— native_log_pipeline
- **目标**：日志缓冲区异常（写满、SQLite 失败、回调异常）全部可观测。
- **要点**：
  - C 层 flush 计数与错误计数通过桥接输出；
  - Python 层定期 dump 统计，触发阈值时告警。

## 阶段 5 —— native_rpc_bridge & native_serialization
- **目标**：RPC 桥与序列化异常（协议不匹配、回调失败、buffer 错误）即时暴露。
- **要点**：
  - 为批量 decode/encode 增加输入摘要、失败上下文；
  - 捕获跨语言回调异常并提供回退策略日志。

## 阶段 6 —— 数据与计算扩展
- **模块范围**：`native_indicator`、`native_finance_ops`、`native_compute`、`native_conversion`、`native_vnpy_conversion`、`native_statistics`、`native_symbol_index`、`native_dataframe_ops`、`native_data_quality`、`dataconverter`、`native_calendar`、`native_collections`。
- **目标**：任何计算失败、数据越界、输入非法均能记录。
- **要点**：
  - C 层参数验证错误输出具体字段与输入摘要；
  - Python 层在调用 guard 中附加调用栈、数据规模信息，并在失败时提示回退算法。

## 阶段 7 —— 监控与采集（native_process_metrics、native_smart_monitor、metrics）
- **目标**：监控数据采集失败/延迟能触发预警。
- **要点**：
  - 采集循环增加心跳日志；
  - 当连续 N 次失败时触发 ALERT 并建议降级策略；
  - `metrics` 包输出的统计异常需包含指标名称、采样窗口、原始错误码。

## 阶段 8 —— 网络/IPC 相关扩展（native_netprobe、native_socket_metrics）
- **目标**：探针/网络指标异常能快速追踪。
- **要点**：
  - 记录目标地址、耗时、系统错误码；
  - 在超时情况下提供重试计划日志。

## 阶段 9 —— UI/渲染扩展（native_qhighlighter 等）
- **目标**：UI 原生组件出现渲染/资源加载异常时具备可诊断日志。
- **要点**：
  - 记录资源 ID、颜色方案、平台信息；
  - 在失败后输出 fallback 策略。

## 阶段 11 —— 系统工具与基础设施（alert、native_fs、native_memory、native_gil、native_smart_monitor 附属、native_socket_metrics 共享组件）
- **目标**：保障底层工具类扩展（文件系统、内存、GIL 管理、Alert 框架等）发生异常时能明确定位。
- **要点**：
  - `alert` 包在发出告警或写入持久化失败时记录上下文（场景、接收端、重试策略）；
  - `native_fs`、`native_memory`、`native_gil` 的关键系统调用记录参数与返回码，便于排查权限/资源问题；
  - 对共享组件（如 `native_socket_metrics`、`native_smart_monitor` 的辅助模块）建立统一的桥接测试用例，确保任何新增工具遵循同一规范。

## 阶段 10 —— 收尾与巡检自动化
- **目标**：构建统一巡检脚本，确保所有 native 包均开启日志桥接且通过冒烟测试。
- **要点**：
  - 编写 `scripts/check_native_logging.py` 检测模块是否注册桥接；
  - 建立 CI job，编译关键扩展并运行基础调用，验证日志输出；
  - 更新文档，指导新扩展如何接入桥接体系。

---

## 并行推进说明
- 阶段 1 是所有后续工作的前置依赖，需优先完成。
- 阶段 2～9 可在阶段 1 落地后并行推进，每个阶段关注不同模块族；实施时建议按照启动关键路径优先级（IPC → 调度 → 日志 → RPC → 计算 → 监控 → 网络 → UI）逐步落地。
- 阶段 10 持续收敛，待核心模块迁移完毕后启动。

## 验收标准（适用于各阶段）
1. **日志覆盖率**：任一 native 调用失败必须至少输出一条结构化日志（含模块名、错误类型、上下文）。
2. **回退策略**：在原生模块不可用时，Python 层需提供明确的降级日志与行为。
3. **测试验证**：引入最少一条单元/集成测试，验证日志桥接与异常捕获链路。
4. **文档更新**：README 或相关文档需记录新行为、调试方式与配置开关。
