三进程架构迁移方案
一、架构设计
1.1 进程划分
当前架构：主进程（UI+业务服务+监控服务）+ 监控进程

目标架构：三进程架构

UI进程：UI层、业务服务层（MarketBoardService、TradingGatewayService、PortfolioService等）
数据进程：数据服务层（ChinaStockEngine、UnifiedDataManager、LoadBalancer、ServerPoolManager等）
监控进程：监控服务层（MonitoringProcessV2、SystemMonitor、ProcessMonitor等）
1.2 统一日志系统设计
核心原则：以事件和严重等级划分，不以进程划分

实现方案：

主进程（UI进程）：
初始化LoggingHub（统一日志中心）
启动MultiProcessLogCollector（收集子进程日志）
统一路由和输出所有日志（file、console、database、event）
数据进程：
使用setup_subprocess_logging配置日志
将日志发送到共享队列（multiprocessing.Queue）
不进行本地路由，所有日志转发到主进程
监控进程：
使用setup_subprocess_logging配置日志
将日志发送到共享队列（multiprocessing.Queue）
不进行本地路由，所有日志转发到主进程
关键文件：

backend/infrastructure/system_vnpy/logging_system.py：MultiProcessLogCollector、setup_subprocess_logging
backend/startup/stages/logging_init.py：LoggingInitStage（初始化MultiProcessLogCollector）
1.3 统一启动安排设计
核心原则：保持统一的启动流程和日志输出

实现方案：

主进程（UI进程）：
使用StartupOrchestrator管理启动阶段
启动顺序：EnvSetupStage → LoggingInitStage → QtFrameworkStage → BackendInitStage → UIActivationStage
在BackendInitStage中启动数据进程和监控进程
数据进程：
创建独立启动脚本：backend/infrastructure/data_module_vnpy/data_process_main.py
接收主进程的启动信号（通过native_ipc或文件信号）
初始化数据服务（ChinaStockEngine、UnifiedDataManager等）
使用setup_subprocess_logging配置日志
监控进程：
保持现有启动方式（通过MonitorLauncherWorker启动）
使用setup_subprocess_logging配置日志
关键文件：

backend/startup/orchestrator.py：StartupOrchestrator
backend/startup/stages/backend_init.py：BackendInitStage（启动数据进程和监控进程）
backend/startup/workers/data_launcher.py：DataLauncherWorker（新增，启动数据进程）
backend/startup/workers/monitor_launcher.py：MonitorLauncherWorker（保持现有）
二、实施步骤
步骤1：创建数据进程启动脚本
文件：backend/infrastructure/data_module_vnpy/data_process_main.py

职责：

初始化数据进程的日志系统（setup_subprocess_logging）
初始化数据服务（ChinaStockEngine、UnifiedDataManager等）
等待主进程的IPC连接
处理主进程的RPC请求（数据查询、计算任务等）
步骤2：创建数据进程启动Worker
文件：backend/startup/workers/data_launcher.py

职责：

启动数据进程（subprocess.Popen）
等待数据进程就绪（通过文件信号或IPC连接）
管理数据进程生命周期
创建IPC管道（data_query、data_calculation等）
步骤3：修改BackendInitStage
文件：backend/startup/stages/backend_init.py

修改内容：

添加DataLauncherWorker（启动数据进程）
保持MonitorLauncherWorker（启动监控进程）
并行启动数据进程和监控进程
等待两个进程都就绪后再继续
步骤4：修改LoggingInitStage
文件：backend/startup/stages/logging_init.py

修改内容：

初始化MultiProcessLogCollector
创建共享队列（multiprocessing.Queue）
将队列传递给数据进程和监控进程
步骤5：修改服务初始化逻辑
文件：backend/startup/initializers/service_initializer.py

修改内容：

移除ChinaStockEngine的初始化（移到数据进程）
移除DataCenterService的初始化（移到数据进程）
保持其他服务的初始化（在主进程）
步骤6：修改跨进程通信
文件：backend/services/market_board_service.py、backend/services/portfolio_service.py等

修改内容：

添加数据进程RPC客户端（native_ipc）
将数据查询和计算任务改为跨进程调用
保持UI响应性（异步调用）
三、关键设计决策
3.1 日志系统设计
决策：所有日志在主进程统一路由和输出

理由：

保持统一日志系统的设计原则（以事件和严重等级划分）
简化日志管理（单一路由点）
确保日志顺序（主进程统一排序）
实现：

主进程：LoggingHub + MultiProcessLogCollector
子进程：setup_subprocess_logging + QueueHandler
3.2 启动顺序设计
决策：主进程先启动，然后并行启动数据进程和监控进程

理由：

保持统一启动安排的设计原则
主进程需要先初始化日志系统
数据进程和监控进程可以并行启动
实现：

主进程：StartupOrchestrator管理启动阶段
数据进程：DataLauncherWorker启动
监控进程：MonitorLauncherWorker启动
3.3 进程间通信设计
决策：使用native_ipc进行跨进程通信

理由：

高性能（Windows Named Pipes + IOCP）
异步支持（asyncio）
已集成到项目中
实现：

数据进程：RPC服务器（data_query、data_calculation等）
监控进程：事件发布（EVENT_SYSTEM_METRICS等）
主进程：RPC客户端 + 事件订阅
四、实施风险
4.1 日志系统风险
风险：子进程日志丢失或延迟

缓解：

使用PersistentBufferHandler缓冲日志
使用OrderedLogQueue确保日志顺序
增加日志重试机制
4.2 启动顺序风险
风险：数据进程或监控进程启动失败

缓解：

增加超时机制
增加重试机制
增加降级机制（主进程可以独立运行）
4.3 进程间通信风险
风险：IPC连接失败或延迟

缓解：

增加连接重试机制
增加超时机制
增加降级机制（本地计算）
五、测试计划
5.1 单元测试
测试数据进程启动脚本
测试数据进程启动Worker
测试跨进程日志收集
测试跨进程RPC通信
5.2 集成测试
测试三进程架构启动流程
测试统一日志系统
测试统一启动安排
测试跨进程通信
5.3 性能测试
测试日志收集性能
测试跨进程RPC性能
测试启动时间
六、回滚计划
如果三进程架构出现问题，可以回滚到当前架构：

移除数据进程启动脚本
移除DataLauncherWorker
恢复服务初始化逻辑
恢复跨进程通信逻辑
七、后续优化
数据进程独立扩展（多实例）
监控进程独立扩展（多实例）
进程间通信优化（批量传输）
日志系统优化（压缩、归档）
