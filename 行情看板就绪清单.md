# 行情看板就绪核查清单

> 覆盖 UI → 服务 → 数据 → 日志 → 监控 → 启动阶段的硬性条件，并补充软性增强项，便于上线前逐项确认。

## 快速核查顺序
1. **启动阶段标志全部 ✅**：`LoggingInitStage` → `QtFrameworkStage` → `BackendInitStage` → `UIActivationStage`。
2. **统一日志系统与路由 ✅**：LoggingHub、路由规则、输出目标全部加载成功。
3. **数据中心/智能负载/实时推送 ✅**：`ChinaStockEngine`、LoadBalancer、统一数据管理器、实时/虚拟推送全就绪。
4. **系统监控进程与事件链路 ✅**：`SystemManagerEngine`、`MonitoringProcessV2`、native_ipc 通道全部成功。
5. **UI 侧 ChartWizard/事件订阅 ✅**：`MarketBoardView`、`ChartWizardEnhanced` 初始化完成并已订阅行情事件。

---

## 硬性前置条件（必须全部满足）

### UI 层
- `MarketBoardView` 初始化完成，主窗口状态 `ui_ready`、`ui_visible` 均为 True。
- `ChartWizardEnhanced` 成功加载 `vnpy_chartwizard`、`pyqtgraph`、`talib` 并能显示基础图表。

### 业务服务层
- `MarketBoardService` 已注册至 `ServiceManager`，`health_check()` 返回 OK。
- `EventEngine`、`MainEngine` 注入成功，行情事件能够正常广播。

### 数据模块层
- `ChinaStockEngine.initialize()` 完成，`is_ready()` 为 True，品种列表缓存成功加载。
- LoadBalancer v3.1、`QueuePressureMonitor`、`TaskStrategyRegistry` 均处于 Ready 状态，无 critical 压力告警。
- 统一数据管理器四层链路：预加载缓存、存储管理、录制、实时推送全部可访问。
- 实时推送网关与虚拟推送网关运行中；`DataCenterService` 能订阅/推送行情。
- 数据质量扫描线程活跃，最近扫描时间在配置阈值内。

### 统一日志系统
- LoggingHub 完全接管 root logger，MemoryHandler 缓冲已刷新。
- `rules_global.yaml`、`rules_stage.yaml`、`rules_module.yaml`、`rules_scenario.yaml` 加载成功无错误。
- 输出目标 `file`、`ai_file`、`event`/`event_throttled`、`ui_statusbar`（以及需要的 `database`、`ui_dialog`）正常运行。
- 行情相关 logger 使用 `log_type`（如 `PROGRESS`、`ALERT`、`STAGE_NODE`）使路由规则生效。

### 系统监控模块
- `SystemManagerEngine` 启动并注入 `MainEngine`、`EventEngine`。
- `MonitoringProcessV2` Level1（2–3 秒）与 Level2（30–60 秒）均完成，存在 `logs/monitor_ready.signal`。
- native_ipc 模块加载成功，`monitor_alerts`、`monitor_status`、`monitor_query` 管道通信正常。
- 监控事件（`EVENT_SYSTEM_METRICS` 等）持续发布并进入 LoggingHub → EventEngine → UI 告警链路。

### 启动流程
- 启动阶段：`LoggingInitStage` → `QtFrameworkStage` → `BackendInitStage` → `UIActivationStage` 全部成功。
- `BackendInitializerWorker`、`MonitorLauncherWorker` 执行完毕；UI 预加载任务无异常。
- 启动 AI 日志中显示 `StartupResult.success=True`。

---

## 软性/可选增强项
- **策略中心联动**：若需叠加策略信号或回放，确保策略服务、策略仓库、指标引擎已初始化。
- **日志数据库查询**：如需系统管理界面查询日志历史，确保 LoggingHub 注入数据库管理器。
- **网络测速/压力评估**：`BandwidthMonitor`、`NetworkSpeedTester` 生成最新缓存数据。
- **native_iocp 加速**：编译成功可避免多指标实时刷新时性能退化。

---

## 常用验证信号
- **启动终端**：阶段输出的 📍 / ✅ 标记。
- **AI 日志**：`logs/ai/` 中最新启动任务、数据任务记录。
- **系统监控 UI**：面板实时数据显示，无“加载中/无数据”。
- **日志文件**：`logs/terminal.log` 与 `logs/ai/*.log` 正常追加。
- **事件链路**：监听 `EVENT_MARKET_DATA`、`EVENT_SYSTEM_METRICS` 等事件持续到达。
- **数据质量**：UI/日志可查看最新质量扫描结果与状态。

---

## 推荐核查流程
1. 先确认启动阶段与 AI 日志，无失败节点。
2. 检查 LoggingHub 路由与日志输出是否完整。
3. 验证数据中心（Engine、LoadBalancer、实时推送、质量扫描）。
4. 检查系统监控进程 Level1/Level2 与事件链路。
5. 最后在 UI 中切换行情看板，确认 ChartWizard、告警、指标刷新全部正常。
