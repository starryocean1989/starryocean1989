# -*- coding: utf-8 -*-
# UI架构使用指南

## 🎯 概述

星辰金融终端v0.50已完成UI前端架构重构，新架构采用**左侧垂直导航 + 右侧内容区**的布局方式，更符合现代应用的使用习惯。

---

## 🏗️ 新架构说明

### 布局结构

```
┌─────────┬──────────────────────────────────────┐
│         │  菜单栏 & 工具栏                     │
│ 系统管理 ├──────────────────────────────────────┤
│         │                                      │
│ 数据中心 │                                      │
│         │                                      │
│ 行情看板 │          当前功能界面内容区           │
│         │                                      │
│ 策略中心 │                                      │
│         │                                      │
│ 交易网关 │                                      │
│         │                                      │
│ 组合投资 │                                      │
│         │                                      │
└─────────┴──────────────────────────────────────┘
   左侧导航              右侧内容显示区
   (垂直Tab)            (QStackedWidget)
```

### 主要特点

1. **左侧垂直导航Tab**
   - 固定宽度（120-150px）
   - 显示6个功能界面名称
   - 点击切换功能界面

2. **右侧内容显示区**
   - 自适应宽度
   - 根据左侧选择显示对应功能界面
   - 平滑切换动画

3. **6个功能界面**
   - 系统管理（8个子界面）
   - 数据中心（4个子界面）
   - 行情看板（单一界面）
   - 策略中心（混合架构：固有组件+2个子界面）
   - 交易网关（混合架构：固有组件+2个子界面）
   - 组合投资（混合架构：2个固有组件）

---

## 🚀 启动应用

### 方法1：使用启动脚本

```bash
# Windows
python start_terminal.py

# 或使用批处理文件
start_terminal.bat
```

### 方法2：直接启动UI

```bash
python -m ui.main_window
```

### 方法3：热更新启动

```bash
python hot_reload.py
```

---

## 📖 功能界面使用指南

### 1. 系统管理（标准架构）

**位置**: 左侧导航第1个
**架构**: 标准架构，8个子界面
**使用方式**: 点击进入后，顶部显示8个子界面选项卡

**子界面列表**:
1. 系统状态实时监控 - 查看CPU、内存、磁盘、网络状态
2. 性能指标展示 - 查看数据处理、策略执行性能
3. 告警信息管理 - 配置和管理系统告警
4. 服务健康检查 - 检查各服务健康状态
5. 系统配置 - 配置系统参数
6. 日志管理 - 查看和分析系统日志
7. 系统诊断 - 诊断系统问题
8. 工具集合 - 系统工具注册和管理

**操作提示**:
- 各子界面通过顶部Tab切换
- 实时监控数据每5秒自动更新
- 可通过刷新按钮手动刷新数据

### 2. 数据中心（标准架构）

**位置**: 左侧导航第2个
**架构**: 标准架构，4个子界面
**使用方式**: 点击进入后，顶部显示4个子界面选项卡

**子界面列表**:
1. 品种列表 - 管理和查看所有交易品种
2. 数据下载 - 下载历史数据（全量/增量）
3. 本地数据 - 查询和管理本地数据
4. 数据源管理 - 管理实时数据源连接

**操作提示**:
- **品种列表**: 支持按交易所和品种类型筛选、搜索
- **数据下载**:
  - 全量下载：下载所有品种的固定长度历史数据
  - 增量下载：选择日期范围下载指定时间段数据
- **本地数据**: 查询本地OHLCV数据，查看数据质量状态
- **数据源管理**: 配置ifind/rqdata/tushare等数据源

### 3. 行情看板（单一界面）

**位置**: 左侧导航第3个
**架构**: 单一集成界面（无子界面分割）
**使用方式**: 点击进入后显示完整的行情分析界面

**界面布局**:
```
┌─────────┬───────────────────┬─────────┐
│ 品种选择 │     主图区域      │ 叠加功能│
│ 实时行情 │   （K线/分时）    │ 控制功能│
│         ├───────────────────┤         │
│         │   技术指标副图    │         │
└─────────┴───────────────────┴─────────┘
```

**功能特点**:
- **主图**: K线/分时/tick切换，多周期支持
- **副图**: MACD/RSI/KDJ/BOLL等技术指标
- **叠加**: 品种叠加、指标叠加
- **坐标**: 普通坐标/对数坐标切换
- **实时**: 支持实时数据推送

**操作提示**:
- 左侧选择品种后，主图自动更新
- 右侧可配置叠加和坐标类型
- 副图指标可通过Tab切换显示

### 4. 策略指标中心（混合架构）

**位置**: 左侧导航第4个
**架构**: 混合架构（1个固有组件 + 2个子界面）
**使用方式**: 左侧显示策略管理器，右侧显示编写/回测界面

**界面布局**:
```
┌────────────┬─────────────────────────┐
│            │  编写 | 回测             │
│  策略/指标 ├─────────────────────────┤
│  管理器    │                         │
│  （固有）  │   子界面内容区          │
│            │                         │
└────────────┴─────────────────────────┘
```

**固有组件**:
- 策略/指标管理器（左侧，可调整大小、可隐藏）
- 树状图显示策略和指标文件
- 支持新建、重命名、删除、拖动

**子界面**:
1. 策略及指标编写 - 代码编辑器 + AI助手
2. 策略及指标回测 - 回测配置 + 结果展示

**操作提示**:
- 左侧管理器可通过拖动调整大小
- 编写界面右下角可打开AI助手
- 回测界面可自定义展示方式

### 5. 交易网关（混合架构）

**位置**: 左侧导航第5个
**架构**: 混合架构（1个固有组件 + 2个子界面）
**使用方式**: 左侧显示网关管理器，右侧显示策略/监控界面

**界面布局**:
```
┌────────────┬─────────────────────────┐
│            │  策略实例 | 交易监控    │
│  网关管理器├─────────────────────────┤
│  （固有）  │                         │
│            │   子界面内容区          │
│            │                         │
└────────────┴─────────────────────────┘
```

**支持的7种网关**:
- CTP - 国内期货期权
- CTP mini - 国内期货期权（迷你版）
- Sopt - 国内ETF期权
- tts - 期货仿真交易
- ib - 海外证券期货期权
- paperaccount - 本地模拟交易
- TDX gateway - 国内股票交易

**操作提示**:
- 左侧点击"新建网关"创建网关实例
- 不同网关类型需要填写不同的配置项
- 连接网关时需要输入密码
- 策略实例界面可部署和管理策略
- 交易监控根据策略类型自动适配监控界面

### 6. 组合投资（混合架构）

**位置**: 左侧导航第6个
**架构**: 混合架构（2个固有组件，无独立子界面）
**使用方式**: 左侧显示组合管理，右侧显示组合监控

**界面布局**:
```
┌────────────┬─────────────────────────┐
│            │                         │
│  组合管理  │    组合投资监控         │
│  （固有1） │    （固有2）            │
│            │                         │
└────────────┴─────────────────────────┘
```

**组合类型**:
1. **自动组合** - 激活>1个策略的网关自动识别（不可删除）
2. **自定义组合** - 用户自定义创建的组合（可删除）

**操作提示**:
- 左侧点击"新建组合"创建自定义组合
- 右侧以选项卡方式显示各组合的监控数据
- 每个选项卡包含实时业绩风控和历史分析

---

## ⚙️ 配置和自定义

### 主题配置

```python
# 在菜单栏 -> 视图 -> 主题中切换
- 暗黑主题（默认）
```

### 窗口配置

```python
# 配置文件: config/terminal_config.json
{
    "ui": {
        "window_width": 1200,
        "window_height": 800,
        "theme": "dark"
    }
}
```

### 快捷键

| 功能 | 快捷键 |
|-----|--------|
| 刷新界面 | F5 |
| 退出程序 | Ctrl+Q |

---

## 🧪 测试和验证

### 自动化测试

```bash
python test_ui_refactor.py
```

**测试内容**:
- ✅ 主窗口组件创建验证
- ✅ 导航Tab数量和名称验证
- ✅ 内容区widget数量验证
- ✅ 功能界面实例化验证
- ✅ 界面切换功能验证

### 手动测试检查项

启动应用后，请检查：

1. **主窗口布局**
   - [ ] 左侧显示6个功能界面的垂直Tab
   - [ ] 右侧显示当前选中的功能界面内容
   - [ ] 分割器可以拖动调整比例

2. **导航功能**
   - [ ] 点击左侧Tab，右侧内容正确切换
   - [ ] 状态栏显示当前界面名称
   - [ ] 切换流畅无卡顿

3. **标准架构界面**
   - [ ] 系统管理：顶部显示8个子界面Tab
   - [ ] 数据中心：顶部显示4个子界面Tab
   - [ ] 子界面切换正常

4. **混合架构界面**
   - [ ] 策略中心：左侧管理器 + 右侧2个子界面
   - [ ] 交易网关：左侧管理器 + 右侧2个子界面
   - [ ] 组合投资：左右两个固有组件

5. **单一界面**
   - [ ] 行情看板：完整的三栏布局

---

## 🐛 常见问题

### Q: 界面显示不完整或错位？

**A**:
1. 检查窗口尺寸，最小尺寸为800x600
2. 尝试最大化窗口
3. 重启应用程序

### Q: 左侧导航Tab不显示？

**A**:
1. 检查日志文件：logs/terminal_v0.50.log
2. 确认主窗口初始化成功
3. 尝试删除配置文件重新启动

### Q: 点击导航Tab无反应？

**A**:
1. 检查信号连接是否正常
2. 查看控制台是否有错误信息
3. 尝试使用菜单栏的快捷按钮切换

### Q: 某个功能界面加载失败？

**A**:
1. 查看界面上的错误提示
2. 检查后端模块是否正确安装
3. 查看日志文件获取详细错误信息

---

## 📚 开发者指南

### 添加新功能界面

1. 在`ui/components/`下创建新目录
2. 创建`main_view.py`并实现界面类
3. 在`ui/main_window.py`中导入并注册
4. 添加到`create_function_interfaces()`方法中

```python
# 示例代码
self.function_interfaces["new_feature"] = NewFeature()
if self.content_stack is not None:
    self.content_stack.addWidget(self.function_interfaces["new_feature"])
if self.tab_widget is not None:
    nav_placeholder = QWidget()
    self.tab_widget.addTab(nav_placeholder, "新功能")
```

### 修改布局比例

```python
# 在 ui/main_window.py 的 setup_ui() 方法中
main_splitter.setSizes([140, 1000])  # [左侧宽度, 右侧宽度]
```

---

## 📝 更新日志

### v0.50 (2025-10-07)
- ✅ 重构主窗口布局为左侧垂直导航
- ✅ 删除临时开发模块ops_center
- ✅ 验证所有6个功能界面的架构
- ✅ 添加自动化测试脚本
- ✅ 完善文档和使用指南

---

## 🔗 相关文档

- [UI_REFACTOR_SUMMARY.md](./UI_REFACTOR_SUMMARY.md) - 重构总结文档
- [README_启动指南.md](./README_启动指南.md) - 启动指南
- [功能链条分解文档.md](./docs/1.权威需求文档/功能链条分解文档.md) - 详细需求
- [6个功能界面.md](./docs/1.权威需求文档/6个功能界面.md) - 界面设计需求

---

**维护者**: 开发团队
**最后更新**: 2025-10-07
**版本**: v0.50

