# 星辰金融终端多进程架构改造指南

**文档版本**: v1.0
**创建日期**: 2025-01-15
**维护者**: 架构团队
**状态**: 正式版 - 用于指导AI重构

---

## 📋 文档目录

- [1. 概述与背景](#1-概述与背景)
- [2. 当前架构分析](#2-当前架构分析)
- [3. 核心决策与原则](#3-核心决策与原则)
- [4. 模块分析与决策矩阵](#4-模块分析与决策矩阵)
- [5. 目标架构设计](#5-目标架构设计)
- [6. 技术方案](#6-技术方案)
- [7. 实施路线图](#7-实施路线图)
- [8. 代码实现指南](#8-代码实现指南)
- [9. 测试与验证](#9-测试与验证)
- [10. 风险与注意事项](#10-风险与注意事项)

---

## 1. 概述与背景

### 1.1 项目背景

**项目名称**: 星辰金融终端 (Terminal v0.50)
**技术栈**: Python 3.10 + PySide6 (Qt) + VnPy + data_module_vnpy
**当前状态**: 基本完成但面临启动崩溃问题

### 1.2 改造目标

本次多进程改造的核心目标：

1. **解决启动崩溃问题** - 主要由日志/告警系统的跨线程UI更新导致
2. **提升系统稳定性** - 通过进程隔离防止故障传播
3. **优化启动性能** - 保持已有的分层启动优化（<2秒UI可见）
4. **保持高性能** - 避免高频数据访问的IPC开销

### 1.3 改造范围

| 改造类型 | 模块 | 优先级 |
|---------|------|--------|
| **必须改造** | 日志/告警系统 | P0 - 立即实施 |
| **保持现状** | UI层、数据服务、策略回测、行情看板、交易网关 | 核心性能模块 |
| **可选改造** | AI助手、组合投资 | P2 - 观察后决定 |

### 1.4 关键约束

1. **Qt框架限制** - QApplication和所有UI组件必须在主进程主线程
2. **性能要求** - 数据访问频率高（每天数千到数万次），不能引入显著IPC开销
3. **架构完整性** - 已有的统一数据管理器（UnifiedDataManager）是核心数据枢纽
4. **向后兼容** - 改造应尽量保持现有接口不变

---

## 2. 当前架构分析

### 2.1 系统架构概览

```
当前架构（单进程多线程）
┌─────────────────────────────────────────────────────────────┐
│                     主进程 (唯一进程)                        │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Qt主线程                                             │  │
│  │  ├─ QApplication                                     │  │
│  │  ├─ MainWindow                                       │  │
│  │  └─ 6个功能界面UI                                    │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  业务服务层（多线程）                                 │  │
│  │  ├─ DataCenterService                                │  │
│  │  │   └─ data_module_vnpy                             │  │
│  │  │       └─ UnifiedDataManager ⭐ 核心数据枢纽        │  │
│  │  ├─ StrategyCenterService                            │  │
│  │  ├─ MarketBoardService                               │  │
│  │  ├─ TradingGatewayService                            │  │
│  │  │   └─ VnPy MainEngine + EventEngine                │  │
│  │  ├─ PortfolioService                                 │  │
│  │  └─ SystemManagerService                             │  │
│  │      ├─ LoggingSystem ⚠️ 崩溃源头                    │  │
│  │      └─ AlertEngine ⚠️ 崩溃源头                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  问题：日志/告警在后台线程运行，但尝试更新Qt UI           │
│       → 违反Qt单线程规则 → 启动时随机崩溃                 │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 6个功能界面

根据需求文档（`docs/1.权威需求文档/6个功能界面.md`），系统包含6个主要功能界面：

| 序号 | 功能界面 | 子界面数 | 架构特点 | 数据依赖 |
|-----|---------|---------|---------|---------|
| 1 | 系统管理 | 8个 | 标准架构 | 监控所有模块 |
| 2 | 数据中心 | 4个 | 标准架构 | UnifiedDataManager |
| 3 | 行情看板 | 0个 | 单一界面 | 高频访问数据 |
| 4 | 策略指标中心 | 2个 | 混合架构 | 高频访问数据（回测） |
| 5 | 交易网关 | 2个 | 混合架构 | 中高频访问数据（策略执行） |
| 6 | 组合投资 | 0个 | 混合架构 | 低频监控 |

### 2.3 核心数据流

```
UnifiedDataManager（统一数据管理器）
├─ 数据源层
│   ├─ 历史数据（StorageManager）
│   ├─ 实时数据（Realtime Layer）
│   ├─ 录制数据（Recording Layer）
│   └─ 预加载缓存（PreloadService）
│
└─ 被以下模块访问
    ├─ 行情看板 ──────► 每秒多次（图表更新）
    ├─ 策略回测 ──────► 批量查询（回测时大量历史数据）
    └─ 交易网关 ──────► 每分钟或每秒（策略执行时获取历史bar）
        └─ VnPy策略引擎 ──► load_bar() 三级回退
            1. Gateway历史数据
            2. Datafeed接口
            3. VnPy Database
```

**关键发现**：统一数据管理器是所有数据访问的中心，高频访问（每天数千到数万次）。

### 2.4 已知问题

#### 问题1：启动崩溃（最严重）

**现象**：
- 启动时随机崩溃，崩溃率约30%
- 崩溃点：`SystemManagerService` 初始化日志/告警系统时

**根本原因**：
```python
# backend/services/system_manager_service.py
def _init_logging_and_alert_system(self):
    """在后台线程中初始化日志和告警系统"""
    # 问题：后台线程创建的告警规则尝试更新UI
    create_default_log_alert_rules()  # ⚠️ 这里会触发Qt UI更新

    # Qt规则：所有UI操作必须在主线程
    # 违反此规则 → 随机崩溃
```

**影响**：
- 启动失败率高，用户体验极差
- 开发调试困难
- 生产环境不可用

#### 问题2：潜在的线程安全问题

- 多个服务在不同线程访问共享资源
- EventEngine事件分发的线程安全性
- 配置文件的并发读写

---

## 3. 核心决策与原则

### 3.1 架构决策原则

#### 原则1：性能优先原则

**核心观点**：高频数据访问的模块必须保持在主进程

**理由**：
```python
# 性能对比分析
同进程访问：
- 数据访问延迟：0.1ms
- 100次查询：10ms
- 每天10000次：1秒

跨进程访问（IPC）：
- 数据访问延迟：5-10ms (序列化+传输+反序列化)
- 100次查询：500-1000ms
- 每天10000次：50-100秒
- 性能损失：50-100倍 ⚠️
```

**适用模块**：
- ✅ 数据服务（UnifiedDataManager）- 核心数据枢纽
- ✅ 行情看板 - 实时图表更新
- ✅ 策略回测 - 大量历史数据查询
- ✅ 交易网关 - 策略执行时的数据访问

#### 原则2：故障隔离原则

**核心观点**：高风险、易崩溃的模块应独立进程

**判断标准**：
1. 是否有跨线程UI更新？（Qt违规操作）
2. 是否有网络IO阻塞风险？
3. 是否运行不可控的第三方代码？
4. 崩溃是否会影响核心功能？

**适用模块**：
- ✅ 日志/告警系统 - 跨线程UI更新，已知崩溃源
- 🔶 交易网关 - VnPy事件循环，但数据访问频繁（权衡后保持主进程）
- 🔶 AI助手 - 网络请求可能阻塞（可选独立）

#### 原则3：最小改动原则

**核心观点**：只改造必要的模块，避免过度设计

**理由**：
- 多进程架构复杂度高
- IPC通信增加维护成本
- 数据一致性问题
- 调试困难度增加

**实施策略**：
- ✅ 优先解决已知问题（日志/告警崩溃）
- ✅ 保持核心性能模块在主进程
- 🔶 其他模块观察运行，有问题再改

#### 原则4：渐进式改造原则

**核心观点**：分阶段实施，每个阶段可独立验证

**实施顺序**：
```
阶段0：准备工作（1天）
  └─ 代码审查、备份、测试环境准备

阶段1：日志/告警进程隔离（2-3天）⚡ 最高优先级
  ├─ 创建独立进程
  ├─ 实现IPC通信
  └─ 验证崩溃问题解决

阶段2：进程管理框架（2-3天）
  ├─ 进程生命周期管理
  ├─ 健康监控
  └─ 自动重启机制

阶段3：可选模块改造（按需）
  ├─ AI助手进程（如果网络阻塞严重）
  └─ 其他模块（根据运行情况决定）
```

### 3.2 Qt框架约束

#### 约束1：QApplication单例限制

```python
# ❌ 错误：不能在多个进程中创建QApplication
# 主进程
app = QApplication(sys.argv)

# 子进程（错误！）
def subprocess_main():
    app2 = QApplication(sys.argv)  # 会失败或行为异常
```

**结论**：QApplication和所有UI必须在主进程。

#### 约束2：Qt对象不可序列化

```python
# ❌ 错误：Qt对象不能通过multiprocessing传递
window = QMainWindow()
queue.put(window)  # TypeError: cannot pickle 'QMainWindow'

# ✅ 正确：只传递数据，在主进程中更新UI
data = {'level': 'ERROR', 'message': 'something wrong'}
queue.put(data)  # OK，纯Python数据
```

**结论**：子进程只能发送数据，不能直接操作UI。

#### 约束3：线程安全规则

```python
# Qt的线程安全规则
# 1. 所有UI操作必须在主线程
# 2. 跨线程更新UI必须使用信号槽或QMetaObject.invokeMethod

# ❌ 错误：在子线程直接更新UI
def worker_thread():
    label.setText("Update")  # 崩溃风险

# ✅ 正确：通过信号更新
class Worker(QObject):
    update_signal = Signal(str)

    def run(self):
        self.update_signal.emit("Update")  # 安全

# 在主线程连接
worker.update_signal.connect(label.setText)
```

### 3.3 数据访问模式分析

#### 模式1：高频小数据（保持主进程）

**特征**：
- 访问频率：每秒多次
- 数据量：小（KB级别）
- 延迟敏感：是

**示例**：
```python
# 行情看板：实时图表更新（60fps）
for tick in realtime_ticks:
    kline_data = unified_manager.get_kline(symbol, '1m', 100)  # 5KB
    chart.update(kline_data)  # 每秒60次

# 如果跨进程：5-10ms延迟 × 60次 = 300-600ms/秒
# 结果：卡顿，无法60fps
```

**结论**：必须保持主进程。

#### 模式2：批量大数据（保持主进程更优）

**特征**：
- 访问频率：低（回测时）
- 数据量：大（MB级别）
- 延迟敏感：中等

**示例**：
```python
# 策略回测：获取多年历史数据
symbols = ['000001.SZ', '600000.SH', ...]  # 100个品种
for symbol in symbols:
    df = unified_manager.get_kline(symbol, '1d', start='2020-01-01', end='2024-01-01')
    # 每个品种：1000根K线 × 10列 × 8字节 = 80KB
    # 总计：100 × 80KB = 8MB

# 同进程：8MB内存拷贝 ~ 10ms
# 跨进程：序列化(100ms) + 传输(50ms) + 反序列化(100ms) = 250ms
# 差距：25倍
```

**结论**：同进程性能优势明显。

#### 模式3：低频控制命令（可跨进程）

**特征**：
- 访问频率：低（用户触发）
- 数据量：小（字节级别）
- 延迟敏感：否

**示例**：
```python
# 用户点击"启动策略"按钮
command = {
    'action': 'start_strategy',
    'strategy_name': 'ma_cross',
    'params': {...}
}
# 通过IPC发送到交易网关进程
# 延迟：10ms，用户无感知
```

**结论**：适合跨进程。

#### 模式4：事件推送（可跨进程）

**特征**：
- 推送频率：中等（每秒几次到几十次）
- 数据量：小（KB级别）
- 延迟要求：低（100ms内可接受）

**示例**：
```python
# 交易网关推送订单成交事件到UI
trade_event = {
    'symbol': '000001.SZ',
    'price': 10.5,
    'volume': 100,
    'time': '2024-01-15 10:30:00'
}
# 通过ZeroMQ PUB/SUB推送
# 延迟：5-10ms，可接受
```

**结论**：适合跨进程。

### 3.4 核心决策总结

基于以上分析，**最终架构决策**：

| 模块 | 是否独立进程 | 决策理由 | 优先级 |
|-----|------------|---------|--------|
| **UI层** | ❌ 主进程 | Qt强制要求 | - |
| **数据服务** | ❌ 主进程 | 高频访问，性能优先 | - |
| **策略回测** | ❌ 主进程 | 大量数据查询，性能优先 | - |
| **行情看板** | ❌ 主进程 | 实时更新，性能优先 | - |
| **交易网关** | ❌ 主进程 | 频繁数据访问，性能优先 | - |
| **系统管理** | ❌ 主进程 | 监控方便（除日志/告警） | - |
| **日志/告警** | ✅ 独立进程 | 跨线程UI更新，已知崩溃源 | **P0** |
| **AI助手** | 🔶 可选 | 网络请求可能阻塞 | P2 |
| **组合投资** | 🔶 可选 | 低频监控，隔离价值不大 | P2 |

**关键洞察**：
1. **不是所有模块都需要独立进程** - 只隔离必要的模块
2. **性能是首要考虑因素** - 数据密集型模块保持主进程
3. **日志/告警是唯一必须独立的** - 解决当前崩溃问题
4. **交易网关保持主进程** - 虽然有风险，但性能损失太大

---

## 4. 目标架构设计

### 4.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                    主进程 (Main Process)                         │
│                    PID: 主进程ID                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Qt主线程                                                 │  │
│  │  ├─ QApplication                                         │  │
│  │  ├─ MainWindow                                           │  │
│  │  └─ 6个功能界面UI                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  核心业务服务（同进程，内存访问）                         │  │
│  │                                                            │  │
│  │  ┌──────────────────────────────────────────────────┐   │  │
│  │  │  UnifiedDataManager（核心数据枢纽）              │   │  │
│  │  │  ├─ 预加载缓存                                  │   │  │
│  │  │  ├─ 历史存储                                    │   │  │
│  │  │  ├─ 录制数据                                    │   │  │
│  │  │  └─ 实时数据                                    │   │  │
│  │  └──────────────────────────────────────────────────┘   │  │
│  │           ▲              ▲              ▲                 │  │
│  │           │              │              │                 │  │
│  │  ┌────────┴────┐  ┌─────┴──────┐  ┌───┴────────┐       │  │
│  │  │ 行情看板     │  │ 策略回测    │  │ 交易网关    │       │  │
│  │  │ (高频访问)   │  │ (批量查询)  │  │ (中高频)    │       │  │
│  │  └─────────────┘  └────────────┘  └────────────┘       │  │
│  │                                                            │  │
│  │  ├─ SystemManagerService（除日志/告警）                   │  │
│  │  ├─ PortfolioService                                     │  │
│  │  └─ AIAssistantService（可选）                           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  IPC通信层                                                │  │
│  │  ├─ LogAlertClient（连接日志/告警进程）                  │  │
│  │  └─ AlertSubscriber（订阅告警推送）                      │  │
│  └──────────────────────────────────────────────────────────┘  │
└──────────────────┬───────────────────────────────────────────────┘
                   │ ZeroMQ IPC
                   │ (日志推送/告警订阅)
        ┌──────────▼──────────────┐
        │  子进程1: 日志/告警进程  │
        │  PID: 子进程ID          │
        ├─────────────────────────┤
        │  • LoggingSystem        │
        │  • AlertEngine          │
        │  • 日志聚合             │
        │  • 告警规则引擎         │
        │  • ZeroMQ服务器         │
        │    - PULL端口(日志接收) │
        │    - PUB端口(告警推送)  │
        └─────────────────────────┘
```

### 4.2 进程职责划分

#### 主进程职责

| 职责类型 | 具体内容 |
|---------|---------|
| **UI渲染** | Qt应用、所有窗口和控件 |
| **数据管理** | UnifiedDataManager、数据存储、缓存管理 |
| **业务逻辑** | 策略回测、行情分析、交易执行、投资组合管理 |
| **IPC客户端** | 与子进程通信（发送日志、接收告警） |
| **进程管理** | 启动/停止/监控子进程 |

#### 日志/告警进程职责

| 职责类型 | 具体内容 |
|---------|---------|
| **日志聚合** | 接收所有进程的日志，统一存储 |
| **告警引擎** | 运行告警规则，检测异常 |
| **通知发送** | 邮件、Webhook、消息推送 |
| **IPC服务器** | 提供日志接收服务、告警推送服务 |
| **数据持久化** | 日志数据库、告警历史 |

### 4.3 进程间通信（IPC）设计

#### 通信方案选择

**选用ZeroMQ的理由**：

| 对比项 | ZeroMQ | multiprocessing.Queue | gRPC |
|-------|--------|----------------------|------|
| **性能** | 高（μs级延迟） | 中等 | 中等 |
| **灵活性** | 多种模式（PUSH/PULL、PUB/SUB） | 简单队列 | RPC调用 |
| **跨平台** | ✅ 优秀 | ✅ 可以 | ✅ 可以 |
| **学习曲线** | 中等 | 简单 | 较陡 |
| **适用场景** | **日志聚合、事件推送** | 简单通信 | 微服务 |

**选择**：ZeroMQ（最适合本项目需求）

#### 通信模式设计

```python
# 模式1：日志推送（PUSH/PULL）
主进程 ──PUSH──► 日志/告警进程
  │               │
  │               ├─ 接收日志
  │               ├─ 存储到数据库
  │               └─ 触发告警规则检查
  │
  └─ 所有业务服务异步发送日志

# 模式2：告警订阅（PUB/SUB）
日志/告警进程 ──PUB──► 主进程
  │                     │
  ├─ 发布告警事件       └─ 订阅告警
  │                       ├─ 更新UI（告警滚动条）
  └─ topic过滤           └─ 触发通知
```

#### 端口分配

| 服务 | 端口 | 模式 | 用途 |
|-----|------|------|------|
| 日志接收 | 5557 | PULL | 接收所有进程的日志 |
| 告警推送 | 5558 | PUB | 推送告警到UI |
| 健康检查 | 5559 | REP | 进程健康状态查询 |

### 4.4 数据流示意图

```
┌─────────────────────────────────────────────────────────┐
│  主进程                                                  │
│                                                          │
│  1. 业务代码生成日志                                     │
│     ├─ logger.info("策略启动")                          │
│     └─ logger.error("连接失败")                         │
│          │                                               │
│          ▼                                               │
│  2. DistributedLogHandler拦截                           │
│     ├─ 格式化日志条目                                    │
│     ├─ 添加元数据（进程ID、线程ID、时间戳）            │
│     └─ 序列化（msgpack）                                │
│          │                                               │
│          ▼                                               │
│  3. ZeroMQ PUSH Socket                                  │
│     └─ 异步发送到日志进程                               │
│          │                                               │
└──────────┼─────────────────────────────────────────────┘
           │ IPC (ZeroMQ)
           ▼
┌─────────────────────────────────────────────────────────┐
│  日志/告警进程                                           │
│                                                          │
│  4. ZeroMQ PULL Socket                                  │
│     └─ 接收日志条目                                      │
│          │                                               │
│          ▼                                               │
│  5. 日志处理流程                                         │
│     ├─ 反序列化                                          │
│     ├─ 写入数据库                                        │
│     ├─ 日志分析                                          │
│     └─ 触发告警规则检查                                  │
│          │                                               │
│          ▼                                               │
│  6. 告警引擎                                             │
│     ├─ 规则匹配                                          │
│     ├─ 聚合（避免告警风暴）                             │
│     └─ 生成告警事件                                      │
│          │                                               │
│          ▼                                               │
│  7. ZeroMQ PUB Socket                                   │
│     └─ 推送告警到订阅者                                  │
│          │                                               │
└──────────┼─────────────────────────────────────────────┘
           │ IPC (ZeroMQ)
           ▼
┌─────────────────────────────────────────────────────────┐
│  主进程                                                  │
│                                                          │
│  8. ZeroMQ SUB Socket                                   │
│     └─ 接收告警事件                                      │
│          │                                               │
│          ▼                                               │
│  9. 主线程安全更新                                       │
│     ├─ QMetaObject.invokeMethod                         │
│     └─ 在Qt主线程中更新UI                               │
│          │                                               │
│          ▼                                               │
│  10. UI更新                                              │
│      ├─ 状态栏告警滚动条                                 │
│      ├─ 告警弹窗                                         │
│      └─ 系统管理界面告警列表                             │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 4.5 异常处理和容错机制

#### 子进程崩溃处理

```python
# 进程管理器监控子进程
class ProcessManager:
    def monitor_process(self, process):
        """监控子进程健康状态"""
        while True:
            if not process.is_alive():
                # 子进程已退出
                exit_code = process.exitcode
                if exit_code != 0:
                    # 异常退出
                    logger.error(f"子进程异常退出，退出码：{exit_code}")
                    # 自动重启
                    self.restart_process(process)
                break

            # 心跳检查
            if not self.check_heartbeat(process):
                logger.warning("子进程心跳超时，可能卡死")
                # 强制终止并重启
                process.terminate()
                process.join(timeout=5)
                if process.is_alive():
                    process.kill()  # 强制杀死
                self.restart_process(process)

            time.sleep(5)  # 每5秒检查一次
```

#### IPC连接断开处理

```python
# 日志发送器自动重连
class ResilientLogSender:
    def send_log(self, log_entry):
        """发送日志，带重试机制"""
        retry_count = 0
        max_retries = 3

        while retry_count < max_retries:
            try:
                self.socket.send(msgpack.packb(log_entry))
                return True
            except zmq.ZMQError as e:
                retry_count += 1
                if retry_count < max_retries:
                    logger.warning(f"日志发送失败，第{retry_count}次重试")
                    time.sleep(1)
                    # 重新连接
                    self.reconnect()
                else:
                    # 达到最大重试次数，降级到本地文件
                    self.fallback_to_file(log_entry)
                    return False
```

#### 降级方案

```python
# 日志系统降级
class LoggingFallback:
    """日志进程不可用时的降级方案"""

    def fallback_to_file(self, log_entry):
        """降级到本地文件日志"""
        local_file = "logs/fallback.log"
        with open(local_file, 'a', encoding='utf-8') as f:
            f.write(json.dumps(log_entry) + '\n')

    def batch_sync_on_recovery(self):
        """日志进程恢复后，同步降级期间的日志"""
        fallback_logs = self.read_fallback_logs()
        for log in fallback_logs:
            self.send_to_log_process(log)
        # 清空降级日志
        os.remove("logs/fallback.log")
```

---

## 5. 实施路线图

### 5.1 总体时间表

| 阶段 | 内容 | 工期 | 优先级 | 验收标准 |
|-----|------|------|--------|---------|
| **阶段0** | 准备工作 | 0.5天 | P0 | 代码备份完成，环境准备就绪 |
| **阶段1** | 日志/告警进程隔离 | 2-3天 | P0 | 启动崩溃率 < 1% |
| **阶段2** | 进程管理框架 | 2-3天 | P1 | 进程监控和自动重启可用 |
| **阶段3** | 测试和优化 | 1-2天 | P1 | 回归测试通过 |
| **阶段4** | 可选模块改造 | 按需 | P2 | 根据运行情况决定 |

### 5.2 阶段0：准备工作（0.5天）

#### 任务清单

- [ ] **代码备份**
  ```bash
  # 创建备份分支
  git checkout -b backup-before-multiprocess
  git commit -am "Backup before multiprocess refactor"
  git push origin backup-before-multiprocess
  ```

- [ ] **安装依赖**
  ```bash
  # 安装ZeroMQ
  pip install pyzmq msgpack-python

  # 验证安装
  python -c "import zmq; import msgpack; print('Dependencies OK')"
  ```

- [ ] **创建目录结构**
  ```bash
  mkdir -p backend/processes
  mkdir -p logs/fallback
  ```

- [ ] **代码审查**
  - 审查 `backend/core/alert_system.py`
  - 审查 `backend/core/logging_system.py`
  - 审查 `backend/services/system_manager_service.py`
  - 识别所有调用日志/告警的代码位置

### 5.3 阶段1：日志/告警进程隔离（2-3天）

#### 子任务分解

##### 任务1.1：创建日志/告警进程（0.5天）

**文件**: `backend/processes/log_alert_process.py`

**任务**：
1. 创建进程入口函数
2. 初始化ZeroMQ服务器
3. 实现日志接收循环
4. 实现告警推送机制

**验收**：
- 进程能独立启动
- ZeroMQ端口监听成功
- 日志接收功能正常

##### 任务1.2：实现IPC通信层（1天）

**文件**: `backend/core/ipc/` (新建目录)

**任务**：
1. 创建 `distributed_log_handler.py` - 分布式日志处理器
2. 创建 `alert_subscriber.py` - 告警订阅客户端
3. 创建 `ipc_config.py` - IPC配置管理
4. 实现连接池和重连机制

**验收**：
- 日志可以发送到子进程
- 告警可以从子进程接收
- 网络断开自动重连

##### 任务1.3：修改SystemManagerService（0.5天）

**文件**: `backend/services/system_manager_service.py`

**任务**：
1. 移除日志/告警系统的直接初始化
2. 添加IPC客户端
3. 通过IPC连接到日志/告警进程
4. 修改告警UI更新逻辑（使用Qt信号槽）

**验收**：
- SystemManagerService不再直接初始化日志/告警
- 通过IPC正常通信
- UI更新不再有跨线程问题

##### 任务1.4：集成到启动流程（0.5-1天）

**文件**: `start_async_fixed.py`

**任务**：
1. 在主进程启动前启动日志/告警进程
2. 等待日志/告警进程就绪
3. 配置主进程的日志处理器
4. 添加进程清理逻辑

**验收**：
- 启动顺序正确
- 主进程能正常连接到子进程
- 退出时子进程能正常清理

### 5.4 阶段2：进程管理框架（2-3天）

#### 子任务分解

##### 任务2.1：创建进程管理器（1天）

**文件**: `backend/core/process_manager.py`

**任务**：
1. 实现进程生命周期管理
2. 实现进程监控和心跳检查
3. 实现自动重启机制
4. 实现进程健康状态查询

**验收**：
- 能启动/停止子进程
- 子进程崩溃后自动重启
- 健康状态查询可用

##### 任务2.2：实现心跳机制（0.5天）

**文件**: 修改 `backend/processes/log_alert_process.py`

**任务**：
1. 添加REP socket监听健康检查请求
2. 定期更新心跳时间戳
3. 响应健康检查查询

**验收**：
- 主进程能查询子进程健康状态
- 心跳超时能被检测到

##### 任务2.3：集成到MainWindow（0.5-1天）

**文件**: `ui/main_window.py`

**任务**：
1. 在MainWindow中创建进程管理器
2. 启动时启动子进程
3. 退出时清理子进程
4. 显示子进程状态

**验收**：
- UI能显示子进程运行状态
- 应用退出时子进程正常关闭
- 子进程异常时UI有提示

### 5.5 阶段3：测试和优化（1-2天）

#### 测试清单

- [ ] **功能测试**
  - [ ] 启动测试（连续启动100次，崩溃率 < 1%）
  - [ ] 日志功能测试（各级别日志正常显示）
  - [ ] 告警功能测试（告警触发和UI更新）
  - [ ] 进程重启测试（手动杀死子进程，验证自动重启）

- [ ] **性能测试**
  - [ ] 日志发送延迟测试（< 10ms）
  - [ ] 告警推送延迟测试（< 50ms）
  - [ ] 内存泄漏测试（长时间运行）

- [ ] **稳定性测试**
  - [ ] 长时间运行测试（24小时）
  - [ ] 网络断开恢复测试
  - [ ] 子进程崩溃恢复测试

- [ ] **回归测试**
  - [ ] 6个功能界面全部可用
  - [ ] 数据中心功能正常
  - [ ] 策略回测功能正常
  - [ ] 交易网关功能正常

---

## 6. 代码实现指南

### 6.1 日志/告警进程实现

#### 文件：backend/processes/__init__.py

```python
"""多进程模块."""
```

#### 文件：backend/processes/log_alert_process.py

```python
# -*- coding: utf-8 -*-
"""日志/告警独立进程实现."""

import os
import sys
import time
import logging
from pathlib import Path
from typing import Dict, Any

# 添加项目根目录到Python路径
project_root = Path(__file__).parent.parent.parent
if str(project_root) not in sys.path:
    sys.path.insert(0, str(project_root))

import zmq
import msgpack


class LogAlertProcess:
    """日志/告警独立进程.

    职责：
    1. 接收所有进程的日志
    2. 日志聚合和存储
    3. 告警规则检查
    4. 告警推送到UI
    """

    def __init__(
        self,
        log_port: int = 5557,
        alert_port: int = 5558,
        health_port: int = 5559
    ):
        """初始化日志/告警进程.

        Args:
            log_port: 日志接收端口（PULL）
            alert_port: 告警推送端口（PUB）
            health_port: 健康检查端口（REP）
        """
        self.log_port = log_port
        self.alert_port = alert_port
        self.health_port = health_port

        # ZeroMQ上下文和socket
        self.context = None
        self.log_receiver = None  # PULL socket
        self.alert_publisher = None  # PUB socket
        self.health_responder = None  # REP socket

        # 日志和告警系统（在进程中初始化，避免主进程问题）
        self.logging_system = None
        self.alert_engine = None

        # 心跳时间戳
        self.last_heartbeat = time.time()

        # 日志计数器（用于监控）
        self.log_count = 0
        self.alert_count = 0

        # 设置本地日志
        self.logger = self._setup_local_logger()

    def _setup_local_logger(self) -> logging.Logger:
        """设置进程本地日志（不通过IPC，避免循环）."""
        logger = logging.getLogger('LogAlertProcess')
        logger.setLevel(logging.INFO)

        # 只输出到文件
        handler = logging.FileHandler('logs/log_alert_process.log', encoding='utf-8')
        formatter = logging.Formatter(
            '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
        )
        handler.setFormatter(formatter)
        logger.addHandler(handler)

        return logger

    def run(self):
        """进程主循环."""
        try:
            self.logger.info("=" * 70)
            self.logger.info(f"日志/告警进程启动 PID={os.getpid()}")
            self.logger.info("=" * 70)

            # 初始化ZeroMQ
            self._init_zmq()

            # 初始化日志和告警系统
            self._init_logging_and_alert()

            # 进入主循环
            self._main_loop()

        except KeyboardInterrupt:
            self.logger.info("收到中断信号，正在退出...")
        except Exception as e:
            self.logger.error(f"进程异常: {e}", exc_info=True)
        finally:
            self._cleanup()

    def _init_zmq(self):
        """初始化ZeroMQ."""
        self.logger.info("初始化ZeroMQ...")

        self.context = zmq.Context()

        # 日志接收器（PULL模式）
        self.log_receiver = self.context.socket(zmq.PULL)
        self.log_receiver.bind(f"tcp://127.0.0.1:{self.log_port}")
        self.logger.info(f"✅ 日志接收器监听端口 {self.log_port}")

        # 告警发布器（PUB模式）
        self.alert_publisher = self.context.socket(zmq.PUB)
        self.alert_publisher.bind(f"tcp://127.0.0.1:{self.alert_port}")
        self.logger.info(f"✅ 告警发布器监听端口 {self.alert_port}")

        # 健康检查响应器（REP模式）
        self.health_responder = self.context.socket(zmq.REP)
        self.health_responder.bind(f"tcp://127.0.0.1:{self.health_port}")
        self.logger.info(f"✅ 健康检查监听端口 {self.health_port}")

    def _init_logging_and_alert(self):
        """初始化日志和告警系统."""
        self.logger.info("初始化日志和告警系统...")

        try:
            # 导入并初始化（在子进程中，安全）
            from backend.core.logging_system import LoggingSystem
            from backend.core.alert_system import AlertEngine
            from backend.core.default_alert_rules import create_default_log_alert_rules

            self.logging_system = LoggingSystem()
            self.alert_engine = AlertEngine()

            # 创建默认告警规则（在子进程中，不会影响主进程）
            create_default_log_alert_rules()

            self.logger.info("✅ 日志和告警系统初始化成功")

        except Exception as e:
            self.logger.error(f"❌ 日志和告警系统初始化失败: {e}", exc_info=True)
            raise

    def _main_loop(self):
        """主循环：处理日志、告警、健康检查."""
        self.logger.info("进入主循环...")

        # 创建poller监听多个socket
        poller = zmq.Poller()
        poller.register(self.log_receiver, zmq.POLLIN)
        poller.register(self.health_responder, zmq.POLLIN)

        while True:
            try:
                # 轮询，超时100ms
                socks = dict(poller.poll(timeout=100))

                # 处理日志
                if self.log_receiver in socks:
                    self._handle_log()

                # 处理健康检查
                if self.health_responder in socks:
                    self._handle_health_check()

                # 更新心跳
                self.last_heartbeat = time.time()

            except Exception as e:
                self.logger.error(f"主循环异常: {e}", exc_info=True)
                time.sleep(1)  # 避免快速失败循环

    def _handle_log(self):
        """处理日志条目."""
        try:
            # 接收日志
            log_data = self.log_receiver.recv()
            log_entry = msgpack.unpackb(log_data, raw=False)

            self.log_count += 1

            # 存储日志
            if self.logging_system:
                self.logging_system.save_log(log_entry)

            # 检查告警规则
            if self.alert_engine:
                alerts = self.alert_engine.check_log(log_entry)

                # 推送告警
                for alert in alerts:
                    self._publish_alert(alert)

        except Exception as e:
            self.logger.error(f"处理日志失败: {e}", exc_info=True)

    def _publish_alert(self, alert: Dict[str, Any]):
        """推送告警到主进程."""
        try:
            # 发布告警事件
            topic = b'alert'  # 主题
            alert_data = msgpack.packb(alert)
            self.alert_publisher.send_multipart([topic, alert_data])

            self.alert_count += 1
            self.logger.debug(f"推送告警: {alert.get('title', 'Unknown')}")

        except Exception as e:
            self.logger.error(f"推送告警失败: {e}", exc_info=True)

    def _handle_health_check(self):
        """响应健康检查."""
        try:
            # 接收请求
            request = self.health_responder.recv_string()

            # 构建响应
            if request == "ping":
                response = {
                    'status': 'ok',
                    'pid': os.getpid(),
                    'uptime': time.time() - self.last_heartbeat,
                    'log_count': self.log_count,
                    'alert_count': self.alert_count
                }
            else:
                response = {'status': 'unknown_request'}

            # 发送响应
            self.health_responder.send_json(response)

        except Exception as e:
            self.logger.error(f"健康检查失败: {e}", exc_info=True)

    def _cleanup(self):
        """清理资源."""
        self.logger.info("清理资源...")

        if self.log_receiver:
            self.log_receiver.close()
        if self.alert_publisher:
            self.alert_publisher.close()
        if self.health_responder:
            self.health_responder.close()
        if self.context:
            self.context.term()

        self.logger.info("日志/告警进程已退出")


def start_log_alert_process():
    """进程入口函数（供multiprocessing调用）."""
    process = LogAlertProcess()
    process.run()


if __name__ == "__main__":
    # 直接运行测试
    start_log_alert_process()
```

### 6.2 分布式日志处理器实现

#### 文件：backend/core/ipc/__init__.py

```python
"""IPC通信模块."""

from .distributed_log_handler import DistributedLogHandler
from .alert_subscriber import AlertSubscriber
from .ipc_config import IPCConfig

__all__ = [
    'DistributedLogHandler',
    'AlertSubscriber',
    'IPCConfig',
]
```

#### 文件：backend/core/ipc/ipc_config.py

```python
# -*- coding: utf-8 -*-
"""IPC配置管理."""

class IPCConfig:
    """IPC配置常量."""

    # ZeroMQ端口
    LOG_PORT = 5557  # 日志接收端口
    ALERT_PORT = 5558  # 告警推送端口
    HEALTH_PORT = 5559  # 健康检查端口

    # 连接地址
    LOG_ENDPOINT = f"tcp://127.0.0.1:{LOG_PORT}"
    ALERT_ENDPOINT = f"tcp://127.0.0.1:{ALERT_PORT}"
    HEALTH_ENDPOINT = f"tcp://127.0.0.1:{HEALTH_PORT}"

    # 重试配置
    MAX_RETRIES = 3
    RETRY_DELAY = 1  # 秒

    # 超时配置
    SEND_TIMEOUT = 1000  # 毫秒
    RECV_TIMEOUT = 5000  # 毫秒
```

#### 文件：backend/core/ipc/distributed_log_handler.py

```python
# -*- coding: utf-8 -*-
"""分布式日志处理器."""

import os
import time
import logging
import threading
from typing import Optional

import zmq
import msgpack

from .ipc_config import IPCConfig


class DistributedLogHandler(logging.Handler):
    """分布式日志处理器.

    将日志条目通过ZeroMQ发送到日志/告警进程。
    如果发送失败，降级到本地文件。
    """

    def __init__(self):
        """初始化处理器."""
        super().__init__()

        self.context = zmq.Context()
        self.socket: Optional[zmq.Socket] = None
        self.lock = threading.Lock()

        # 降级日志文件
        self.fallback_file = "logs/fallback.log"

        # 连接到日志进程
        self._connect()

    def _connect(self):
        """连接到日志进程."""
        try:
            self.socket = self.context.socket(zmq.PUSH)
            self.socket.setsockopt(zmq.SNDTIMEO, IPCConfig.SEND_TIMEOUT)
            self.socket.connect(IPCConfig.LOG_ENDPOINT)
        except Exception as e:
            print(f"日志处理器连接失败: {e}")
            self.socket = None

    def emit(self, record: logging.LogRecord):
        """发送日志记录."""
        try:
            # 格式化日志条目
            log_entry = {
                'timestamp': record.created,
                'level': record.levelname,
                'logger': record.name,
                'message': self.format(record),
                'pathname': record.pathname,
                'lineno': record.lineno,
                'funcName': record.funcName,
                'process': os.getpid(),
                'thread': record.thread,
                'threadName': record.threadName,
            }

            # 添加异常信息
            if record.exc_info:
                log_entry['exc_info'] = self.formatException(record.exc_info)

            # 发送
            self._send_log(log_entry)

        except Exception:
            # 处理器本身不应抛出异常
            self.handleError(record)

    def _send_log(self, log_entry: dict):
        """发送日志条目（带重试）."""
        retry_count = 0

        while retry_count < IPCConfig.MAX_RETRIES:
            try:
                with self.lock:
                    if self.socket is None:
                        self._connect()

                    if self.socket:
                        self.socket.send(msgpack.packb(log_entry))
                        return  # 成功

            except zmq.ZMQError:
                retry_count += 1
                if retry_count < IPCConfig.MAX_RETRIES:
                    time.sleep(IPCConfig.RETRY_DELAY)
                    self._reconnect()

        # 所有重试失败，降级到文件
        self._fallback_to_file(log_entry)

    def _reconnect(self):
        """重新连接."""
        with self.lock:
            if self.socket:
                self.socket.close()
                self.socket = None
            self._connect()

    def _fallback_to_file(self, log_entry: dict):
        """降级到本地文件."""
        try:
            import json
            with open(self.fallback_file, 'a', encoding='utf-8') as f:
                f.write(json.dumps(log_entry, ensure_ascii=False) + '\n')
        except Exception:
            pass  # 静默失败

    def close(self):
        """关闭处理器."""
        with self.lock:
            if self.socket:
                self.socket.close()
                self.socket = None
        super().close()
```

#### 文件：backend/core/ipc/alert_subscriber.py

```python
# -*- coding: utf-8 -*-
"""告警订阅客户端."""

import threading
import logging
from typing import Callable, Optional

import zmq
import msgpack

from PySide6.QtCore import QObject, Signal

from .ipc_config import IPCConfig


class AlertSubscriber(QObject):
    """告警订阅客户端（在主进程中运行）.

    职责：
    1. 连接到日志/告警进程
    2. 订阅告警事件
    3. 通过Qt信号安全地更新UI
    """

    # Qt信号：告警到达（在主线程中触发）
    alert_received = Signal(dict)

    def __init__(self):
        """初始化订阅客户端."""
        super().__init__()

        self.context = zmq.Context()
        self.socket: Optional[zmq.Socket] = None
        self.receiver_thread: Optional[threading.Thread] = None
        self.running = False

        self.logger = logging.getLogger(self.__class__.__name__)

    def start(self):
        """启动订阅."""
        if self.running:
            self.logger.warning("订阅客户端已在运行")
            return

        try:
            # 创建SUB socket
            self.socket = self.context.socket(zmq.SUB)
            self.socket.connect(IPCConfig.ALERT_ENDPOINT)

            # 订阅所有告警（topic=alert）
            self.socket.subscribe(b'alert')

            self.logger.info(f"✅ 连接到告警推送服务: {IPCConfig.ALERT_ENDPOINT}")

            # 启动接收线程
            self.running = True
            self.receiver_thread = threading.Thread(
                target=self._receive_loop,
                name='AlertReceiverThread',
                daemon=True
            )
            self.receiver_thread.start()

            self.logger.info("✅ 告警订阅客户端已启动")

        except Exception as e:
            self.logger.error(f"启动订阅客户端失败: {e}", exc_info=True)

    def stop(self):
        """停止订阅."""
        self.running = False

        if self.receiver_thread:
            self.receiver_thread.join(timeout=2)

        if self.socket:
            self.socket.close()
            self.socket = None

        self.logger.info("告警订阅客户端已停止")

    def _receive_loop(self):
        """接收循环（在后台线程中运行）."""
        self.logger.info("进入告警接收循环...")

        while self.running:
            try:
                # 设置超时，允许检查running标志
                if self.socket.poll(timeout=1000):  # 1秒超时
                    # 接收消息
                    topic, alert_data = self.socket.recv_multipart()
                    alert = msgpack.unpackb(alert_data, raw=False)

                    # 通过Qt信号发送到主线程（线程安全）
                    self.alert_received.emit(alert)

                    self.logger.debug(f"收到告警: {alert.get('title', 'Unknown')}")

            except zmq.ZMQError as e:
                if self.running:  # 只在运行时报错
                    self.logger.error(f"接收告警失败: {e}")
                    time.sleep(1)  # 避免快速失败循环
            except Exception as e:
                self.logger.error(f"处理告警异常: {e}", exc_info=True)

        self.logger.info("告警接收循环已退出")
```

### 6.3 进程管理器实现

#### 文件：backend/core/process_manager.py

```python
# -*- coding: utf-8 -*-
"""进程管理器."""

import os
import time
import logging
import multiprocessing
from typing import Dict, Optional, Callable
from enum import Enum

import zmq


class ProcessStatus(Enum):
    """进程状态枚举."""
    STOPPED = "stopped"
    STARTING = "starting"
    RUNNING = "running"
    STOPPING = "stopping"
    ERROR = "error"


class ManagedProcess:
    """受管理的进程."""

    def __init__(
        self,
        name: str,
        target: Callable,
        health_endpoint: Optional[str] = None
    ):
        """初始化受管理进程.

        Args:
            name: 进程名称
            target: 进程目标函数
            health_endpoint: 健康检查端点
        """
        self.name = name
        self.target = target
        self.health_endpoint = health_endpoint

        self.process: Optional[multiprocessing.Process] = None
        self.status = ProcessStatus.STOPPED
        self.start_time: Optional[float] = None
        self.restart_count = 0

        self.logger = logging.getLogger(f"ProcessManager.{name}")

    def start(self) -> bool:
        """启动进程."""
        try:
            if self.process and self.process.is_alive():
                self.logger.warning("进程已在运行")
                return False

            self.status = ProcessStatus.STARTING

            self.process = multiprocessing.Process(
                target=self.target,
                name=self.name,
                daemon=False
            )
            self.process.start()

            self.start_time = time.time()
            self.status = ProcessStatus.RUNNING

            self.logger.info(f"✅ 进程已启动 PID={self.process.pid}")
            return True

        except Exception as e:
            self.status = ProcessStatus.ERROR
            self.logger.error(f"启动进程失败: {e}", exc_info=True)
            return False

    def stop(self, timeout: int = 5) -> bool:
        """停止进程.

        Args:
            timeout: 超时时间（秒）
        """
        try:
            if not self.process or not self.process.is_alive():
                self.logger.info("进程未运行")
                return True

            self.status = ProcessStatus.STOPPING

            # 优雅终止
            self.process.terminate()
            self.process.join(timeout=timeout)

            # 如果还活着，强制杀死
            if self.process.is_alive():
                self.logger.warning("进程未响应terminate，使用kill")
                self.process.kill()
                self.process.join(timeout=2)

            self.status = ProcessStatus.STOPPED
            self.logger.info("✅ 进程已停止")
            return True

        except Exception as e:
            self.status = ProcessStatus.ERROR
            self.logger.error(f"停止进程失败: {e}", exc_info=True)
            return False

    def restart(self) -> bool:
        """重启进程."""
        self.logger.info("重启进程...")
        self.stop()
        time.sleep(1)  # 等待资源释放
        success = self.start()
        if success:
            self.restart_count += 1
        return success

    def is_alive(self) -> bool:
        """检查进程是否存活."""
        return self.process is not None and self.process.is_alive()

    def check_health(self) -> Dict:
        """检查进程健康状态."""
        if not self.is_alive():
            return {'status': 'dead', 'uptime': 0}

        if not self.health_endpoint:
            # 没有健康检查端点，只检查进程存活
            return {
                'status': 'alive',
                'uptime': time.time() - self.start_time if self.start_time else 0,
                'pid': self.process.pid
            }

        # 通过ZeroMQ查询健康状态
        try:
            context = zmq.Context()
            socket = context.socket(zmq.REQ)
            socket.setsockopt(zmq.RCVTIMEO, 2000)  # 2秒超时
            socket.connect(self.health_endpoint)

            socket.send_string("ping")
            response = socket.recv_json()

            socket.close()
            context.term()

            return response

        except zmq.ZMQError:
            return {'status': 'timeout', 'message': '健康检查超时'}
        except Exception as e:
            return {'status': 'error', 'message': str(e)}


class ProcessManager:
    """进程管理器.

    职责：
    1. 管理所有子进程的生命周期
    2. 监控子进程健康状态
    3. 自动重启崩溃的进程
    4. 提供统一的进程操作接口
    """

    def __init__(self, auto_restart: bool = True):
        """初始化进程管理器.

        Args:
            auto_restart: 是否自动重启崩溃的进程
        """
        self.processes: Dict[str, ManagedProcess] = {}
        self.auto_restart = auto_restart
        self.monitor_thread: Optional[threading.Thread] = None
        self.monitoring = False

        self.logger = logging.getLogger(self.__class__.__name__)

    def register_process(
        self,
        name: str,
        target: Callable,
        health_endpoint: Optional[str] = None
    ) -> ManagedProcess:
        """注册进程.

        Args:
            name: 进程名称
            target: 进程目标函数
            health_endpoint: 健康检查端点
        """
        managed = ManagedProcess(name, target, health_endpoint)
        self.processes[name] = managed
        self.logger.info(f"注册进程: {name}")
        return managed

    def start_all(self) -> bool:
        """启动所有进程."""
        all_success = True

        for name, proc in self.processes.items():
            if not proc.start():
                self.logger.error(f"启动进程 {name} 失败")
                all_success = False

        # 启动监控线程
        if self.auto_restart:
            self.start_monitoring()

        return all_success

    def stop_all(self) -> bool:
        """停止所有进程."""
        # 停止监控
        self.stop_monitoring()

        all_success = True

        for name, proc in self.processes.items():
            if not proc.stop():
                self.logger.error(f"停止进程 {name} 失败")
                all_success = False

        return all_success

    def start_monitoring(self):
        """启动进程监控."""
        if self.monitoring:
            return

        self.monitoring = True
        self.monitor_thread = threading.Thread(
            target=self._monitor_loop,
            name='ProcessMonitorThread',
            daemon=True
        )
        self.monitor_thread.start()

        self.logger.info("✅ 进程监控已启动")

    def stop_monitoring(self):
        """停止进程监控."""
        self.monitoring = False
        if self.monitor_thread:
            self.monitor_thread.join(timeout=2)
        self.logger.info("进程监控已停止")

    def _monitor_loop(self):
        """监控循环."""
        self.logger.info("进入监控循环...")

        while self.monitoring:
            try:
                for name, proc in self.processes.items():
                    # 检查进程是否存活
                    if not proc.is_alive() and proc.status == ProcessStatus.RUNNING:
                        # 进程意外退出
                        exit_code = proc.process.exitcode if proc.process else None
                        self.logger.error(
                            f"进程 {name} 意外退出，退出码: {exit_code}"
                        )

                        # 自动重启
                        if self.auto_restart:
                            self.logger.info(f"自动重启进程 {name}...")
                            proc.restart()

                    # 健康检查
                    elif proc.is_alive():
                        health = proc.check_health()
                        if health.get('status') not in ['ok', 'alive']:
                            self.logger.warning(
                                f"进程 {name} 健康检查失败: {health}"
                            )
                            # 可选：重启不健康的进程

                # 每5秒检查一次
                time.sleep(5)

            except Exception as e:
                self.logger.error(f"监控循环异常: {e}", exc_info=True)

    def get_status(self) -> Dict:
        """获取所有进程状态."""
        status = {}
        for name, proc in self.processes.items():
            status[name] = {
                'status': proc.status.value,
                'alive': proc.is_alive(),
                'pid': proc.process.pid if proc.process else None,
                'uptime': time.time() - proc.start_time if proc.start_time else 0,
                'restart_count': proc.restart_count
            }
        return status
```

### 6.4 修改SystemManagerService

#### 文件：backend/services/system_manager_service.py

需要修改的关键部分：

```python
# -*- coding: utf-8 -*-
"""系统管理服务（多进程改造版）."""

from backend.services.base_and_utils import BaseService
from backend.core.ipc import AlertSubscriber


class SystemManagerService(BaseService):
    """系统管理服务."""

    def __init__(self):
        """初始化系统管理服务."""
        super().__init__()

        # ⚠️ 不再直接初始化LoggingSystem和AlertEngine
        # 改为通过IPC连接到独立进程

        # 告警订阅客户端
        self.alert_subscriber: Optional[AlertSubscriber] = None

        # 告警缓存（用于UI显示）
        self.alert_cache: List[Dict] = []

        # 其他组件保持不变...
        self.system_monitor = SystemMonitor()
        self.network_tester = NetworkTester()
        # ...

    def _do_initialize(self) -> bool:
        """初始化系统管理服务."""
        try:
            self.logger.info("初始化系统管理服务...")

            # ⚠️ 关键变更：不再初始化日志/告警系统
            # 改为连接到独立进程
            self._connect_to_log_alert_process()

            return True

        except Exception as e:
            self.logger.error(f"初始化失败: {e}", exc_info=True)
            return False

    def _connect_to_log_alert_process(self):
        """连接到日志/告警进程."""
        try:
            self.logger.info("连接到日志/告警进程...")

            # 创建告警订阅客户端
            self.alert_subscriber = AlertSubscriber()

            # 连接信号到处理函数
            self.alert_subscriber.alert_received.connect(self._on_alert_received)

            # 启动订阅
            self.alert_subscriber.start()

            self.logger.info("✅ 已连接到日志/告警进程")

        except Exception as e:
            self.logger.error(f"连接日志/告警进程失败: {e}", exc_info=True)
            # 不抛出异常，允许服务继续运行（降级模式）

    def _on_alert_received(self, alert: Dict):
        """处理接收到的告警（在主线程中调用）.

        Args:
            alert: 告警数据字典
        """
        try:
            # 添加到缓存
            self.alert_cache.append(alert)

            # 限制缓存大小
            if len(self.alert_cache) > 1000:
                self.alert_cache = self.alert_cache[-1000:]

            # 发送信号给UI（如果有的话）
            # 这里可以定义一个信号，让UI组件订阅
            self.logger.info(f"收到告警: {alert.get('title', 'Unknown')}")

        except Exception as e:
            self.logger.error(f"处理告警失败: {e}", exc_info=True)

    def get_recent_alerts(self, limit: int = 100) -> List[Dict]:
        """获取最近的告警列表.

        Args:
            limit: 返回数量限制
        """
        return self.alert_cache[-limit:]

    def _do_shutdown(self) -> bool:
        """关闭系统管理服务."""
        try:
            # 停止告警订阅
            if self.alert_subscriber:
                self.alert_subscriber.stop()

            return True

        except Exception as e:
            self.logger.error(f"关闭失败: {e}", exc_info=True)
            return False
```

---

## 7. 启动流程集成

### 7.1 修改start_async_fixed.py

在主启动脚本中启动日志/告警进程：

```python
# start_async_fixed.py
import multiprocessing

def main():
    startup_start = time.time()

    print("=" * 70)
    print("🚀 星辰金融终端 - 启动中（多进程模式）")
    print("=" * 70)

    # ==================== 新增：启动日志/告警进程 ====================
    print("\n[LOG-ALERT-PROC] 启动日志/告警独立进程...")

    from backend.processes.log_alert_process import start_log_alert_process

    log_alert_proc = multiprocessing.Process(
        target=start_log_alert_process,
        name='LogAlertProcess',
        daemon=False  # 不是守护进程，确保正常退出
    )
    log_alert_proc.start()

    # 等待进程启动（简单延迟，实际应该等待端口就绪）
    time.sleep(0.5)

    print(f"[LOG-ALERT-PROC] ✅ 日志/告警进程已启动 PID={log_alert_proc.pid}")

    # ==================== 配置主进程使用分布式日志 ====================
    print("\n[LOG-SETUP] 配置主进程日志系统...")

    from backend.core.ipc import DistributedLogHandler

    # 获取根logger
    root_logger = logging.getLogger()

    # 添加分布式日志处理器
    distributed_handler = DistributedLogHandler()
    distributed_handler.setLevel(logging.INFO)
    root_logger.addHandler(distributed_handler)

    print("[LOG-SETUP] ✅ 主进程日志已配置为分布式模式")

    # ==================== 原有启动流程（保持不变）====================
    # ... 环境准备、Qt初始化、UI启动等 ...

    # ==================== 退出时清理子进程 ====================
    def cleanup_on_exit():
        print("\n[CLEANUP] 清理子进程...")
        if log_alert_proc.is_alive():
            log_alert_proc.terminate()
            log_alert_proc.join(timeout=5)
            if log_alert_proc.is_alive():
                log_alert_proc.kill()
        print("[CLEANUP] ✅ 子进程已清理")

    import atexit
    atexit.register(cleanup_on_exit)

    # 运行应用...
    return app.exec()
```

### 7.2 完整启动流程示例

以下是完整的启动流程修改示例：

```python
# start_async_fixed.py - 完整的多进程启动版本

import os
import sys
import time
import logging
import multiprocessing
from pathlib import Path


def setup_environment():
    """环境准备."""
    project_root = Path(__file__).parent
    if str(project_root) not in sys.path:
        sys.path.insert(0, str(project_root))

    # 设置环境变量
    os.environ["PYTHONEXECUTABLE"] = sys.executable
    os.environ["QT_WEBENGINE_PYTHON_EXECUTABLE"] = sys.executable
    os.environ["QT_AUTO_SCREEN_SCALE_FACTOR"] = "1"
    os.environ["CONFIG_FILE"] = str(project_root / "config" / "terminal_config.json")


def check_port_available(port: int) -> bool:
    """检查端口是否可用."""
    import socket
    try:
        with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
            s.bind(('127.0.0.1', port))
        return True
    except OSError:
        return False


def main():
    """多进程启动主函数."""
    startup_start = time.time()

    print("=" * 70)
    print("🚀 星辰金融终端 - 启动中（多进程模式）")
    print("=" * 70)

    # ========== 阶段0：环境准备 ==========
    print("\n[STAGE-0] 环境准备...")
    setup_environment()
    print("[STAGE-0] ✅ 环境准备完成")

    # ========== 检查是否使用单进程模式 ==========
    single_process_mode = os.getenv('SINGLE_PROCESS_MODE') == '1'

    log_alert_proc = None

    if not single_process_mode:
        # ========== 新增：启动日志/告警进程 ==========
        print("\n[LOG-ALERT] 启动日志/告警独立进程...")

        # 检查端口
        if not check_port_available(5557):
            print("⚠️ 警告：端口5557已被占用，将使用备用端口5567")
            # 这里可以实现端口协商逻辑

        from backend.processes.log_alert_process import start_log_alert_process

        log_alert_proc = multiprocessing.Process(
            target=start_log_alert_process,
            name='LogAlertProcess',
            daemon=False
        )
        log_alert_proc.start()

        # 等待进程启动
        time.sleep(0.5)

        print(f"[LOG-ALERT] ✅ 日志/告警进程已启动 PID={log_alert_proc.pid}")

        # ========== 配置主进程使用分布式日志 ==========
        print("\n[LOG-SETUP] 配置主进程日志系统...")

        from backend.core.utils import setup_logging
        from backend.core.ipc import DistributedLogHandler

        # 先设置基础日志
        logger = setup_logging(
            name="StartupOptimized",
            level="INFO",
            log_file="logs/startup_optimized.log"
        )

        # 添加分布式日志处理器
        distributed_handler = DistributedLogHandler()
        distributed_handler.setLevel(logging.INFO)
        logging.getLogger().addHandler(distributed_handler)

        print("[LOG-SETUP] ✅ 主进程日志已配置为分布式模式")

    else:
        print("\n⚠️ 单进程模式（调试）- 日志/告警在主进程中运行")
        from backend.core.utils import setup_logging
        logger = setup_logging(
            name="StartupOptimized",
            level="INFO",
            log_file="logs/startup_optimized.log"
        )

    # ========== 阶段1：Qt框架初始化 ==========
    print("\n[STAGE-1] Qt框架初始化...")
    logger.info("[STAGE-1] 开始Qt框架初始化")

    from PySide6.QtWidgets import QApplication
    from backend.config import init_settings

    app = QApplication(sys.argv)
    app.setApplicationName("星辰金融终端")
    app.setApplicationVersion("5.0.0")

    # 加载配置
    config_file = os.getenv("CONFIG_FILE")
    init_settings(config_file if config_file else None)

    print("[STAGE-1] ✅ Qt框架初始化完成")

    # ========== 阶段2：创建启动协调器 ==========
    from ui.startup_coordinator import StartupCoordinator
    coordinator = StartupCoordinator(app, config_already_initialized=True)

    # ========== 阶段3：创建主窗口 ==========
    print("\n[STAGE-2] 创建主窗口...")
    from ui.main_window import MainWindow

    main_window = MainWindow(backend_ready=False)
    main_window.show()

    ui_visible_time = (time.time() - startup_start) * 1000
    print(f"[STAGE-2] ✅ 主窗口已显示 ({ui_visible_time:.0f}ms)")

    # ========== 连接后端初始化回调 ==========
    def on_startup_completed():
        """后端初始化完成回调."""
        try:
            logger.info("[STAGE-4] 后端初始化完成，激活UI功能")

            main_window.initialize_function_interfaces_after_backend()
            coordinator.hide_splash(main_window)

            total_time = (time.time() - startup_start) * 1000
            print(f"\n✅ 系统启动完成！总耗时: {total_time:.0f}ms")
            logger.info(f"✅ 系统启动完成，总耗时: {total_time:.0f}ms")

        except Exception as e:
            logger.error(f"UI激活失败: {e}", exc_info=True)

    def on_startup_failed(error: str):
        """后端初始化失败回调."""
        logger.error(f"后端初始化失败: {error}")
        coordinator.hide_splash()
        print(f"\n⚠️ 后端初始化失败: {error}")

    coordinator.startup_completed.connect(on_startup_completed)
    coordinator.startup_failed.connect(on_startup_failed)

    # ========== 阶段4：启动后端初始化 ==========
    print("\n[STAGE-3] 启动后端服务初始化（异步）...")
    coordinator.start()

    # ========== 退出时清理子进程 ==========
    def cleanup_on_exit():
        """清理子进程."""
        if log_alert_proc and log_alert_proc.is_alive():
            print("\n[CLEANUP] 停止日志/告警进程...")
            log_alert_proc.terminate()
            log_alert_proc.join(timeout=5)
            if log_alert_proc.is_alive():
                log_alert_proc.kill()
            print("[CLEANUP] ✅ 日志/告警进程已停止")

    import atexit
    atexit.register(cleanup_on_exit)

    # ========== 进入Qt事件循环 ==========
    print("\n[EVENT-LOOP] 进入Qt事件循环...")
    return app.exec()


if __name__ == "__main__":
    # Windows兼容性：设置启动方法
    if sys.platform == 'win32':
        multiprocessing.freeze_support()

    sys.exit(main())
```

### 7.3 关键修改点汇总

#### 修改点1：移除SystemManagerService中的日志/告警初始化

```python
# backend/services/system_manager_service.py

# ❌ 删除这些代码（原有的直接初始化）
# def _init_logging_and_alert_system(self):
#     self.logging_system = LoggingSystem()
#     self.alert_engine = AlertEngine()
#     create_default_log_alert_rules()

# ✅ 替换为IPC连接
def _connect_to_log_alert_process(self):
    self.alert_subscriber = AlertSubscriber()
    self.alert_subscriber.alert_received.connect(self._on_alert_received)
    self.alert_subscriber.start()
```

#### 修改点2：配置全局日志使用分布式处理器

```python
# 在启动流程早期（创建QApplication之后）
from backend.core.ipc import DistributedLogHandler

distributed_handler = DistributedLogHandler()
distributed_handler.setLevel(logging.INFO)
logging.getLogger().addHandler(distributed_handler)
```

#### 修改点3：UI组件订阅告警信号

```python
# ui/components/system_manager/alert_manager_widget.py

class AlertManagerWidget(QWidget):
    def __init__(self, parent=None):
        super().__init__(parent)

        # 获取SystemManagerService的告警订阅器
        from backend.core.base import get_service
        system_service = get_service('SystemManagerService')

        if system_service and system_service.alert_subscriber:
            # 连接告警信号
            system_service.alert_subscriber.alert_received.connect(
                self._on_new_alert
            )

    def _on_new_alert(self, alert: dict):
        """处理新告警（在主线程中调用）."""
        # 更新UI
        self.add_alert_to_table(alert)

        # 如果是严重告警，弹窗提示
        if alert.get('severity') == 'CRITICAL':
            QMessageBox.warning(self, "严重告警", alert.get('message'))
```

---

## 8. 风险与注意事项

### 8.1 技术风险

#### 风险1：ZeroMQ端口冲突

**描述**：如果端口5557-5559已被占用，进程启动失败。

**缓解措施**：
```python
# 启动时检查端口可用性
import socket

def check_port_available(port):
    """检查端口是否可用."""
    try:
        with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
            s.bind(('127.0.0.1', port))
        return True
    except OSError:
        return False

# 启动前检查
if not check_port_available(5557):
    logger.error("端口5557已被占用，无法启动日志进程")
    sys.exit(1)
```

#### 风险2：进程间通信延迟

**描述**：IPC增加延迟，可能影响日志实时性。

**实际影响**：
- 日志延迟：5-10ms（可接受）
- 告警延迟：10-50ms（可接受）
- 不影响业务逻辑性能

**验证方法**：
```python
# 性能测试
import time

start = time.time()
logger.info("Test log")
end = time.time()
print(f"日志延迟: {(end-start)*1000:.2f}ms")
```

#### 风险3：子进程崩溃

**描述**：日志/告警进程崩溃导致日志丢失。

**缓解措施**：
1. **降级方案**：自动写入本地文件
2. **自动重启**：进程管理器监控并重启
3. **健康检查**：定期检查子进程状态

#### 风险4：序列化开销

**描述**：msgpack序列化增加CPU开销。

**实际测试**：
```python
# 序列化性能测试
log_entry = {
    'timestamp': time.time(),
    'level': 'INFO',
    'message': 'Test message' * 10,
    'process': os.getpid()
}

import msgpack
start = time.time()
for _ in range(10000):
    data = msgpack.packb(log_entry)
    msgpack.unpackb(data)
end = time.time()
print(f"10000次序列化+反序列化耗时: {(end-start)*1000:.2f}ms")
# 预期：< 100ms（平均每次 < 0.01ms）
```

### 8.2 运维风险

#### 风险1：调试困难度增加

**问题**：多进程调试比单进程困难。

**解决方案**：
1. **日志完善**：每个进程独立日志文件
2. **进程标识**：日志中包含进程ID
3. **单进程模式**：环境变量控制

```python
# 支持单进程调试模式
if os.getenv('SINGLE_PROCESS_MODE') == '1':
    print("⚠️ 单进程模式（调试）")
    # 不启动子进程，日志/告警在主进程
else:
    print("✅ 多进程模式（生产）")
    # 启动子进程
```

#### 风险2：资源占用增加

**问题**：多进程占用更多内存。

**实际影响**：
- 主进程：~200MB
- 日志/告警进程：~50MB
- 总增加：+50MB（可接受）

#### 风险3：启动时间增加

**问题**：启动子进程增加启动时间。

**实际影响**：
- 子进程启动：~500ms
- 对总启动时间影响：< 10%（从2秒变为2.5秒，仍达标）

### 8.3 数据风险

#### 风险1：日志丢失

**场景**：
1. 子进程未启动时发送日志
2. 子进程崩溃期间的日志
3. IPC队列满

**缓解措施**：
1. **启动顺序**：先启动子进程，等待就绪
2. **降级文件**：自动写入fallback.log
3. **恢复同步**：子进程恢复后同步降级日志

#### 风险2：告警重复

**场景**：子进程重启后重新加载规则，可能重复触发。

**缓解措施**：
1. **告警去重**：基于时间窗口和内容去重
2. **状态持久化**：告警状态保存到数据库

### 8.4 兼容性风险

#### 风险1：Windows vs Linux

**问题**：multiprocessing在Windows和Linux行为不同。

**Windows特殊性**：
- 使用spawn而非fork
- 子进程需要显式导入模块
- 需要`if __name__ == "__main__"`保护

**解决方案**：
```python
# 兼容Windows的写法
if __name__ == "__main__":
    # 设置启动方法
    multiprocessing.set_start_method('spawn', force=True)
    main()
```

#### 风险2：Python版本

**要求**：Python 3.7+（支持multiprocessing改进）

**验证**：
```python
import sys
assert sys.version_info >= (3, 7), "需要Python 3.7+"
```

---

## 9. 测试验收标准

### 9.1 功能验收

| 测试项 | 验收标准 | 测试方法 |
|-------|---------|---------|
| **启动成功率** | ≥ 99% | 连续启动100次，崩溃次数 ≤ 1 |
| **日志功能** | 所有级别日志正常 | 发送INFO/WARNING/ERROR日志，验证接收 |
| **告警功能** | 告警触发和推送正常 | 触发ERROR日志，验证告警UI更新 |
| **进程监控** | 子进程状态可查询 | 查询健康状态，验证响应 |
| **自动重启** | 子进程崩溃后5秒内重启 | 手动杀死子进程，验证重启 |

### 9.2 性能验收

| 指标 | 目标值 | 测试方法 |
|-----|-------|---------|
| **日志发送延迟** | < 10ms | 发送1000条日志，统计平均延迟 |
| **告警推送延迟** | < 50ms | 触发告警，测量UI更新时间 |
| **内存占用** | 总增加 < 100MB | 运行1小时，监控内存 |
| **CPU占用** | IPC开销 < 5% | 高频日志场景，监控CPU |

### 9.3 稳定性验收

| 测试项 | 目标 | 测试方法 |
|-------|------|---------|
| **长时间运行** | 24小时无崩溃 | 压力测试，模拟正常使用 |
| **网络恢复** | 断开后自动重连 | 模拟网络断开，验证重连 |
| **降级恢复** | 降级日志完整同步 | 停止子进程，验证降级和恢复 |

---

## 10. 总结与展望

### 10.1 改造总结

本次多进程架构改造遵循"最小改动，精准打击"原则：

1. **只隔离必要模块**：仅将日志/告警系统独立进程，其他模块保持主进程
2. **性能优先**：保持数据密集型模块（数据服务、策略回测、交易网关）在主进程，避免IPC开销
3. **渐进式实施**：分阶段改造，每个阶段可独立验证
4. **完善容错机制**：降级方案、自动重启、健康监控

**预期效果**：
- ✅ **解决启动崩溃**：崩溃率从30% → <1%
- ✅ **提升稳定性**：日志/告警进程隔离，故障不传播
- ✅ **保持高性能**：核心数据访问无IPC开销
- ✅ **改造成本低**：2-3天完成核心改造

### 10.2 未来扩展方向

#### 短期（1-3个月）

如果运行过程中发现其他稳定性问题，可按需隔离：

1. **AI助手进程**（如果网络请求阻塞严重）
   - 估算工期：2-3天
   - IPC开销：可接受（低频调用）

2. **组合投资进程**（如果监控逻辑复杂）
   - 估算工期：2-3天
   - 适用场景：多策略并行时

#### 中期（3-6个月）

架构优化方向：

1. **进程池管理**
   - 统一的进程生命周期管理
   - 进程资源限制和监控
   - 自动扩缩容

2. **配置热重载**
   - 无需重启即可更新配置
   - 动态调整进程参数

#### 长期（6个月+）

如果系统规模扩大，考虑：

1. **微服务架构**
   - 后端服务完全独立进程
   - 通过gRPC通信
   - 支持分布式部署

2. **云原生部署**
   - 容器化（Docker）
   - Kubernetes编排
   - 服务网格

**但当前阶段，不建议过度设计。先完成核心的日志/告警进程隔离即可。**

### 10.3 关键决策回顾

| 决策 | 理由 | 结果 |
|-----|------|------|
| **交易网关保持主进程** | 数据访问频繁，IPC开销太大 | 性能优先 ✅ |
| **只隔离日志/告警** | 已知崩溃源，低频通信 | 精准打击 ✅ |
| **使用ZeroMQ** | 高性能，灵活的通信模式 | 技术适配 ✅ |
| **渐进式改造** | 降低风险，可独立验证 | 工程实践 ✅ |

---

## 附录

### 附录A：依赖安装

```bash
# requirements.txt 中添加
pyzmq>=24.0.0
msgpack-python>=1.0.0
```

### 附录B：端口分配表

| 端口 | 用途 | 协议 | socket类型 |
|-----|------|------|-----------|
| 5557 | 日志接收 | TCP | PULL |
| 5558 | 告警推送 | TCP | PUB |
| 5559 | 健康检查 | TCP | REP |

### 附录C：日志级别与告警对应关系

| 日志级别 | 是否触发告警 | 告警严重级别 | 通知方式 |
|---------|------------|------------|---------|
| DEBUG | ❌ | - | - |
| INFO | ❌ | - | - |
| WARNING | 🔶 可选 | LOW | UI通知 |
| ERROR | ✅ | HIGH | UI通知 + 邮件 |
| CRITICAL | ✅ | CRITICAL | UI通知 + 邮件 + 短信 |

### 附录D：故障排查清单

| 问题 | 可能原因 | 排查方法 | 解决方案 |
|-----|---------|---------|---------|
| 子进程启动失败 | 端口被占用 | 检查端口 | 更换端口或杀死占用进程 |
| 日志未接收 | IPC连接失败 | 查看fallback.log | 检查子进程是否运行 |
| 告警未显示 | 订阅未建立 | 查看主进程日志 | 重启订阅客户端 |
| 内存泄漏 | ZeroMQ未清理 | 监控内存增长 | 检查socket关闭逻辑 |

---

**文档结束**

**版本**: v1.0
**最后更新**: 2025-01-15
**维护者**: 架构团队

---

**重要提示**：本文档是多进程架构改造的完整指南，请严格按照文档进行实施。如有疑问，请联系架构团队。

---

## 补充说明

### S1. 为什么交易网关保持在主进程？

这是一个经过深入分析后的关键决策，需要详细说明。

#### S1.1 数据访问模式分析

**交易网关的数据依赖**：

```python
# 交易网关运行策略时的典型数据访问模式
class MACrossStrategy(CtaTemplate):
    """移动平均线交叉策略（示例）"""

    def on_bar(self, bar: BarData):
        """每个新K线到来时执行"""

        # 策略需要加载历史数据计算指标
        # ⚠️ 这里会访问UnifiedDataManager
        self.load_bar(days=20, interval=Interval.MINUTE)

        # 计算MA指标（需要100根历史bar）
        ma5 = self.am.sma(5)
        ma20 = self.am.sma(20)

        # 交易逻辑
        if ma5 > ma20 and self.pos == 0:
            self.buy(price, volume)
```

**VnPy策略引擎的数据获取流程**：

```python
# vnpy_ctastrategy/engine.py
class CtaEngine:
    def load_bar(self, vt_symbol, days, interval, callback, use_database):
        """策略引擎加载历史数据的三级回退机制"""

        # 级别1: 从Gateway查询历史数据
        if not use_database:
            bars = self.main_engine.query_history(req, gateway_name)

        # 级别2: 从Datafeed查询（RQData、Tushare等）
        if not bars:
            bars = self.query_bar_from_datafeed(...)

        # 级别3: 从VnPy Database查询
        if not bars:
            bars = self.database.load_bar_data(...)

        # ⚠️ 关键：本项目使用统一数据管理器
        # 需要将UnifiedDataManager集成到这个流程中
```

**本项目的数据集成方式**：

```python
# 方案1：通过Datafeed接口桥接（推荐）
# 实现一个CustomDatafeed，内部调用UnifiedDataManager

class UnifiedDatafeed(BaseDatafeed):
    """桥接UnifiedDataManager到VnPy的Datafeed接口"""

    def __init__(self, unified_manager):
        self.unified_manager = unified_manager

    def query_bar_history(self, req: HistoryRequest):
        """VnPy调用此方法获取历史数据"""
        # 调用统一数据管理器
        df = self.unified_manager.get_kline_data(
            symbol=req.symbol,
            interval=req.interval,
            start_date=req.start,
            end_date=req.end
        )
        # 转换为VnPy BarData格式
        return self._convert_to_bars(df)

# 在CtaEngine中注册
cta_engine.datafeed = UnifiedDatafeed(unified_manager)
```

**数据访问频率估算**：

| 策略类型 | 访问频率 | 单次数据量 | 每天总请求 |
|---------|---------|----------|----------|
| **CTA（1分钟bar）** | 每分钟1次 × 初始化 | 100根bar<br>~5KB | 240次 |
| **CTA（多策略）** | 10个策略 | 5KB×10 | 2,400次 |
| **高频策略（秒级）** | 每秒1次 | 100根bar | 14,400次 |
| **组合策略** | 每分钟 × N品种 | 5KB×N | 240×N次 |

**性能影响对比**：

```
场景：10个CTA策略同时运行

同进程方案：
- 单次查询：0.1ms（内存访问）
- 每天2400次 × 0.1ms = 240ms
- 累积延迟：可忽略 ✅

跨进程方案（如果交易网关独立）：
- 单次查询：5-10ms（IPC + 序列化）
- 每天2400次 × 7.5ms = 18秒
- 累积延迟：可能影响策略时效性 ⚠️

高频策略场景（秒级）：
- 同进程：14,400次 × 0.1ms = 1.44秒 ✅
- 跨进程：14,400次 × 7.5ms = 108秒 ❌ 不可用！
```

**结论**：交易网关必须保持主进程，才能保证策略性能。

#### S1.2 故障隔离的替代方案

虽然交易网关保持主进程，但仍可通过其他方式实现故障隔离：

##### 方案1：策略沙盒执行

```python
class StrategyExecutor:
    """安全的策略执行器（主进程内）"""

    def execute_strategy(self, strategy, bar):
        """执行策略，带超时和异常隔离"""
        try:
            # 设置超时（防止策略死循环）
            with timeout_context(seconds=5):
                strategy.on_bar(bar)

        except TimeoutError:
            self.logger.error(f"策略 {strategy.name} 执行超时（>5秒）")
            self.stop_strategy(strategy)
            self.send_alert("策略执行超时", severity="HIGH")

        except Exception as e:
            self.logger.error(f"策略 {strategy.name} 异常: {e}", exc_info=True)
            self.error_count[strategy.name] += 1

            # 连续错误3次，自动停止策略
            if self.error_count[strategy.name] >= 3:
                self.stop_strategy(strategy)
                self.send_alert(f"策略已停止（连续错误{self.error_count[strategy.name]}次）")
```

##### 方案2：进程级监控和快速重启

```python
# 虽然在同一进程，但可以：
# 1. 严格的异常捕获
# 2. 策略白名单（只允许经过测试的策略）
# 3. 资源限制（CPU、内存）
# 4. 快速重启机制（整个应用重启 < 5秒）

class RobustTradingGateway:
    def __init__(self):
        # 策略白名单
        self.approved_strategies = set()

        # 资源监控
        self.resource_monitor = ResourceMonitor()

    def deploy_strategy(self, strategy_code):
        """部署策略（带安全检查）"""
        # 1. 代码审查
        if not self.code_review(strategy_code):
            raise ValueError("策略代码未通过安全审查")

        # 2. 回测验证
        backtest_result = self.run_backtest(strategy_code)
        if backtest_result['sharpe'] < 0.5:
            raise ValueError("策略回测表现不佳")

        # 3. 沙盒测试
        sandbox_result = self.run_in_sandbox(strategy_code)
        if not sandbox_result['safe']:
            raise ValueError("策略在沙盒中异常")

        # 4. 添加到白名单
        self.approved_strategies.add(strategy_code_hash)
```

##### 方案3：VnPy自带的风险管理

```python
# VnPy的vnpy_riskmanager已提供风控功能
from vnpy_riskmanager import RiskManagerApp

# 在MainEngine中启用风控
risk_manager = main_engine.add_app(RiskManagerApp)

# 设置风控参数
risk_manager.update_setting({
    'order_flow_limit': 50,  # 单位时间委托流量限制
    'order_size_limit': 1000,  # 单笔委托数量限制
    'trade_limit': 100,  # 成交限制
    'active_order_limit': 50,  # 活动委托限制
})

# 风控会自动拦截异常订单，保护系统
```

#### S1.3 为什么不考虑缓存方案？

有人可能建议：交易网关独立进程 + 在子进程中缓存数据

**问题分析**：

```python
# 缓存方案的实现复杂度
class CachedDataBridge:
    """在交易网关进程中缓存数据"""

    def __init__(self):
        self.cache = {}  # 本地缓存
        self.cache_ttl = 60  # 缓存60秒

    def get_kline(self, symbol, period, count):
        # 1. 检查缓存（需要实现LRU、TTL逻辑）
        # 2. 缓存未命中，通过IPC查询（需要实现IPC客户端）
        # 3. 更新缓存（需要实现缓存淘汰策略）
        # 4. 数据版本同步（需要实现版本控制）
        pass

# 代码量估算：
# - IPC通信层：500行
# - 缓存管理：400行
# - 数据同步：300行
# - 错误处理：200行
# 总计：~1400行纯"管道"代码
```

**收益分析**：

| 方面 | 缓存方案 | 同进程方案 |
|-----|---------|-----------|
| **故障隔离** | ✅ 完全隔离 | 🔶 异常捕获隔离 |
| **性能** | 🔶 首次慢，缓存命中快 | ✅ 始终最快 |
| **实现复杂度** | ❌ 非常复杂 | ✅ 简单 |
| **数据一致性** | ❌ 可能不一致 | ✅ 绝对一致 |
| **代码量** | ❌ +1400行 | ✅ 无需额外代码 |

**结论**：缓存方案的成本远大于收益，不推荐。

---

### S2. 各模块独立进程的决策依据

完整的决策矩阵：

| 模块 | 数据访问频率 | IPC开销 | 故障风险 | 独立价值 | 决策 |
|-----|------------|---------|---------|---------|------|
| **UI层** | - | - | 低 | - | ❌ Qt限制，必须主进程 |
| **UnifiedDataManager** | - | - | 低 | - | ❌ 核心枢纽，必须主进程 |
| **行情看板** | 高（60fps） | 巨大 | 低 | 低 | ❌ 性能优先 |
| **策略回测** | 高（批量） | 巨大 | 低 | 低 | ❌ 性能优先 |
| **交易网关** | 中高 | 显著 | 中 | 中 | ❌ 性能优先，用沙盒隔离 |
| **系统管理** | 低 | 小 | 低 | 低 | ❌ 监控方便 |
| **日志/告警** | 低 | 小 | **高** | **高** | ✅ **必须独立** |
| **AI助手** | 低 | 小 | 中 | 中 | 🔶 可选 |
| **组合投资** | 低 | 小 | 低 | 低 | 🔶 可选（价值不大） |

**评分说明**：
- **数据访问频率**：每秒多次 = 高，每分钟多次 = 中，用户触发 = 低
- **IPC开销**：对性能的影响程度
- **故障风险**：已知崩溃/阻塞问题
- **独立价值**：独立进程带来的收益

---

### S3. 单进程调试模式

为了方便调试，提供单进程模式开关：

#### 启用单进程模式

```bash
# Windows
set SINGLE_PROCESS_MODE=1
python start_async_fixed.py

# Linux/Mac
SINGLE_PROCESS_MODE=1 python start_async_fixed.py
```

#### 单进程模式的行为

```python
if os.getenv('SINGLE_PROCESS_MODE') == '1':
    # 不启动子进程
    # 日志/告警在主进程中运行（可能崩溃，但便于调试）
    print("⚠️ 单进程模式 - 仅用于调试")

    # 直接初始化（原有逻辑）
    from backend.core.logging_system import LoggingSystem
    from backend.core.alert_system import AlertEngine

    logging_system = LoggingSystem()
    alert_engine = AlertEngine()
else:
    # 多进程模式（生产环境）
    print("✅ 多进程模式 - 生产环境")

    # 启动子进程，使用IPC
    log_alert_proc = multiprocessing.Process(...)
    log_alert_proc.start()
```

**适用场景**：
- ✅ 本地开发调试
- ✅ 使用IDE断点调试
- ✅ 性能分析（Profiling）
- ❌ 生产环境（不稳定）

---

### S4. 性能基准测试

改造完成后，建议进行以下性能基准测试：

#### 测试1：日志性能测试

```python
# tests/test_log_performance.py
import time
import logging

def test_log_throughput():
    """测试日志吞吐量"""
    logger = logging.getLogger('test')

    count = 10000
    start = time.time()

    for i in range(count):
        logger.info(f"Test log message {i}")

    elapsed = time.time() - start
    throughput = count / elapsed

    print(f"日志吞吐量: {throughput:.0f} 条/秒")
    print(f"单条延迟: {elapsed/count*1000:.2f}ms")

    # 验收标准
    assert throughput > 1000, "日志吞吐量应 > 1000条/秒"
    assert elapsed/count < 0.01, "单条日志延迟应 < 10ms"
```

#### 测试2：告警延迟测试

```python
def test_alert_latency():
    """测试告警推送延迟"""
    from PySide6.QtCore import QElapsedTimer

    timer = QElapsedTimer()
    received = False

    def on_alert(alert):
        nonlocal received
        received = True

    # 连接信号
    alert_subscriber.alert_received.connect(on_alert)

    # 触发告警
    timer.start()
    logger.error("Test alert trigger")

    # 等待告警到达
    for _ in range(100):  # 最多等待1秒
        QApplication.processEvents()
        if received:
            break
        time.sleep(0.01)

    latency = timer.elapsed()
    print(f"告警延迟: {latency}ms")

    # 验收标准
    assert latency < 100, "告警延迟应 < 100ms"
```

#### 测试3：数据访问性能对比

```python
def test_data_access_performance():
    """对比主进程数据访问性能"""

    # 测试场景：策略回测获取历史数据
    symbols = ['000001.SZ', '600000.SH', '000002.SZ']

    start = time.time()
    for symbol in symbols:
        df = unified_manager.get_kline_data(
            symbol=symbol,
            interval='1d',
            start_date='2023-01-01',
            end_date='2024-01-01'
        )
        # 约250根K线，~20KB数据

    elapsed = time.time() - start
    print(f"3个品种历史数据查询: {elapsed*1000:.2f}ms")

    # 验收标准
    assert elapsed < 0.1, "3次查询应 < 100ms"
```

---

### S5. 未来架构演进路径

如果系统继续发展，可能的架构演进：

#### 阶段1：当前（多进程v1.0）

```
主进程：UI + 数据 + 业务逻辑
子进程：日志/告警
```

**适用规模**：
- 用户数：单用户桌面应用
- 策略数：< 20个
- 数据规模：< 100GB

#### 阶段2：扩展（多进程v2.0）

```
主进程：UI + 数据 + 核心业务
子进程1：日志/告警
子进程2：AI服务
子进程3：数据下载器
```

**适用规模**：
- 用户数：单用户，多任务并行
- 策略数：20-50个
- 数据规模：100-500GB

#### 阶段3：微服务（分布式v1.0）

```
前端：Web UI（浏览器）
后端服务：
├─ 数据服务（FastAPI）
├─ 策略服务（FastAPI）
├─ 交易服务（FastAPI）
└─ 日志/告警服务（FastAPI）
```

**适用规模**：
- 用户数：多用户，远程访问
- 策略数：> 50个
- 数据规模：> 500GB

**但当前阶段，v1.0已经足够！**

---

### S6. 常见问题FAQ

#### Q1: 为什么不把所有模块都独立进程？

**A**:
- 数据访问频率高的模块（数据服务、策略回测、交易网关）如果独立，会导致严重的性能损失（10-100倍）
- IPC通信增加架构复杂度，需要维护大量"管道"代码
- 调试困难度指数级增加
- **只隔离必要的模块是工程最佳实践**

#### Q2: 日志进程崩溃了怎么办？

**A**:
- 自动降级到本地文件（fallback.log）
- 进程管理器自动重启子进程
- 恢复后自动同步降级期间的日志
- 对主进程业务逻辑无影响

#### Q3: 如何调试多进程程序？

**A**:
- 使用单进程模式（`SINGLE_PROCESS_MODE=1`）
- 每个进程独立日志文件
- 日志中包含进程ID和线程ID
- 使用远程调试（PyCharm支持多进程调试）

#### Q4: 能否在运行时动态切换单/多进程模式？

**A**:
- 不建议。进程模式应在启动时确定
- 如需切换，重启应用即可（< 5秒）

#### Q5: 未来如果交易网关也需要独立怎么办？

**A**:
- 如果确实需要（故障频发），可以实施
- 但必须实现数据缓存层（预计增加1400行代码）
- 需要详细的性能测试验证
- 建议先尝试主进程内的故障隔离方案

---

## 快速实施检查清单 ✅

### 核心任务（预计2-3天）

#### ☑ 第1步：环境准备

```bash
# 1. 安装依赖
pip install pyzmq msgpack-python

# 2. 验证安装
python -c "import zmq; import msgpack; print('✅ OK')"

# 3. 创建目录
mkdir backend/processes
mkdir backend/core/ipc
mkdir logs/fallback

# 4. 备份代码
git checkout -b backup-before-multiprocess
git add .
git commit -m "Backup before multiprocess refactor"
```

#### ☑ 第2步：创建文件（按顺序）

1. [ ] `backend/processes/__init__.py`（空文件）
2. [ ] `backend/processes/log_alert_process.py`（代码见第6.1节）
3. [ ] `backend/core/ipc/__init__.py`（代码见第6.2节）
4. [ ] `backend/core/ipc/ipc_config.py`（代码见第6.2节）
5. [ ] `backend/core/ipc/distributed_log_handler.py`（代码见第6.2节）
6. [ ] `backend/core/ipc/alert_subscriber.py`（代码见第6.3节）

#### ☑ 第3步：修改文件

**文件1**: `backend/services/system_manager_service.py`

找到并修改：
```python
# ❌ 删除（第138-143行）
threading.Thread(
    target=self._init_logging_and_alert_system_async,
    daemon=True,
    name="LogAlertSystemInitializer",
).start()

# ✅ 替换为
self._connect_to_log_alert_process()
```

添加新方法（参考第6.4节）：
```python
def _connect_to_log_alert_process(self):
    """连接到日志/告警进程."""
    # 见第6.4节完整代码
```

**文件2**: `start_async_fixed.py`

在 `main()` 函数中添加（在环境准备之后）：
```python
# 启动日志/告警进程（见第7.2节完整代码）
```

#### ☑ 第4步：验证测试

```bash
# 1. 独立测试日志进程
python backend/processes/log_alert_process.py
# 预期输出：监听端口5557、5558、5559

# 2. 完整启动测试
python start_async_fixed.py
# 预期：启动成功，无崩溃

# 3. 连续测试（稳定性）
for i in {1..10}; do
    echo "第 $i 次测试"
    python start_async_fixed.py
done
# 预期：10次全部成功
```

### 最小化实施（如果时间紧张）

如果需要快速验证概念，可以先实施最小版本：

1. ✅ 只创建 `log_alert_process.py`（核心）
2. ✅ 只创建 `distributed_log_handler.py`（必需）
3. ✅ 修改启动流程启动子进程
4. 🔶 暂时不实现告警订阅（先解决崩溃）
5. 🔶 暂时不实现进程管理器（手动管理）

---

**重要提示**：本文档是多进程架构改造的完整指南，请严格按照文档进行实施。如有疑问，请联系架构团队。

