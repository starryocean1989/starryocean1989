# -*- coding: utf-8 -*-
# 快速测试指南 - Data Module vnpy 新功能

## 前置条件

1. 启动应用程序：`python start.py`
2. 确保 vnpy 和 data_module_vnpy 已正确安装
3. 确保有可用的品种列表缓存

## 测试 1：轮询转推送网关

### 步骤
1. 打开应用 → 数据中心 → 数据源管理
2. 找到"轮询转推送"行
3. 点击"配置"按钮
4. 填写配置：
   - 轮询间隔：10（秒）
   - 订阅品种：600000,000001
5. 点击"确定"保存配置
6. 点击"启动"按钮
7. 观察状态变化

### 预期结果
- ✅ 配置对话框正常打开和关闭
- ✅ 点击启动后显示"轮询网关启动成功"
- ✅ 状态列显示"已连接"或"推送中"
- ✅ 日志中显示轮询和推送相关信息
- ✅ 停止按钮可用

### 注意
- 非交易时间不会轮询数据，需要在交易时间段测试
- 或临时修改 `polling_gateway.py` 中的 `_is_trading_time()` 返回 `True`

---

## 测试 2：虚拟推送网关

### 步骤
1. 打开应用 → 数据中心 → 数据源管理
2. 找到"虚拟推送"行
3. 点击"配置"按钮
4. 填写配置：
   - 起始时间：选择过去有数据的某个时间点
   - 推送速度：10.0（10倍速，加快测试）
   - 订阅品种：600000,000001
5. 点击"确定"保存配置
6. 点击"启动"按钮
7. 观察状态变化

### 预期结果
- ✅ 配置对话框正常打开和关闭
- ✅ 日期时间选择器可用
- ✅ 点击启动后显示"虚拟网关启动成功"
- ✅ 状态列显示"已连接"或"推送中"
- ✅ 日志中显示数据推送相关信息
- ✅ 停止按钮可用

### 注意
- 必须先有历史1分钟线数据
- 建议先执行全量或增量下载

---

## 测试 3：数据标准化读取器

### 步骤
1. 打开应用 → 系统管理 → 工具集合
2. 在"数据标准化读取器"面板中：
3. 数据源类型：通达信（已选中）
4. 数据类型：日线
5. 市场：上证
6. 通达信根目录：`C:\new_tdx`（或你的实际路径）
   - 点击"浏览"按钮选择目录
7. 品种代码：600000,600036
8. 点击"批量读取并保存"按钮
9. 观察状态标签

### 预期结果
- ✅ 浏览按钮可以选择目录
- ✅ 点击读取后状态显示"正在读取 2 个品种..."
- ✅ 读取完成后显示"读取完成：成功 X 个，失败 Y 个"
- ✅ 弹出提示框显示结果
- ✅ 数据保存到 `data/kline/{symbol}/` 目录

### 验证数据
```bash
# 检查保存的 Parquet 文件
dir data\kline\600000\
dir data\kline\600036\

# 应该看到类似：600000_1d.parquet 的文件
```

---

## 测试 4：网关互斥测试

### 步骤
1. 启动轮询网关
2. 尝试启动虚拟网关

### 预期结果
- ✅ 显示错误："轮询网关正在运行，同时只能运行一个推送网关"
- ✅ 虚拟网关启动失败
- ✅ 轮询网关继续运行

---

## 常见问题排查

### Q1: 网关启动失败
**可能原因**：
- MainEngine 或 EventEngine 不可用
- 底层包导入失败

**排查步骤**：
1. 查看日志：`logs/terminal_v0.50.log`
2. 检查 vnpy 是否正确安装：`pip list | findstr vnpy`
3. 检查 mootdx 是否安装：`pip list | findstr mootdx`

### Q2: 数据读取失败
**可能原因**：
- 通达信目录不正确
- 品种代码不存在
- 文件权限问题

**排查步骤**：
1. 验证目录路径：`dir {tdx_root}\vipdoc\sh\lday`
2. 检查文件是否存在：`dir {tdx_root}\vipdoc\sh\lday\sh600000.day`
3. 查看详细错误日志

### Q3: 虚拟网关无数据推送
**可能原因**：
- 本地没有历史1分钟线数据
- 起始时间超出数据范围

**排查步骤**：
1. 检查数据目录：`dir data\kline\{symbol}\`
2. 确认有 `{symbol}_1m.parquet` 文件
3. 调整起始时间到数据范围内

---

## 性能测试

### 轮询网关性能
- 测试品种数：10, 50, 100
- 轮询间隔：10秒
- 监控CPU和内存使用

### 数据读取器性能
- 测试品种数：10, 50, 100
- 测试数据类型：日线、5分钟线、1分钟线
- 记录处理时间

---

## 测试完成检查清单

- [ ] 轮询网关可以正常启动和停止
- [ ] 虚拟网关可以正常启动和停止
- [ ] 两个网关互斥控制正常工作
- [ ] 数据读取器可以读取日线数据
- [ ] 数据读取器可以读取5分钟线数据
- [ ] 数据读取器可以读取1分钟线数据
- [ ] 配置持久化正常工作
- [ ] UI状态显示正确
- [ ] 错误提示友好且准确
- [ ] 日志记录完整

---

## 报告问题

如发现问题，请提供：
1. 操作步骤
2. 预期结果 vs 实际结果
3. 错误截图
4. 日志文件片段

测试愉快！🎉

