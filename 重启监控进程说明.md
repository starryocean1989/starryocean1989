# 磁盘IO监控更新说明

## ✅ 已完成的修改

1. **显示名称**：从 `PhysicalDrive0` 改为 `物理磁盘0`
2. **监控对象**：从逻辑分区（C:\、D:\、E:\ 等）改为真实物理磁盘
3. **数据增强**：新增 `partitions`（所属分区）和 `is_system_disk`（是否系统盘）字段

## 📋 当前检测结果

根据测试，你的系统有 **3个物理磁盘**：
- `物理磁盘0`: 931.51 GB SSD，包含分区 E:、D:
- `物理磁盘1`: 232.88 GB SSD，包含分区 C:（系统盘）
- `物理磁盘2`: 953.86 GB SSD，包含分区 F:

## ⚠️ 重要：需要重启监控进程

系统监控使用**独立进程**运行（`monitor_process_entry.py`），修改代码后需要：

### 方法1：使用重启脚本（推荐）
```powershell
# 在项目根目录运行
.\restart_monitor_process.ps1
```

### 方法2：手动重启
1. 查找监控进程：
```powershell
Get-Process python | Where-Object {$_.CommandLine -like '*monitor_process*'}
```

2. 结束旧进程：
```powershell
# 找到进程ID后
Stop-Process -Id <进程ID> -Force
```

3. 重新启动终端：
```bash
python start_async_fixed.py
# 或者
启动终端（增强版）.bat
```

### 方法3：重启整个应用
直接关闭并重新运行终端应用即可。

## 🔍 如何验证修改生效

运行验证脚本：
```bash
python check_monitor_data_flow.py
```

应该看到：
- ✅ 显示 3 个磁盘（而不是6个）
- ✅ 磁盘名称为中文：`物理磁盘0`、`物理磁盘1`、`物理磁盘2`
- ✅ 每个磁盘都有 `partitions` 字段显示包含的分区

## 📊 新的数据格式

```json
{
  "物理磁盘0": {
    "read_speed": 0.0,
    "write_speed": 0.0,
    "disk_type": "ssd",
    "partitions": ["E:", "D:"],
    "is_system_disk": false
  }
}
```

## ❓ 如果仍然显示6个磁盘

可能的原因：
1. **监控进程未重启** → 按上述方法重启
2. **UI界面缓存** → 刷新UI界面或重启应用
3. **多个监控进程同时运行** → 查找并结束多余进程

检查是否有多个监控进程：
```powershell
Get-Process python | Where-Object {$_.CommandLine -like '*monitor*'} | Select-Object Id, ProcessName, CommandLine
```

如果发现多个，结束旧的进程：
```powershell
Stop-Process -Id <旧进程ID> -Force
```

