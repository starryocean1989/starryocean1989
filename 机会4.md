# -*- coding: utf-8 -*-
# C 扩展替代机会（第四批）

## 1. 多进程日志管道原生化

- **现状**：`MultiProcessLogCollector` 通过 `multiprocessing.managers.SyncManager` 暴露日志队列，主进程再用 `QueueListener` 消费，子进程需要先 base64→pickle 反序列化代理后才能写入。随着三进程架构和大量服务启动，`manager.start()`、`Queue()` 创建与序列化/反序列化引入数百毫秒延迟，并在高并发日志突发时出现 GIL 争用。
```672:745:backend/infrastructure/system_vnpy/logging_system.py
        manager = SyncManager(address=("127.0.0.1", 0), authkey=authkey)
        manager.start()
        queue_proxy = manager.Queue(-1)
        queue_pickled = base64.b64encode(pickle.dumps(queue_proxy)).decode("ascii")
        ...
        self.queue_listener = QueueListener(
            self.queue,
            self.logging_hub,
            respect_handler_level=True,
        )
```
- **机会**：在 `native_log_pipeline` 或新增 `native_log_bridge` 中提供跨进程无锁环形缓冲，实现 `create_shared_log_ring(capacity)`/`attach_shared_log_ring(handle)` API，内部使用命名共享内存 + 原子写指针。主进程可直接把 ring 绑定到现有 `LoggingHub`，子进程通过句柄写入结构化 `UnifiedLogRecord`，无须 `QueueHandler`/pickle。保留 Python 路径作为降级。
- **预期收益**：日志初始化阶段减少 `SyncManager` 启动阻塞，进程间日志写入改为内存复制 + 原子 CAS，可把峰值吞吐从当前 ~5 万条/秒提升到 20 万条/秒以上，并降低高频日志场景下的 CPU 占用。

## 2. 进程快照采集下沉到 native_process_metrics

- **现状**：监控进程仍依赖 `psutil.process_iter()` 周期性扫描所有进程，逐个调用 `memory_info()` 累加。三进程架构下 UI/Data/Monitor 同时上报，`process_iter` 在 Windows 上每次遍历 300+ 进程要 80-120ms，且会触发频繁的 Python 对象创建。
```6708:6758:backend/infrastructure/system_vnpy/monitor_system.py
        for proc in psutil.process_iter(
            ["pid", "name", "cpu_percent", "memory_percent", "status"]
        ):
            proc_info: Any = proc.info
            proc_info["memory_mb"] = proc.memory_info().rss / 1024 / 1024
            processes.append(proc_info)
```
- **机会**：扩展 `native_process_metrics`，新增 `enumerate_processes(limit, sort_key)` 接口，基于 `NtQuerySystemInformation` / `SystemProcessInformation` 一次性获取快照并在 C 层排序（CPU、内存、线程数），可选地直接返回 top-N 结果。Python 侧保留现有签名，默认走原生路径，失败时回退 `psutil`。
- **预期收益**：将单次进程列表采集成本压到 5-8ms，并减少大量 `psutil.Process` 对象创建与 GIL 切换，监控循环可降到 3 秒内稳定运行，不再因大规模进程迭代导致 UI 指标卡顿。

## 3. 定时任务调度器原生线程池化

- **现状**：`DataCenterService` 采用 `APScheduler` + `ThreadPoolExecutor` 执行清理、校验等周期任务。调度器自身依赖 Python `threading` 定时，频繁 wake-up 触发 GIL；同时 Qt 环境下还需要手动避免 `QTimer` 互斥，调度粒度被迫控制在秒级。
```474:505:backend/services/data_center_service.py
from apscheduler.schedulers.background import BackgroundScheduler
from apscheduler.executors.pool import ThreadPoolExecutor
executors = {"default": ThreadPoolExecutor(max_workers=2)}
self.scheduler = BackgroundScheduler(executors=executors)
self.scheduler.add_job(self._cleanup_recorded_data_daily, "cron", hour=0, minute=0, ...)
self.scheduler.start()
```
- **机会**：基于现有 `native_scheduler` 扩展（C 端高精度计时 + 原生线程池），封装 `NativeCronScheduler`，支持 Cron / interval / 一次性任务。调度任务在 C 层维护最小堆，回调通过 `native_threadpool` 或 `native_gil.task_executor` 下发到 Python，避免 APScheduler 层层包装。服务启动时优先使用原生调度器，APS 作为兜底。
- **预期收益**：调度循环 CPU 占用下降 60%+，触发延迟缩短到亚毫秒；夜间清理、增量刷新等任务可在不争用 GIL 的情况下运行，避免 APScheduler 在高并发时出现 drift 或线程堆积问题。

## 4. 数据文件监控事件流原生化

- **现状**：`DataFileWatcher` 仍在 Python 线程里每秒调用 `_scan_directory()` 与 `_process_pending_events()` 轮询目录，依赖 `time.sleep(1.0)` 和跨平台遍历函数。监控目录包含上万 Parquet/K 线文件时，每次扫描都会创建大量 Python 对象并长时间持有 GIL，直接拖慢数据进程与 UI 事件循环。
```1749:1782:backend/infrastructure/data_module_vnpy/data_quality.py
    def _watch_loop(self):
        """监控循环"""
        logger.info("🔍 文件监控循环启动")

        while self._watching:
            try:
                self._scan_directory()
                self._process_pending_events()
                time.sleep(1.0)  # 每秒扫描一次
            except Exception as e:
                logger.error(
                    f"❌ 文件监控循环异常: {e}", exc_info=True, extra={"log_type": "SYSTEM"}
                )
```
- **机会**：在 `native_fs` 模块新增 `watch_directory(path, callback, recursive)`，Windows 侧封装 `ReadDirectoryChangesW` + IOCP，Linux 侧封装 `inotify`，再通过无锁环形缓冲把事件推送到数据进程事件循环。Python 侧仅负责把事件发布到 `EventEngine` 或 `DataQualityService`，保留现有轮询逻辑作为降级路径。
- **预期收益**：轮询延迟从 1 秒降至 <5ms，彻底消除高频磁盘遍历带来的 CPU 峰值与 GIL 争用；数据文件追加/修复在原生事件触发后能即时同步到缓存层，避免 UI 等待整轮扫描。

## 5. Socket 缓冲区统计扩展原生化

- **现状**：`SystemMonitor.get_socket_buffer_info()` 原生接口不可用时，会退回到 Python 遍历 `psutil.net_connections(kind="inet")`，对每个 TCP 连接通过 `socket.fromfd` + `getsockopt` 取缓冲区大小，并为了估算使用率强制 `time.sleep(0.05)` 读取两次 `psutil.net_io_counters()`。在 500+ 连接场景下单次调用耗时 180-250ms，且频繁触发 `OSError`/权限异常。
```6404:6532:backend/infrastructure/system_vnpy/monitor_system.py
        total_connections = len(connections)
        tcp_connections = 0
        established_connections = 0
        recv_buffer_sizes = []
        send_buffer_sizes = []

        for conn in connections:
            try:
                if conn.type != socket_module.SOCK_STREAM:
                    continue
                tcp_connections += 1
                if conn.status == 'ESTABLISHED':
                    established_connections += 1
                if hasattr(conn, 'fd') and conn.fd is not None and conn.fd >= 0:
                    sock = socket_module.fromfd(conn.fd, socket_module.AF_INET, socket_module.SOCK_STREAM)
                    recv_buffer_sizes.append(sock.getsockopt(socket_module.SOL_SOCKET, socket_module.SO_RCVBUF))
                    send_buffer_sizes.append(sock.getsockopt(socket_module.SOL_SOCKET, socket_module.SO_SNDBUF))
                    sock.close()
            except Exception as e:
                logger.debug("[SOCKET-BUFFER] 处理连接失败: %s", e, extra={"log_type": "SYSTEM"})
                continue

        if recv_buffer_size_avg > 0:
            net_io_1 = psutil.net_io_counters()
            time.sleep(0.05)
            net_io_2 = psutil.net_io_counters()
            recv_buffer_usage_ratio = ...
```
- **机会**：扩展 `native_socket_metrics` 增加 `get_tcp_buffer_snapshot()`，Windows 使用 `GetExtendedTcpTable`/`WSAIoctl(SIO_TCP_INFO)`/`GetPerTcpConnectionEStats`，Linux 使用 `NETLINK_INET_DIAG`/`TCP_INFO` 直接返回缓冲区大小、使用率、拥塞窗口等聚合指标。Python 仅接收结构化结果，原 psutil 路径保留兜底。
- **预期收益**：单次采集降至 <10ms，避免对 `psutil` 的高频遍历及 `socket.fromfd` 失败日志，监控进程 CPU 占用可下降 70%+，并能获得更精确的网络拥塞告警。

## 6. 负载均衡配置计算下沉 C 扩展

- **现状**：`LoadBalancer.get_optimal_config()` 在每次并发评估时都要在 Python 中串行执行“资源木桶评估→队列压力→服务器约束→任务约束”。三进程架构下它以 1 秒频率运行，单次计算要 6-8ms，并阻塞资源采集线程的 GIL，限制了并发调整频率。
```1690:1838:backend/infrastructure/data_module_vnpy/load_balancer.py
    def get_optimal_config(...):
        ...
        resource_metrics = self.resource_monitor.get_metrics()
        ...
        processes = max(1, int(strategy["base_processes"] * resource_scale))
        coroutines_per_process = max(10, int(strategy["base_coroutines_per_process"] * resource_scale))
        ...
        max_total_coroutines = self.server_pool.calculate_total_max_connections()
        if total_concurrency > max_total_coroutines:
            coroutines_per_process = max(1, max_total_coroutines // processes)
            if coroutines_per_process < 3 and processes > 1:
                processes = max(1, max_total_coroutines // 3)
                coroutines_per_process = max(1, max_total_coroutines // processes)
```
- **机会**：实现 `native_load_balancer.optimize(task_meta, resource_snapshot, queue_stats)`，在 C 层读取共享内存化的资源指标、队列历史与服务器池信息完成浮点缩放、整数约束与模式防抖，并将结果写回结构体。Python 只需封装/回退，配合已有的 `native_process_metrics`/`native_scheduler` 数据面。
- **预期收益**：配置计算时延降至 <1ms，可把负载均衡刷新频率提升到 200ms 级；同时减少 `LoadBalancer` 与 `ResourceMonitor` 间的 GIL 争用，使下载/扫描任务能更快地收敛到最优并发。


