# 🎯 Terminal v0.50 启动架构修复计划

- **更新时间**：2025-11-10
- **当前版本**：v0.50
- **状态**：架构第三阶段未完成，启动流程仍失败
- **目标**：恢复可用启动路径，明确剩余技术债务与优化路线

---

## 1. 当前状态概览

| 维度 | 现状 | 影响 |
| --- | --- | --- |
| 启动结果 | ❌ `start_new.py` 仍然退出，主进程在导入阶段失败 | 无法进入应用，阻断所有后续体验 |
| 日志系统 | ⚠️ `LazyLogger` 被迫禁用，日志恢复为同步写入 | 启动性能回退，无法验证第三阶段成果 |
| Native 扩展 | ✅ native_fs / native_collections 严格使用原生 C 扩展（`.pyd`），不再提供降级实现 | 启动链路依赖 C 扩展可用性，性能为原生水平 |
| 文档一致性 | ⚠️ 原方案仍标记为“已完成” | 信息与真实状态不符，阻碍排期与沟通 |

---


- **验收标准**：
  - LazyLogger 默认启用，`initialize_logging_hub_complete` 返回时间 < 1.5s；
  - 当 LazyLogger 不可用时，日志系统自动降级且日志中包含明确告警；
  - 文档与代码一致，描述当前行为与回退策略。

## 3. 执行路线与优先级

| 优先级 | 事项 | 负责人 | 截止 | 输出 |
| --- | --- | --- | --- | --- |
| P0 | 恢复 native 扩展（严格原生实现），确保 `start_new.py` 可运行 | 架构组 | 2025-11-12 | 新的 `.pyd` 包、自检脚本 | ✅ 已完成 - 重新启用 native_fs/native_collections C 扩展，移除降级实现 |
| P0 | 调整 LazyLogger 集成，恢复异步日志初始化 | 架构组 | 2025-11-14 | 更新后的 `logging_system.py`、自动化测试 |
| P1 | 更新《启动完整设计文档》与本方案 | 架构组 | 2025-11-21 | 与代码一致的文档版本 |

---
## 5. 当前状态总结

**✅ 已完成**：
- **P0 任务**：native_fs、native_collections 严格使用原生 C 扩展，移除所有降级实现，启动链路依赖 C 扩展可用性
- **文档更新**：移除了已完成的内容，保留未实施的项目，更新了当前状态和实施路线

**🔄 进行中**：
- 等待测试验证 start_new.py 是否能成功运行
- 需要进一步诊断其余模块导入问题（如 backend.core.vnpy_imports）
- 已在 `logging_system.py` 中默认禁用 LazyLogger，避免导入期崩溃，后续需补齐依赖后再恢复异步路径

**📋 后续工作**：
- P0: 调整 LazyLogger 集成，恢复异步日志初始化
- P1: 更新相关设计文档

---

## 6. 附录：近期事实校验

- 2025-11-10：native_fs 和 native_collections C 扩展重新启用，移除降级实现以恢复原生性能
- 2025-11-10：DataModelManager 已支持 dict 和 C++ 缓存的双重兼容
- 2025-11-10：backend/infrastructure/native/__init__.py 已重新启用 native_fs 和 native_collections 模块导入
- 2025-11-10：优化.md 已更新，反映当前真实状态而非过时信息

> **备注**：待上述问题关闭后，再恢复本文原有的阶段性路线与长期规划。
