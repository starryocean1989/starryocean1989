# -*- coding: utf-8 -*-
# 优化6：三进程新增热点的原生扩展替换方案

## 一、背景
- v0.50 三进程架构上线后，组合分析、跨进程 RPC 与监控网络探测进入高频使用阶段，现有 Python 实现开始暴露 CPU 占用与事件循环阻塞问题。
- 现行《优化4》《优化5》主要覆盖系统监控底层采集、数据模块 DataFrame 管线与负载均衡；本次聚焦仍未被替换的业务逻辑热点，补齐全链路的 C 扩展落地路线。

## 二、待优化热点

### 2.1 组合分析服务的 Python 聚合链路
- `backend/services/portfolio_service.py` 中 `_get_portfolio_returns`、`_calculate_performance_curve`、`_calculate_period_statistics_data`、`_calculate_max_drawdown_duration` 依靠 Python `dict`/`defaultdict`、多重循环与 `datetime` 解析完成日频聚合与绩效指标计算。
- 在 200+ 组合或高频刷新场景下，上述函数会反复执行 `sorted`、`for` 循环以及 `datetime.strptime`，UI 线程卡顿明显，且与 `numpy`/`pandas` 混用导致频繁的对象创建与 GC。

### 2.2 数据进程 RPC 桥接的同步阻塞与多重序列化
- `backend/infrastructure/data_module_vnpy/data_process_client.py` 的 `call()` 会在 UI/服务线程里调用 `loop.run_until_complete()`，当 `qasync` 已接管事件循环时，该同步调用会触发 `RuntimeError` 或阻塞 UI。
- 请求报文使用 Python 层的 `dict → orjson.dumps → AsyncIPCPipe.write`，响应再 `orjson.loads`，中间仍需复制 bytes 并创建中间对象，缺少与 `native_serialization`、`native_ipc` 的零拷贝协同。
- data/data_calculation 管道选择、超时重试、请求 ID 校验均在 Python 层完成，随着调用量增加，锁竞争与日志输出占用显著。

### 2.3 监控工具的串行 Socket 探测
- `backend/infrastructure/system_vnpy/monitor_toolkit.py` 中 `NetworkTester.test_connectivity` / `PortScanner.scan_ports` 逐端口同步调用 `socket.connect_ex`，`BandwidthMonitor`（`monitor_system.py`）也依赖 `requests`/同步 I/O，每次网络诊断都阻塞 `SystemMonitor` 的事件循环。
- 当前逻辑缺乏批量探测与 IOCP 支持，无法与 `native_ipc` 共享线程池；当需要同时测试数十台服务器时，CPU 与等待时间迅速上升，导致告警与 UI 反馈延迟。

## 三、建议的 C 扩展方案

### 3.1 `native_finance_ops`：组合分析原生算子
- **功能**：
  - 提供 `aggregate_daily_pnl(date: int[], pnl: double[]) -> struct`，在 C 层完成按日期聚合、排序与累计收益曲线生成；
  - 提供 `compute_return_metrics(pnl_series, equity_series)`，一次性输出收益率、波动率、夏普、最大回撤与回撤持续天数；
  - 提供 `bucketize_period(equity_series, period_mode)`，将日序列切片为周/月/年分组并返回聚合结果。
- **对接路径**：`PortfolioService` 中上述方法改为调用原生 API，并保留 Python fallback；结果通过 `memoryview`/`pyarrow.Array` 与现有 UI 绑定。
- **收益**：在 10 万条成交记录测试下，预计 CPU 降幅 60%+，组合视图刷新延迟从 ~600ms 降至 <150ms；同时减少 Python GC 压力。
- **注意事项**：需关注金融统计的数值稳定性（双精度误差累计），并与回测结果进行基准对比；提供 `USE_NATIVE_FINANCE_OPS` 环境开关。

### 3.2 `native_rpc_bridge`：零拷贝 RPC 管线
- **功能**：
  - 在 C 扩展中封装 `AsyncIPCPipe` 读写，直接接受 Python `dict`/`TypedDict` 并输出共享内存视图，避免 Python 层多次 `json.dumps/loads`；
  - 搭配 `native_serialization` 的结构化消息头（method_id、payload_size、routing_flag），在 C 层完成请求 ID/超时验证并返回状态码；
  - 提供同步/异步双接口：`call_sync(method_id, payload)` 内部使用 IOCP 事件等待，替换 `loop.run_until_complete`；异步接口直接返回 awaitable，兼容 `qasync`。
- **对接路径**：`DataProcessClient` 替换 `call`/`call_async` 实现，将方法名称映射为短整数 ID，暴露回退逻辑（当扩展不可用时继续使用现有 Python 版本）。
- **收益**：
  - 消除 UI 阻塞风险，确保在 qasync 下稳定运行；
  - 单次 RPC CPU 开销预计下降 40%~55%，高频查询时跨进程延迟可降低 15~20ms；
  - 为后续批量请求（pipeline/batching）铺平道路。
- **注意事项**：需要保证跨语言序列化兼容性（保持 JSON/Arrow 格式），并在信号文件失效或子进程崩溃时安全回退。

### 3.3 `native_netprobe`：IOCP 网络探测器
- **功能**：
  - 基于 Windows IOCP/`ConnectEx` 实现批量端口探测，支持超时、重试与并发度控制；
  - 在扩展层实现简单的带宽下载器（WinHTTP/WinInet）并支持实时速率回调，替换 `requests` 的阻塞调用；
  - 与 `native_ipc` 共享事件循环，实现探测结果的异步广播。
- **对接路径**：
  - `NetworkTester`/`PortScanner` 改为调用 `native_netprobe.multi_connect`；
  - `BandwidthMonitor` 使用新的 `native_netprobe.speed_test(server_cfg)`，并将进度推送到 `SystemMonitor` 事件引擎。
- **收益**：
  - 单次全量端口扫描耗时由 2~3s 降至 <300ms，CPU 峰值下降 70% 以上；
  - 带宽测试可在 1~2 秒内完成并返回稳定结果，避免阻塞监控线程。
- **注意事项**：需要处理管理员权限、TLS 证书验证与错误回退；同时保留现有 Python 实现作为兼容模式。

## 四、落地步骤与风险控制
1. **原型阶段**：
   - 基于 Cython/CPython API 编写 MVP，覆盖核心函数并与单元测试（组合指标、RPC 往返、端口探测）对比；
   - 将性能基准纳入 `tests/test_batch_performance.py`、新增 `tests/test_finance_ops.py`、`tests/test_rpc_bridge.py`、`tests/test_netprobe.py`。
2. **集成阶段**：
   - 在服务初始化时检测扩展可用性，按模块进行灰度开关（配置或环境变量）；
   - 与日志系统结合，若捕获原生异常则回退 Python 路径并输出 `WARNING`。
3. **发布阶段**：
   - 在三进程联调环境验证长时间运行稳定性与互斥行为；
   - 更新 `startup` 日志埋点，记录原生模块启用状态，便于运维追踪；
   - 补充打包脚本，确保新扩展在 CI/CD 中自动编译。

## 五、优先级建议

| 优先级 | 模块 | 说明 | 预期收益 | 风险评估 |
| --- | --- | --- | --- | --- |
| 🔴 高 | `native_finance_ops` | 直接影响组合视图响应速度与策略复盘体验 | CPU ↓60%+，刷新延迟显著下降 | 中：需验证金融指标精度 |
| 🔴 高 | `native_rpc_bridge` | 解决 UI 阻塞 & RPC 高并发开销 | 跨进程延迟 ↓15~20ms，稳定性提升 | 中：需完善异常回退 |
| 🟠 中 | `native_netprobe` | 优化网络诊断与告警实时性 | 扫描 & 测速耗时 ↓70% | 中高：涉及 WinHTTP/权限兼容 |

> 建议优先落地 `native_finance_ops` 与 `native_rpc_bridge`，同步开展 `native_netprobe` 的接口设计与权限评估，再根据测试结果逐步推广到监控进程。


