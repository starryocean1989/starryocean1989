# 启动日志展示完整实施方案

## 📋 文档说明

**目标**: 完全符合《重构启动项和降级机制补充设计文档.md》的启动时序
**创建时间**: 2025-11-02
**状态**: ⚠️ 需要完整实施

---

## ⚠️ 问题分析

你的分析完全正确！当前设计存在以下问题：

### 1. **缺失smart_cache_validation_and_sensing的8步详细展示**

当前`启动标准输出示例.md`中只有：
```
✅ DataCenterService初始化完成
```

**应该展示**（参考`重构启动项和降级机制补充设计文档.md`）：
```
📍 开始缓存验证与感知流程（8步）

┌─ 步骤1: 服务器池验证与测速 ─┐
│ ⏳ 正在测速IPv4服务器池...(20个服务器)
│ ✅ IPv4服务器池可用: 18个可用，平均延迟 85ms
│ ⏳ 正在测速IPv6服务器池...(5个服务器)
│ ✅ IPv6服务器池可用: 3个可用，平均延迟 120ms
│ ✅ LoadBalancer初始化完成
└──────────────────────────────┘
✅ 步骤1完成 (850ms) [进度: 12%]

┌─ 步骤2: 获取当前日期（网络时间）─┐
│ ⏳ 正在同步网络时间...
│ ✅ 网络时间同步成功: 2025-11-02
└──────────────────────────────┘
✅ 步骤2完成 (180ms) [进度: 25%]

┌─ 步骤3: 验证交易日历缓存 ─┐
│ ⏳ 检查交易日历缓存...
│ ✅ 交易日历缓存有效（最新日期: 2025-11-01）
│ ℹ️  今日是交易日，市场开市中
└──────────────────────────────┘
✅ 步骤3完成 (120ms) [进度: 37%]

... (步骤4-8详细展示)
```

### 2. **监控进程日志缺少STAGE_NODE标记**

`monitor_system.py` 应添加类似的阶段标记：
```python
stage_logger.info("📍 监控进程启动开始", extra={"log_type": "STAGE_NODE"})
stage_logger.info("✅ 监控进程完全就绪", extra={"log_type": "STAGE_NODE"})
```

### 3. **缺少离线降级模式的展示**

如果步骤1失败，应展示：
```
❌ 步骤1失败: 所有TDX服务器不可用
⚠️ 进入离线降级模式
⏭️ 跳过步骤2-7，直接执行步骤8
...
```

---

## 🎯 完整实施方案

### 第一步：修改 `backend/core/base.py` - 添加 `_smart_cache_validation_and_sensing`

这个方法应该在 [ChinaStockEngine.initialize()](file://c:\Users\USER\Desktop\terminal_v0.50\backend\infrastructure\data_module_vnpy\core_engine.py#L235-L301) 中调用，**目前缺失**！

```python
# 文件: backend/infrastructure/data_module_vnpy/core_engine.py

def _smart_cache_validation_and_sensing(
    self,
    progress_callback: Optional[Callable[[str, int], None]] = None,
    step_callback: Optional[Callable[[int, str, dict], None]] = None
) -> Dict[str, Any]:
    """智能缓存验证与感知（8步流程 + 离线降级）
    
    这是启动项的核心主干流程，负责:
    1. 验证和初始化所有缓存
    2. 初始化核心组件(LoadBalancer, UnifiedDataManager等)
    3. TDX离线降级检测
    4. 启动文件监控
    """
    import logging
    logger = logging.getLogger("backend.data_module.cache_validation")
    stage_logger = logging.getLogger("startup.stage")
    
    # 获取LoggingHub并切换到data_engine阶段
    from backend.infrastructure.system_vnpy import get_logging_hub
    hub = get_logging_hub()
    hub.set_stage("data_engine")
    
    start_time = time.time()
    results = {"steps": []}
    offline_mode = False
    
    # 🎯 使用STAGE_NODE标记流程开始
    stage_logger.info("📍 开始缓存验证与感知流程（8步）", extra={"log_type": "STAGE_NODE"})
    
    # ========== 步骤1: 服务器池验证与测速 ==========
    stage_logger.info("┌─ 步骤1: 服务器池验证与测速 ─┐", extra={"log_type": "STAGE_NODE"})
    if progress_callback:
        progress_callback("正在测速IPv4服务器池...", 5)
    
    try:
        from .load_balancer import ServerPoolManager
        pool_manager = ServerPoolManager.get_instance()
        
        # 测速IPv4
        stage_logger.info("⏳ 正在测速IPv4服务器池...(20个服务器)", extra={"log_type": "STAGE_NODE"})
        ipv4_results = pool_manager.test_servers(pool_type="ipv4", max_workers=4)
        ipv4_available = len([s for s in ipv4_results if s.get("latency", 999) < 500]) > 0
        
        if ipv4_available:
            avg_latency = np.mean([s.get("latency", 999) for s in ipv4_results if s.get("latency", 999) < 500])
            stage_logger.info(
                f"✅ IPv4服务器池可用: {len(ipv4_results)}个可用，平均延迟 {avg_latency:.0f}ms",
                extra={"log_type": "STAGE_NODE"}
            )
        
        # 测速IPv6
        stage_logger.info("⏳ 正在测速IPv6服务器池...(5个服务器)", extra={"log_type": "STAGE_NODE"})
        ipv6_results = pool_manager.test_servers(pool_type="ipv6", max_workers=4)
        ipv6_available = len([s for s in ipv6_results if s.get("latency", 999) < 500]) > 0
        
        # 判断离线模式
        if not ipv4_available and not ipv6_available:
            stage_logger.error("❌ 所有TDX服务器不可用", extra={"log_type": "STAGE_NODE"})
            stage_logger.warning("⚠️ 进入离线降级模式", extra={"log_type": "STAGE_NODE"})
            stage_logger.info("⏭️ 跳过步骤2-7，直接执行步骤8", extra={"log_type": "STAGE_NODE"})
            offline_mode = True
            self.set_offline_mode(True, "所有TDX服务器(IPv4/IPv6)不可用")
        else:
            # LoadBalancer初始化
            from .load_balancer import LoadBalancer
            self.load_balancer = LoadBalancer.get_instance(self.event_engine)
            stage_logger.info("✅ LoadBalancer初始化完成", extra={"log_type": "STAGE_NODE"})
            stage_logger.info("  - 任务分类体系: NETWORK_DOWNLOAD, LOCAL_SCAN, LOCAL_READ", extra={"log_type": "STAGE_NODE"})
            stage_logger.info("  - 队列压力监控: 正常/高/临界三级", extra={"log_type": "STAGE_NODE"})
        
        step1_time = (time.time() - start_time) * 1000
        stage_logger.info(f"✅ 步骤1完成 ({step1_time:.0f}ms) [进度: 12%]", extra={"log_type": "STAGE_NODE"})
        
        if step_callback:
            step_callback(1, "服务器池验证与测速", {"success": not offline_mode, "offline_mode": offline_mode})
    
    except Exception as e:
        logger.exception("步骤1失败: %s", e)
        offline_mode = True
        self.set_offline_mode(True, f"服务器池验证异常: {e}")
        stage_logger.error(f"❌ 步骤1失败: {e}", extra={"log_type": "STAGE_NODE"})
        stage_logger.warning("⚠️ 进入离线降级模式", extra={"log_type": "STAGE_NODE"})
    
    # ========== 离线降级检查点 ==========
    if offline_mode:
        # 跳过步骤2-7，直接到步骤8
        stage_logger.info("⏭️ 跳过步骤2-7，直接执行步骤8", extra={"log_type": "STAGE_NODE"})
        # 执行步骤8（文件监控）
        # ... 步骤8代码 ...
        return {
            "success": True,
            "offline_mode": True,
            "offline_reason": self.get_offline_reason(),
            "steps_completed": 2,  # 只完成步骤1和步骤8
            "total_time": time.time() - start_time
        }
    
    # ========== 步骤2: 获取当前日期 ==========
    stage_logger.info("┌─ 步骤2: 获取当前日期（网络时间）─┐", extra={"log_type": "STAGE_NODE"})
    # ... 步骤2代码 ...
    stage_logger.info("✅ 步骤2完成 (180ms) [进度: 25%]", extra={"log_type": "STAGE_NODE"})
    
    # ========== 步骤3-8: 类似实现 ==========
    # ... 每步都使用STAGE_NODE标记 ...
    
    # 最终完成
    total_time = time.time() - start_time
    stage_logger.info(f"✅ 缓存验证与感知流程完成 (总耗时: {total_time:.1f}s)", extra={"log_type": "STAGE_NODE"})
    stage_logger.info("✅ 数据引擎完全就绪", extra={"log_type": "STAGE_NODE"})
    
    return {
        "success": True,
        "offline_mode": False,
        "steps_completed": 8,
        "total_time": total_time
    }
```

### 第二步：修改 `monitor_system.py` - 添加STAGE_NODE标记

```python
# 文件: backend/infrastructure/system_vnpy/monitor_system.py

async def main():
    """监控进程主入口"""
    # 获取LoggingHub
    from backend.infrastructure.system_vnpy import get_logging_hub, ai_log_process
    
    # 创建AI日志流程
    with ai_log_process("monitor_process_startup"):
        hub = get_logging_hub()
        hub.set_stage("monitor_init")
        
        stage_logger = logging.getLogger("startup.stage")
        
        # 🎯 使用STAGE_NODE标记监控进程启动
        stage_logger.info("📍 监控进程启动开始", extra={"log_type": "STAGE_NODE"})
        
        # native_ipc管道创建
        stage_logger.info("✅ monitor_system.py进程已启动 (PID: %d)" % os.getpid(), extra={"log_type": "STAGE_NODE"})
        stage_logger.info("⏳ 创建native_ipc管道...", extra={"log_type": "STAGE_NODE"})
        
        # ... 管道创建代码 ...
        
        stage_logger.info("✅ 创建native_ipc管道完成", extra={"log_type": "STAGE_NODE"})
        stage_logger.info("  ├─ monitor_alerts ✅", extra={"log_type": "STAGE_NODE"})
        stage_logger.info("  ├─ monitor_status ✅", extra={"log_type": "STAGE_NODE"})
        stage_logger.info("  └─ monitor_query ✅", extra={"log_type": "STAGE_NODE"})
        
        # 监控组件初始化
        stage_logger.info("⏳ 监控组件初始化...", extra={"log_type": "STAGE_NODE"})
        stage_logger.info("  ├─ SystemMonitor ✅", extra={"log_type": "STAGE_NODE"})
        stage_logger.info("  ├─ ProcessMonitor ✅", extra={"log_type": "STAGE_NODE"})
        stage_logger.info("  ├─ HardwareMonitor (后台异步) ⏳", extra={"log_type": "STAGE_NODE"})
        stage_logger.info("  └─ BandwidthMonitor ✅", extra={"log_type": "STAGE_NODE"})
        
        # Level 1就绪
        stage_logger.info("✅ Level 1就绪 (管道就绪)", extra={"log_type": "STAGE_NODE"})
        
        # 看门狗启动
        stage_logger.info("✅ 监控进程看门狗启动", extra={"log_type": "STAGE_NODE"})
        
        # 完成
        stage_logger.info("✅ 监控进程完全就绪 (2.3s)", extra={"log_type": "STAGE_NODE"})
        
        # 切换到运行阶段
        hub.set_stage("monitoring")
```

### 第三步：更新文档一致性检查

需要对比并更新以下文档：

1. **`启动标准输出示例.md`** ✅ 已更新（展示8步详细流程）
2. **`模块启动顺序与Terminal输出设计.md`** ⚠️ 需要添加8步流程说明
3. **实际代码实现** ❌ 尚未实现

---

## 📊 文档一致性对比

### ✅ 符合设计文档的部分

1. 使用STAGE_NODE日志类型 ✅
2. hub.set_stage()阶段切换 ✅
3. ai_log_process()流程管理 ✅

### ❌ 不符合设计文档的部分

1. **缺少_smart_cache_validation_and_sensing的8步详细流程** ❌
2. **缺少离线降级模式的完整逻辑** ❌
3. **monitor_system.py未添加STAGE_NODE标记** ❌
4. **没有CacheValidationWorker的Qt线程包装** ❌

---

## 🛠️ 实施计划

### P0 - 核心功能（必须完成）

- [ ] 1. 在`core_engine.py`中实现完整的`_smart_cache_validation_and_sensing()`方法（8步）
- [ ] 2. 为每一步添加STAGE_NODE日志标记
- [ ] 3. 实现离线降级逻辑（步骤1失败→跳过2-7→直接步骤8）
- [ ] 4. 在`start_async_fixed.py`中调用缓存验证流程

### P1 - 监控集成（优先）

- [ ] 5. 修改`monitor_system.py`添加STAGE_NODE标记
- [ ] 6. 添加AI日志流程包装
- [ ] 7. 阶段切换（monitor_init → monitoring）

### P2 - 完善优化（后续）

- [ ] 8. 创建`CacheValidationWorker` Qt线程包装器
- [ ] 9. 完善进度回调机制
- [ ] 10. 添加步骤完成回调
- [ ] 11. 更新所有相关文档

---

## 📝 下一步行动

**建议按以下顺序执行**：

1. **立即修正**`启动标准输出示例.md` ✅ **已完成**
2. **实现**`_smart_cache_validation_and_sensing()`完整8步流程
3. **集成**monitor_system.py的STAGE_NODE标记
4. **测试**实际启动输出是否符合标准示例
5. **更新**所有相关设计文档

---

## 🎯 验收标准

Terminal输出应该完全符合`启动标准输出示例.md`，包括：

1. ✅ 8步缓存验证流程详细展示
2. ✅ 每步的进度百分比
3. ✅ 离线降级模式的清晰标注
4. ✅ 监控进程的阶段性输出
5. ✅ 所有关键节点都有STAGE_NODE标记

---

**文档版本**: v2.0  
**更新时间**: 2025-11-02  
**状态**: ⚠️ 待完整实施  
**优先级**: P0（核心功能，必须完成）
