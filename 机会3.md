# -*- coding: utf-8 -*-
# C 扩展替代机会（第三批）

## 1. SharedMemoryManager 原生共享上下文

- **现状**：`SharedMemoryManager` 仍然依赖 `multiprocessing.Manager`/`manager.dict()` 来在进程间共享 `ValidationContext`。在数据进程启动阶段，每次调用 `prepare_shared_data()` 都要经过一次完整的对象拷贝与 pickle 序列化，随着 IPO 日期/交易日集合增长，这条路径会阻塞数据进程主线程。
```1123:1166:backend/infrastructure/data_module_vnpy/data_storage.py
        self.manager = Manager()
        self._shared_dict = self.manager.dict()
...
        self._shared_dict["ipo_dates"] = ipo_dates
        self._shared_dict["trading_days"] = trading_days
        self._shared_dict["latest_trading_day"] = latest_trading_day
        self._shared_dict["base_date"] = base_date
```
- **机会**：在 `native_memory` 内新增 “共享字典/集合” 原生句柄（基于 `CreateFileMapping` + `MapViewOfFile`），提供 `create_shared_map(name, payload)` 与 `attach_shared_map(name)` 接口；数据进程利用现有的 `ZeroCopyMemory` 把序列化后的 Arrow/MsgPack 缓冲写入共享区，子进程侧直接读取 memoryview。Python 包装层保持 `SharedMemoryManager` API 不变，仅在扩展不可用时降级到当前实现。
- **预期收益**：避免 `Manager` 带来的 GIL 争用与 `pickle` 成本，`prepare_shared_data()` 延迟可从百毫秒级降到个位数毫秒；同时与 `native_serialization`/`ZeroCopyMemory` 对齐，后续可在 C 层做原地增量更新。

## 2. TdxDataReader 多进程结果零拷贝

- **现状**：`fetch_batch_multiprocess()` 通过 `Manager.Queue()` 向主进程回传 `DataFrame`，每次结果都经由 `pickle` 序列化/反序列化 + pandas 重建，当前批量读取 800 档时 CPU 开销可达 1.5s。
```119:190:backend/infrastructure/tdx_asyncio/readers/data_reader.py
        manager = Manager()
        task_queue = manager.Queue()
        result_queue = manager.Queue()
...
        while completed < total_tasks:
            symbol, df = result_queue.get(timeout=1)
            results[symbol] = df
```
- **机会**：扩展 `native_ipc`，新增面向批量读取的 ring-buffer/双端队列，支持在 worker 侧直接发送 `build_dataframe_payload()` 生成的 Arrow buffer/零拷贝结构；主进程用 `memoryview` + `pyarrow.deserialize` 或 `native_serialization.deserialize_table` 取回数据。仍保留 Manager 队列作为兜底，但默认走原生通道。
- **预期收益**：消除大量 `pickle` + pandas 解析成本，批量读取总耗时可下降 40%-60%；同时减少多进程间的对象复制，降低峰值内存占用。

## 3. SystemMonitor 磁盘拓扑原生采集

- **现状**：`SystemMonitor.get_physical_disks_info()`、`get_disks_with_types()` 每次调用都要遍历 WMI + `psutil.disk_partitions()`，在多磁盘主机上 WMI 查询耗时 300-600ms，而且重复 I/O。
```5116:5218:backend/infrastructure/system_vnpy/monitor_system.py
            if system == "Windows" and HAS_WMI and HAS_PSUTIL:
                c = wmi.WMI()
                for disk in c.Win32_DiskDrive():
                    ...
                        for partition in disk.associators("Win32_DiskDriveToDiskPartition"):
                            for logical_disk in partition.associators("Win32_LogicalDiskToPartition"):
                                partitions.append(logical_disk.DeviceID)
...
            if not physical_disks and HAS_PSUTIL:
                partitions = psutil.disk_partitions()
                ...
```
- **机会**：基于 Win32 `IOCTL_STORAGE_QUERY_PROPERTY`、`DeviceIoControl`、`GetLogicalDriveStringsW` 在新的 `native_storage_inventory` 扩展中一次性拉取物理磁盘 → 分区映射及接口类型；并缓存结果（配合热插拔事件时刷新）。Linux 路径可使用 `libudev` 或直接读取 `/sys/block/*`。Python 侧仅在扩展失效时回退 WMI。
- **预期收益**：将磁盘拓扑采集延迟从几百毫秒降至 <5ms，避免监控线程被 WMI 阻塞；拓扑缓存也能让后续的 SMART/带宽指标复用相同的原生数据源，减少重复查询。


