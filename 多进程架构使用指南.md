# 多进程架构 - 使用指南

## 快速开始

### 1. 验证安装

确认依赖已安装：

```bash
pip show pyzmq msgpack
```

如果未安装：

```bash
pip install pyzmq msgpack
```

### 2. 运行测试

快速验证系统是否正常：

```bash
# 集成测试
python test_multiprocess_architecture.py

# 启动稳定性测试（10次）
python test_startup_stability.py 10

# 性能测试
python test_performance.py
```

预期输出：

```
✅ 所有测试通过！
崩溃率: 0.00%
```

### 3. 正常启动应用

多进程架构已自动集成到启动流程，无需手动操作：

```bash
# Windows
启动终端（增强版）.bat

# 或直接运行Python
python -m ui.main_window
```

---

## 系统架构说明

### 进程模型

```
主进程（UI + 业务服务）
  ├─ Qt主线程（UI渲染）
  ├─ 数据中心服务（同进程内存访问）
  ├─ 策略回测引擎（同进程内存访问）
  ├─ 交易网关服务（同进程内存访问）
  └─ IPC客户端（日志发送、告警订阅）
      │
      ↓ ZeroMQ通信
      │
日志/告警进程（独立隔离）
  ├─ 日志聚合（5557端口PULL）
  ├─ 日志存储（SQLite）
  ├─ 告警引擎（规则检查）
  └─ 告警推送（5558端口PUB）
```

### 端口分配

| 端口 | 协议 | 用途 |
|-----|------|------|
| 5557 | ZeroMQ PULL | 接收主进程的日志 |
| 5558 | ZeroMQ PUB | 推送告警到主进程 |
| 5559 | ZeroMQ REP | 健康检查响应 |

---

## 日志系统使用

### 基本用法

日志会自动通过IPC发送到日志进程：

```python
import logging

logger = logging.getLogger(__name__)

# 所有级别的日志都会自动发送到日志进程
logger.debug("调试信息")
logger.info("普通信息")
logger.warning("警告信息")
logger.error("错误信息")
logger.critical("严重错误")
```

### 查询日志

```python
from backend.services.system_manager_service import SystemManagerService

service = SystemManagerService()

# 查询最近100条ERROR日志
logs = service.query_logs(
    level="ERROR",
    limit=100
)

# 查询指定模块的日志
logs = service.query_logs(
    module="data_center_service",
    limit=100
)

# 查询时间范围内的日志
logs = service.query_logs(
    start_time="2025-10-15T00:00:00",
    end_time="2025-10-15T23:59:59",
    limit=1000
)
```

### 日志统计

```python
# 获取日志统计信息
stats = service.get_log_stats()

print(f"总日志数: {stats['total_count']}")
print(f"各级别统计: {stats['level_stats']}")
print(f"模块统计: {stats['module_stats']}")
```

---

## 告警系统使用

### 查看默认告警规则

系统已预配置3条告警规则：

1. **错误日志告警** - 监控所有ERROR和CRITICAL日志
2. **数据中心告警** - 监控数据中心模块的错误
3. **交易网关告警** - 监控交易网关模块的错误

### 添加自定义告警规则

```python
from backend.core.alert_system import LogAlertRule
from backend.core.utils import AlertSeverity

# 创建自定义规则
custom_rule = LogAlertRule(
    rule_id="my_custom_rule",
    name="我的自定义告警",
    log_levels=["ERROR", "CRITICAL"],
    modules=["my_module"],  # 监控指定模块
    keywords=["连接失败", "超时"],  # 关键字匹配
    severity=AlertSeverity.WARNING,
    enabled=True,
    priority=1,
    description="监控我的模块的错误",
    suppression_window=300,  # 5分钟抑制窗口
)

# 添加规则（需要在日志进程中）
# 注意：当前版本规则需要在日志进程启动时添加
```

### 查询告警历史

```python
from backend.services.system_manager_service import SystemManagerService

service = SystemManagerService()

# 查询最近100条告警
alerts = service.get_alert_history(limit=100)

# 查询指定级别的告警
alerts = service.get_alert_history(
    severity="CRITICAL",
    limit=100
)

# 查询时间范围内的告警
alerts = service.get_alert_history(
    start_time="2025-10-15T00:00:00",
    end_time="2025-10-15T23:59:59",
    limit=1000
)
```

### 确认/解决告警

```python
# 确认告警
service.acknowledge_alert(
    alert_id="alert_12345678",
    note="已收到告警，正在处理"
)

# 解决告警
service.resolve_alert(
    alert_id="alert_12345678",
    note="问题已解决"
)
```

---

## 进程管理

### 查看进程状态

```python
from backend.processes.process_manager import get_process_manager

pm = get_process_manager()

# 获取所有进程状态
status = pm.get_all_status()

print(f"日志/告警进程状态: {status['log_alert']}")
```

### 手动重启进程

```python
# 重启日志/告警进程
pm.restart_process("log_alert")
```

### 健康检查

```python
# 检查单个进程健康状态
health = pm.check_health("log_alert")

if health.get("healthy"):
    print(f"进程健康，运行时间: {health['uptime_seconds']}秒")
    print(f"统计信息: {health['stats']}")
else:
    print(f"进程不健康: {health.get('reason')}")
```

---

## 配置调优

### 日志批处理配置

编辑 `backend/processes/log_alert_process.py`：

```python
class LogAlertProcess:
    def __init__(self, ...):
        # 批处理大小（默认100）
        self.log_batch_size = 100
        
        # 批处理刷新间隔（默认1秒）
        # 在 _main_loop 中调整
```

建议值：
- 高负载：`log_batch_size = 200`，刷新间隔 `0.5秒`
- 低负载：`log_batch_size = 50`，刷新间隔 `2秒`

### 告警抑制配置

编辑告警规则的 `suppression_window` 参数：

```python
LogAlertRule(
    suppression_window=300,  # 秒
    # 300秒 = 5分钟（默认）
    # 600秒 = 10分钟（更长抑制）
    # 60秒 = 1分钟（更频繁告警）
)
```

### 进程重启策略

编辑 `backend/processes/process_manager.py`：

```python
class ManagedProcess:
    def __init__(self, ...):
        # 最大重启次数（默认5）
        self.max_restarts = 5
        
        # 重启延迟基数（默认1秒）
        # 实际延迟 = base * (2 ** restart_count)
        # 1s, 2s, 4s, 8s, 16s
        self.restart_delay_base = 1.0
```

### 健康检查间隔

编辑 `backend/processes/process_manager.py`：

```python
def monitor_all(self):
    def monitor_loop():
        while self.monitoring_enabled:
            # 检查所有进程健康状态
            ...
            time.sleep(5)  # 默认5秒，可调整
```

---

## 故障排查

### 问题1：进程启动失败

**症状**：
```
[ProcessManager] 进程 log_alert 启动超时
```

**排查步骤**：

1. 检查端口是否被占用：
```bash
netstat -an | findstr "5557 5558 5559"
```

2. 检查日志输出：
```bash
# 查看进程输出
cat logs/fallback/fallback.log
```

3. 手动启动进程测试：
```bash
python -c "from backend.processes import log_alert_process; log_alert_process.main()"
```

**解决方案**：
- 如果端口被占用，修改端口配置
- 如果是依赖问题，检查pyzmq和msgpack是否安装

---

### 问题2：日志未发送到日志进程

**症状**：
- 日志只在控制台显示
- 日志数据库无新记录

**排查步骤**：

1. 检查进程是否运行：
```python
from backend.processes.process_manager import get_process_manager
pm = get_process_manager()
health = pm.check_health("log_alert")
print(health)
```

2. 检查分布式日志处理器：
```python
import logging
handlers = logging.root.handlers
print(f"日志处理器: {[type(h).__name__ for h in handlers]}")
```

3. 检查降级日志：
```bash
cat logs/fallback/fallback.log
```

**解决方案**：
- 如果进程未运行，手动启动
- 如果IPC连接失败，检查网络/防火墙
- 降级日志可在进程恢复后同步

---

### 问题3：告警未推送到UI

**症状**：
- 日志进程检测到告警
- UI未显示告警

**排查步骤**：

1. 检查告警订阅器：
```python
from backend.processes.ipc_client import get_alert_subscriber
subscriber = get_alert_subscriber()
print(f"订阅器运行: {subscriber.running}")
```

2. 检查Qt信号连接：
```python
# 在MainWindow中
print(f"告警信号连接数: {subscriber.alert_received.receivers()}")
```

**解决方案**：
- 启动告警订阅器：`subscriber.start()`
- 连接Qt信号到UI更新方法

---

### 问题4：内存占用过高

**症状**：
- 长时间运行后内存持续增长

**排查步骤**：

1. 检查日志批处理队列：
```python
# 在 log_alert_process 中
print(f"批处理队列大小: {len(self.log_batch)}")
```

2. 检查数据库大小：
```bash
dir data\logs.db
```

**解决方案**：
- 清理旧日志：
```python
from backend.core.logging_system import LogDatabase
db = LogDatabase()
deleted = db.cleanup_old_logs(retention_days=30)
print(f"清理了{deleted}条旧日志")
```

- 数据库压缩：
```python
import sqlite3
conn = sqlite3.connect("data/logs.db")
conn.execute("VACUUM")
conn.close()
```

---

## 监控指标

### 关键指标

建议监控以下指标（可集成到系统管理界面）：

```python
from backend.processes.process_manager import get_process_manager

pm = get_process_manager()
status = pm.get_all_status()

# 1. 进程健康状态
print(f"进程状态: {status['log_alert']['state']}")

# 2. 运行时间
print(f"运行时间: {status['log_alert']['uptime_seconds']}秒")

# 3. 统计信息
stats = status['log_alert']['stats']
print(f"已接收日志: {stats['logs_received']}")
print(f"已触发告警: {stats['alerts_triggered']}")
print(f"健康检查次数: {stats['health_checks']}")
print(f"错误次数: {stats['errors']}")
```

### 性能指标

```python
import psutil
import os

# 获取进程对象
pid = status['log_alert']['pid']
process = psutil.Process(pid)

# CPU占用
print(f"CPU占用: {process.cpu_percent()}%")

# 内存占用
mem = process.memory_info().rss / 1024 / 1024  # MB
print(f"内存占用: {mem:.1f}MB")

# 线程数
print(f"线程数: {process.num_threads()}")
```

---

## 最佳实践

### 1. 日志级别使用

```python
# ✅ 正确使用
logger.debug("详细的调试信息")  # 开发环境
logger.info("业务流程信息")     # 正常运行
logger.warning("警告但不影响运行")  # 需要注意
logger.error("错误但可恢复")     # 触发告警
logger.critical("严重错误需立即处理")  # 高优先级告警

# ❌ 错误使用
logger.error("用户点击了按钮")  # 不应该用ERROR
logger.info("数据库连接失败")   # 应该用ERROR
```

### 2. 告警规则设计

```python
# ✅ 好的规则
LogAlertRule(
    rule_id="database_error",
    name="数据库错误告警",
    log_levels=["ERROR", "CRITICAL"],
    modules=["database"],
    keywords=["连接失败", "查询超时"],
    suppression_window=300,  # 避免告警风暴
)

# ❌ 不好的规则
LogAlertRule(
    rule_id="all_logs",
    name="所有日志告警",
    log_levels=["DEBUG", "INFO", "WARNING", "ERROR"],  # 太宽泛
    suppression_window=10,  # 太短，会产生大量告警
)
```

### 3. 资源管理

```python
# 定期清理旧日志（建议每天执行）
from backend.core.logging_system import LogDatabase

db = LogDatabase()
deleted = db.cleanup_old_logs(retention_days=30)
logger.info(f"清理了{deleted}条旧日志")

# 监控数据库大小
import os
db_size = os.path.getsize("data/logs.db") / 1024 / 1024  # MB
if db_size > 1000:  # 超过1GB
    logger.warning(f"日志数据库过大: {db_size:.1f}MB")
```

---

## FAQ

### Q1: 多进程架构会影响性能吗？

**A**: 不会。性能测试显示：
- 日志发送延迟 < 1ms（远优于10ms目标）
- 数据密集型服务（数据中心、策略回测）仍在主进程中，保持最快速度
- ZeroMQ IPC通信开销极小（微秒级）

### Q2: 如果日志进程崩溃怎么办？

**A**: 系统有多重保护：
1. 自动重启机制（最多5次）
2. 降级到本地文件（`logs/fallback/fallback.log`）
3. 进程恢复后自动同步降级日志
4. 主进程继续正常运行

### Q3: 可以添加更多独立进程吗？

**A**: 可以，但需谨慎：
- ✅ 适合隔离：网络密集型服务（AI助手）
- ❌ 不适合隔离：数据密集型服务（影响性能）
- 参考设计文档的"改造范围"章节

### Q4: 如何在生产环境部署？

**A**: 建议步骤：
1. 先在测试环境运行24小时
2. 监控关键指标（内存、CPU、崩溃率）
3. 调优配置参数
4. 灰度发布（部分用户先体验）
5. 全量发布

### Q5: Windows和Linux有区别吗？

**A**: 核心功能无区别，但：
- Windows进程重启有已知限制（不影响主要功能）
- Linux性能略优（进程fork更高效）
- 配置和使用方法完全相同

---

## 技术支持

### 日志文件位置

```
data/logs.db          - 日志数据库
data/alerts.db        - 告警数据库
logs/fallback/        - 降级日志目录
```

### 调试模式

开启详细日志输出：

```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

### 问题反馈

如遇问题，请提供：
1. 错误日志（`logs/fallback/fallback.log`）
2. 进程状态（`pm.get_all_status()`）
3. 系统环境（OS版本、Python版本）
4. 复现步骤

---

**文档版本**：1.0  
**更新日期**：2025-10-15  
**适用版本**：terminal_v0.50+
