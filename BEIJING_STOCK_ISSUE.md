# 北交所股票数据问题说明

## 🔍 问题

北交所股票（8/9/4开头）无法获取数据

## 测试结果

```
市场代码测试：
- 0（深交所）：✅ 成功
- 1（上交所）：✅ 成功  
- 2（北交所）：❌ 失败（返回空）
```

## 🎯 解决方案

### 方案1：跳过北交所股票（临时）

由于mootdx服务器不支持北交所数据，暂时跳过这些品种。

**修改建议**：
在engine.py中过滤北交所股票：
```python
# 过滤掉北交所股票（暂不支持）
stocks = [s for s in stocks if not s.startswith(('8', '9', '4'))]
```

### 方案2：使用深交所市场代码（尝试）

北交所股票可能需要用深交所市场代码（0）访问：

```python
# 北交所尝试用深交所代码
if symbol.startswith(("8", "9", "4")):
    market = 0  # 尝试用深交所
```

### 方案3：完全跳过（推荐）

在market_types中默认不包含北证A股：

```python
market_types = ["上证A股", "深证A股", "T+0基金", "含可转债"]  # 不包含北证A股
```

## 建议

**当前最优方案**：在获取市场品种列表时，默认不包含北证A股

**原因**：
1. mootdx服务器不提供北交所数据
2. 市场代码2不work
3. 北交所股票数量少，影响不大
4. 避免大量空数据日志

## 实施

修改默认market_types配置，不包含"北证A股"

