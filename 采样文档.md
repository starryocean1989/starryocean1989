## 采样方案概述

为确认 TDX 批量读取链路的真实瓶颈，需要在 CPU、磁盘 I/O、进程通信与业务处理环节追加采样埋点。以下计划以“最小侵入 + 可重复”为原则，可逐步验证每一块是否为短板。

---

### 1. 磁盘 I/O 指标

| 目标 | 采样方式 | 实施位置 | 备注 |
|------|----------|----------|------|
| Busy 百分比、IOPS、吞吐 | 使用 `psutil.disk_io_counters(perdisk=True)` 记录 `read_bytes/write_bytes`、`read_time/write_time`、`busy_time`，计算：<br/>- `busy_ratio = Δbusy_time / Δt`<br/>- `iops = Δ(read_count+write_count) / Δt`<br/>- `throughput = Δ(read_bytes+write_bytes) / Δt` | `SystemMetricsMonitor._read_direct_metrics` 增补采样窗口 | 若 Busy 持续趋近 100% 或 IOPS 达到硬盘极限，则磁盘即为短板。 |
| 写入耗时 | 在 `StorageManager.save_kline` 前后记录 `start/end` 时间 | `backend/infrastructure/data_module_vnpy/local_data/data_quality.py` 里的存储实现 | 对比磁盘 Busy 与单次写入耗时，区分 I/O 还是合并/序列化开销。 |

---

### 2. 协程执行 vs 等待占比

| 目标 | 采样方式 | 实施位置 | 备注 |
|------|----------|----------|------|
| 协程实际计算时间 | 在 `_tdx_worker_async.process_symbol` 内部，分别记录：<br/>- `读取`：`await reader.read` 前后<br/>- `标准化`：`reader.standardize` 调用<br/>- `保存`：`await loop.run_in_executor`（含前后等待）<br/>将耗时写入 `result_queue` 或调试日志 | `backend/.../data_readers/data_readers.py` | 统计“等待 + 真正执行”占比，确认等待是否远大于 CPU 处理。 |
| 运行摘要 | 已在 `TdxDynamicExecutor` 中记录 `duration`、`avg_symbols_per_sec`、动作统计，可扩展加入“平均读取/写入耗时” | `TdxDynamicExecutor.execute_batch` | 便于压测后统一收集。 |

---

### 3. 进程间通信压力

| 目标 | 采样方式 | 实施位置 | 备注 |
|------|----------|----------|------|
| 队列积压 | 监控 `Manager().Queue()` 的长度：<br/>- `result_queue.qsize()`<br/>- `config_queue.qsize()` | `_tdx_worker_process` 或主进程的 `_adjustment_monitor` | 若积压显著增长，说明进程间通信成为瓶颈。 |
| 队列操作耗时 | 包装 `put/get`，记录阻塞时间 | 同上 | 负载高时若耗时明显上升，需考虑换用本地共享内存或调大队列容量。 |

---

### 4. 系统层辅助工具

- Windows：`Resource Monitor`、`WPR/WPA` 捕捉磁盘延迟、线程状态。
- Linux：`perf top`, `iostat`, `pidstat`。
- Python：`asyncio` 的任务追踪或手动统计 Await 时间。

---

### 5. 采样步骤建议

1. **基线压测**：使用现有脚本 `scripts/run_tdx_concurrency_test.py` 运行 8 进程、16 进程两种场景，保留修改前的数据。
2. **逐项开启采样**：
   - 先开启磁盘 Busy/IOPS 记录；
   - 再加入读取/写入耗时日志；
   - 最后观察队列积压与协程等待占比。
3. **对比分析**：通过 CSV 或日志对每个阶段的耗时/利用率进行可视化，判断真正的短板。
4. **迭代优化**：针对短板调整策略，如增大进程数、降低批大小、改写存储策略或并行化转换逻辑。

---

### 6. 输出与复盘

采样完成后，将关键指标（CPU、内存、Busy%、IOPS、单次读/写耗时、队列长度、协程等待占比）整理为表格，再与吞吐、完成时间进行关联分析；必要时可附上 `hardware_limit`、`LoadBalancer` 的 raise/hold/reject 动作曲线，帮助快速定位瓶颈。
