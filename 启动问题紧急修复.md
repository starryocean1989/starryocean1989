# 启动卡住问题紧急修复

## 问题分析

根据启动日志，系统在以下位置卡住：

```
[DEBUG] 创建连接失败告警规则...
```

之后没有任何输出，说明在创建该规则时发生了阻塞。

## 根本原因

1. **告警系统初始化阻塞**
   - 创建连接失败告警规则时可能触发了某些同步操作
   - 可能是数据库写入、日志系统调用等

2. **后端初始化中的健康检查**
   - 虽然已经注释掉，但可能还有其他地方在调用

## 紧急修复方案

### 方案1：延迟告警规则创建（推荐）

将默认告警规则的创建延迟到系统完全启动后：

```python
# 在 backend/core/alert_system.py 中修改
def create_default_log_alert_rules():
    """创建默认的日志告警规则（异步执行）."""
    import threading
    
    def _create_rules_async():
        try:
            print(f"[DEBUG] 后台创建默认日志告警规则...")
            time.sleep(1)  # 等待系统初始化
            # ... 原有规则创建代码 ...
        except Exception as e:
            print(f"[DEBUG] 后台创建规则失败: {e}")
    
    # 在后台线程中创建
    thread = threading.Thread(target=_create_rules_async, daemon=True)
    thread.start()
```

### 方案2：临时禁用告警规则创建

在`backend/services/system_manager_service.py`中查找告警初始化代码，临时注释掉：

```python
# 临时禁用默认告警规则创建
# create_default_log_alert_rules()
```

### 方案3：简化告警规则

只创建最基本的ERROR级别规则，删除其他规则：

```python
def create_default_log_alert_rules():
    """创建默认的日志告警规则（简化版）."""
    try:
        alert_engine = AlertEngine()
        
        # 只创建ERROR级别规则
        error_rule = LogAlertRule(
            rule_id="log_error_monitoring",
            name="ERROR级别日志监控",
            log_levels=["ERROR"],
            severity=AlertSeverity.ERROR,
            description="监控所有ERROR级别日志记录",
        )
        alert_engine.add_rule(error_rule)
        
    except Exception as e:
        print(f"[DEBUG] 创建告警规则失败: {e}")
```

## 立即执行的修复

我将实施方案1（异步创建告警规则），这是最安全的方案。
