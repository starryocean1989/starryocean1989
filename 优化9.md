<!-- -*- coding: utf-8 -*- -->
# 优化9：继续推进原生扩展替换

## 背景概览
- 三进程架构已完成第一轮 C 扩展替换，但仍存在 Python 热点会在 5000+ 品种、4 周期的大规模扫描下暴露 CPU 瓶颈。
- 以下建议聚焦在仍由 Python 循环承担的热点路径，兼顾可复用性与落地成本，全部保持可降级策略。

```4973:4974:data_module_vnpy新架构业务细节文档.md
- 高效扫描海量数据文件(5000+品种 × 4周期)
```

## 建议 1：历史逐笔成交解析的前缀和下沉 native_compute
- **现状**：`AsyncGetHistoryTransactionData.parseResponse` 在 Python 中做价格差值前缀求和，随后再交给 `batch_compute` 做缩放，整段逻辑在超大成交量场景下成为热点。

```82:102:backend/infrastructure/tdx_asyncio/parsers/std/history_transaction.py
        last_price = 0
        cumulative_prices = []
        for price_raw in price_raws:
            last_price = last_price + price_raw
            cumulative_prices.append(last_price)

        if len(cumulative_prices) > 0:
            prices = batch_compute(cumulative_prices, "divide")  # type: ignore
```

- **优化思路**：在 `native_compute` 中新增 `prefix_sum_scale(int32* diffs, double scale)`，一次性计算前缀和并完成缩放，返回 Python list。接口沿用批量 API 风格，支持 `divide_by_100`, `divide_by_1000` 等枚举以保持与现有调用兼容。
- **实施要点**：
  - 新增 C 函数及 Python 绑定，沿用现有错误处理和可用性标志。
  - 在解析器中引入 feature flag，保留 Python 退化实现，用于未编译扩展或回退调试。
- **验证计划**：
  - `native_compute` 单元测试新增大样本 (≥100k) 随机差值向量基准，对比 Python 结果。
  - `test_native_finance_ops_standalone.py` 或新增解析基准，验证 99p latency。

## 建议 2：品种过滤器链迁移至原生批处理
- **现状**：过滤链当前依赖纯 Python 循环完成重复品种去重、空字段剔除。一次刷新需多次遍历 `list[dict]`，在 5000+ 品种时形成 CPU hotspot。

```999:1023:backend/infrastructure/data_module_vnpy/data_acquisition.py
        seen_codes = set()
        results = []

        for symbol in symbols:
            code = symbol.get("code")
            if code is None:
                continue

            if code not in seen_codes:
                seen_codes.add(code)
                results.append(symbol)
```

```1043:1069:backend/infrastructure/data_module_vnpy/data_acquisition.py
        results = []

        for symbol in symbols:
            code = symbol.get("code")
            name = symbol.get("name")

            if not code or code.strip() == "":
                continue

            if self.check_name and (not name or name.strip() == ""):
                continue

            results.append(symbol)
```

- **优化思路**：
  - 新增 `native_dataframe_ops.filter_symbols(records, options)`，基于 struct array 一次性完成去重、空字段、IPO 日期阈值等规则。
  - 或新建 `native_symbol_filters`，直接消费 `list[Mapping]`，内部使用 `PyUnicodeObject` 缓存、开放布尔开关，复用 `MemoryPool` 减少临时对象。
- **集成步骤**：
  - FilterChain 中增加能力检测，优先调用原生批处理，失败时回退 Python。
  - 迁移现有埋点，确保过滤前后统计仍可输出。
- **预期收益**：在 5000×N（分类数量）场景下降低 40%+ CPU，用于刷新符号和下载准备阶段。

## 建议 3：SymbolLoader 查询链接入 LockFreeHashMap
- **现状**：`extract_all_codes` / `extract_codes_by_market` / `get_symbol_info` 每次都做 O(n) 遍历，频繁被 UI 与数据流程调用。

```2226:2296:backend/infrastructure/data_module_vnpy/data_acquisition.py
        all_codes = set()
        for symbols in self.classified_symbols.values():
            for symbol in symbols:
                code = symbol.get("code")
                if code:
                    all_codes.add(code)
        ...
        for symbols in self.classified_symbols.values():
            for symbol in symbols:
                if symbol.get("code") == code:
                    return symbol
```

- **优化思路**：在刷新分类后构建 `native_gil.LockFreeHashMap` 索引：
  - `code -> symbol dict`、`market -> vector<code>` 等映射一次性落地。
  - 暴露 `get_symbol_info_native`, `get_codes_by_market_native`，支持并发读并复用原有缓存生命周期。
- **实施注意**：
  - 初始化阶段在写锁内构建索引，完成后仅读；若扩展不可用则保持现有 set/list 逻辑。
  - 结合 `HighPerfLRUCache` 缓存最近查询，避免 UI 高频命中 Python dict。
- **验证**：通过 `tests/test_batch_performance.py` 加入查询基准，确保并发查询在多线程下无锁争用。

## 建议 4：IPO 日期批处理结果聚合改造为原生 reducer
- **现状**：异步下载 IPO 日期后用 Python 遍历 `task_results` 统计成功率、回填结果字典，CPU 时间占据整体流程 20% 以上（log 已埋 `result_processing_elapsed`）。

```4997:5055:backend/infrastructure/data_module_vnpy/data_acquisition.py
            for i, result in enumerate(task_results):
                if isinstance(result, Exception):
                    ...
                if isinstance(result, tuple) and len(result) == 2:
                    sym, ipo_date = result
                    results[sym] = ipo_date
                    ...
            result_processing_elapsed = (time.time() - result_processing_start) * 1000
            ...
            logger.info(
                f"[IPO-DOWNLOAD-ASYNC-性能埋点] 完整统计: "
                f"总耗时={total_elapsed:.1f}ms, "
                f"结果处理耗时={result_processing_elapsed:.1f}ms, "
            )
```

- **优化方案**：开发 `native_async.reduce_task_results`（可放入 `native_ipc` 或独立模块），接受 `PyObject* tasks`，内部使用结构化遍历：
  - 直接生成 `dict` / `vector`，同时统计成功、失败、None 计数。
  - 提供循环过程中的回调钩子（用于进度日志），兼容 Python 回调不可抛异常的约束。
- **风险与回退**：
  - 需处理 `Exception` 类型引用计数，保证在 C 层也能安全传回。
  - 原函数保留 Python 版作为 fallback；通过 feature flag 控制逐步灰度。
- **验证**：编写基准，对比 5k/10k 任务下的处理耗时，确保提升明显；保持现有日志语义。

## 收尾计划
- 以上四项按模块拆分提交 MR，每个 MR 附基准脚本与压测截图。
- 建议优先落地建议 1 & 2（对前台延迟影响最大），随后处理索引与 IPO reducer。
- 编译新的扩展后，记得更新 `test_all_native_extensions.py` 白名单并在 CI 中串联。


