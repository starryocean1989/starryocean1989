模块名称 data_module_vnpy

该模块需要接入vnpy架构。
C:\Users\USER\Desktop\data_module_vnpy\venv310\Lib\site-packages\vnpy
该模块需要调用mootdx包的方法。
C:\Users\USER\Desktop\data_module_vnpy\venv310\Lib\site-packages\mootdx
该模块配置驱动。
该模块是一个量化交易项目后端的本地数据功能部分，前端通过配置界面，修改后端配置文件来修改保存地址等内容。

模块方法
1.品种列表获取。当该方法执行时，调用mootdx模块的stock_all()来请求所有品种的列表，stock_all() 返回一个包含股票基本信息的复合数据类型，包含以下字段：市场代码，0或者1，数值，代表上证品种或者深证品种。股票代码，数值“600000”或者“000001”，返回的品种不只有股票还有基金债券。通过市场代码0，品种代码688，60开头的6位数代码品种可以找到所有上证A股，通过市场代码1，品种代码000，001，002，300，301开头的6位数代码品种可以找到所有深证A股。通过电脑上下载的通达信金融终端，在该软件根目录new_tdx文件夹内可以扫描到spblock.dat这个文件，其中GKB编码了中文字符，ASCII编码了品种代码。7位数字编码0开头是上证A股，1开头是深证A股，2开头是北证A股，所以2+9开头的那些品种就是所有北证A股。除此之外，spblock.dat内还有T+0基金分类的品种，含可转债分类的品种。按照以上方法，品种列表获取可以获得上证A股，深证A股，北证A股，T+0基金，含可转债5类品种的6位数代码列表，并把该列表缓存到本地，直到下次执行该方法时覆盖。缓存地址写入配置文件。品种列表获取还有一个隐藏方法：按照配置好的通达信软件根目录扫描spblock.dat文件并解码读取。

2.K线数据全量下载。当该方法执行时，调用模块方法1生成的缓存品种列表和mootdx模块的get_k_data() - 轮询该列表品种的过去的K线数据，日线指定参数8000根K线，5min和1min指定参数20000根K线。
K线数据增量下载。当该方法执行时，调用模块方法1生成的缓存品种列表和mootdx模块的bar() ,按用户给定的日期参数t，下载t日至今的日K线，5min和1minK线数据。

3.保存数据到本地：用二进制列式压缩格式存储，存储位置写入配置文件，与K线数据全量下载方法和K线数据增量下载方法配合使用。筛选读取数据方法：筛选读取指定品种，指定周期，指定时间段的数据。

4.数据感知。感知本地数据品种缺失：按照最新的品种列表缓存对标进行感知。感知本地数据历史数据缺失：把历史的某一日设为基日，对基日至今的数据缺失进行感知，基日保存于配置文件。
感知逻辑错误：比如某天的高价竟然比低价还低，这明显不对。
感知数据格式错误：确保价格、成交量等数据都是数字，不是乱码。
实时感知：一旦数据文件被修改、新增或删除，系统自动重新检查数据质量。
实时反馈：数据有问题时，系统感知到会立即告知。
缓存检查结果，直到下次更新。
增量检查机制，多线程/多进程处理。

前端通过vnpy架构调用data_module_vnpy包，前端点击刷新品种时会通过vnpy架构向data_module_vnpy请求已经保存到本地的品种列表缓存（不调用api更新缓存）。

前端点击重新加载品种时会向data_module_vnpy发送指令调用品种列表获取方法更新缓存，然后前端读取该最新缓存。

前端点击全量下载时，通过vnpy架构调用data_module_vnpy包执行.K线数据全量下载方法。

前端配置日期并点击增量下载时，通过vnpy架构调用data_module_vnpy包执行K线数据增量下载方法。

前端通过选择标的，时间段，K线周期通过vnpy架构调用data_module_vnpy包执行筛选读取数据方法。

后端数据感知要立即反馈前端。

前端可以通过配置界面更改品种列表缓存位置，K线下载保存位置，通达信软件根目录位置，数据感知基日等内容。

