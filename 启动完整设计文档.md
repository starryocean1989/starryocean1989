# 启动完整设计文档

## 📋 文档说明

**用途**: 完整的应用启动设计文档，包含标准输出示例、Log输出设计、底层代码设计和降级机制设计
**更新时间**: 2025-11-02
**对应版本**: v0.50
**日志系统**: unified_log_system.py v5.0

**文档内容**:
1. **Terminal标准输出**: 展示应用正常启动时Terminal的标准输出格式
2. **Log输出设计**: 基于STAGE_NODE日志类型的分阶段日志输出设计
3. **底层代码设计**: 启动流程的核心架构、类和时序设计
4. **TDX离线降级机制设计**: 当TDX服务器不可用时的自动降级机制

---

## 🎯 Terminal标准输出

```
C:\Users\USER\Desktop\terminal_v0.50>python start_async_fixed.py

======================================================================
【阶段0: 环境准备】 (0-5%)
======================================================================

📍 阶段0: 环境准备开始
✅ 环境准备完成 (85ms)

======================================================================
【阶段1: 日志系统初始化】 (5-10%)
======================================================================

📍 阶段1: 日志系统初始化开始
✅ LoggingHub创建完成
✅ 路由规则引擎初始化完成
  - 全局规则: 7 个LogType
  - 阶段规则: 8 个阶段
  - 模块规则: 8 个模块
  - 场景规则: 10 个场景
✅ AI日志Handler初始化完成
  - 基础目录: C:\Users\USER\Desktop\terminal_v0.50\logs\ai
✅ MemoryHandler日志重放完成 (1250条)
✅ 日志系统就绪 (180ms)

======================================================================
【阶段2: Qt应用框架】 (10-20%)
======================================================================

📍 阶段2: Qt应用框架开始
✅ QApplication创建完成
✅ EventEngine预创建完成
✅ 主题系统加载完成
  - 当前主题: modern_dark
  - 主题配置: C:\Users\USER\Desktop\terminal_v0.50\ui\components\themes.json
✅ 启动画面显示
✅ Qt框架就绪 (450ms)

===================================================================
【阶段3: 后端服务初始化】 (20-90%) - 并行执行
======================================================================

📍 阶段3: 后端服务初始化开始

┌──────────────────────────────────────────────────────────────────┐
│ 分支A: 监控进程                                                   │
└──────────────────────────────────────────────────────────────────┘

📍 监控进程启动开始
✅ monitor_system.py进程已启动 (PID: 12345)
✅ 创建native_ipc管道
  ├─ monitor_alerts ✅
  ├─ monitor_status ✅
  └─ monitor_query ✅
✅ 监控组件初始化
  ├─ SystemMonitor ✅
  ├─ ProcessMonitor ✅
  ├─ HardwareMonitor (后台异步) ⏳
  └─ BandwidthMonitor ✅
✅ Level 1就绪 (管道就绪)
✅ 监控进程看门狗启动
✅ 监控进程完全就绪 (2.3s)

┌──────────────────────────────────────────────────────────────────┐
│ 分支B: 数据引擎初始化（smart_cache_validation_and_sensing）      │
└──────────────────────────────────────────────────────────────────┘

📍 阶段3.1: ChinaStockEngine初始化开始
✅ ChinaStockEngine实例化完成

📍 开始缓存验证与感知流程（8步）

┌─ 步骤1: 服务器池验证与测速 ─┐
│ ⏳ 正在测速IPv4服务器池...（20个服务器）
│ ✅ IPv4服务器池可用: 18个可用，平均延迟 85ms
│ ⏳ 正在测速IPv6服务器池...（5个服务器）
│ ✅ IPv6服务器池可用: 3个可用，平均延迟 120ms
│ ✅ LoadBalancer初始化完成
│   - 任务分类体系: NETWORK_DOWNLOAD, LOCAL_SCAN, LOCAL_READ
│   - 队列压力监控: 正常/高/临界三级
└─────────────────────────────┘
✅ 步骤1完成 (850ms) [进度: 12%]

┌─ 步骤2: 获取当前日期（网络时间）─┐
│ ⏳ 正在同步网络时间...
│ ✅ 网络时间同步成功: 2025-11-02
└─────────────────────────────┘
✅ 步骤2完成 (180ms) [进度: 25%]

┌─ 步骤3: 验证交易日历缓存 ─┐
│ ⏳ 检查交易日历缓存...
│ ✅ 交易日历缓存有效（最新日期: 2025-11-01）
│ ℹ️  今日是交易日，市场开市中
└─────────────────────────────┘
✅ 步骤3完成 (120ms) [进度: 37%]

┌─ 步骤4: 验证品种列表缓存 ─┐
│ ⏳ 检查品种列表缓存...
│ ℹ️  缓存时间: 2025-11-01 16:30:00（未过期）
│ ✅ 品种列表缓存有效
│ ✅ SymbolLoader初始化完成
│   - 上证A股: 2100
│   - 深证A股: 2800
│   - 北证A股: 200
│   - T+0基金: 50
│   - 可转债: 50
│   - 总计: 5200品种
└─────────────────────────────┘
✅ 步骤4完成 (450ms) [进度: 50%]

┌─ 步骤5: 验证IPO日期缓存 ─┐
│ ⏳ 检查IPO日期缓存...
│ ✅ IPO日期缓存有效（5200品种）
│ ℹ️  待上市品种: 15个（已过滤）
└─────────────────────────────┘
✅ 步骤5完成 (200ms) [进度: 62%]

┌─ 步骤6: 更新本地数据索引 ─┐
│ ⏳ 扫描本地数据文件...
│ ✅ StorageManager初始化完成
│ ⏳ 本地数据索引扫描中...（5200品种 × 2周期）
│ ✅ 本地数据索引更新完成
│   - 有效品种: 5198
│   - 无效品种: 2（已标记）
│   - 总数据文件: 10400个
│   - 数据总量: 125GB
└─────────────────────────────┘
✅ 步骤6完成 (2.5s) [进度: 75%]

┌─ 步骤7: 检查数据更新状态 ─┐
│ ⏳ 检查数据新鲜度...
│ ✅ DataSensor初始化完成
│ ℹ️  数据新鲜度分析:
│   - 最新数据: 1250品种（今日）
│   - 1天前: 2800品种
│   - 2-7天前: 1148品种
│   - 需要更新: 0品种
│ ✅ UnifiedDataManager初始化完成
│   - 四层数据融合已启用
│   - 预加载缓存: 64品种
└─────────────────────────────┘
✅ 步骤7完成 (800ms) [进度: 87%]

┌─ 步骤8: 启动文件监控 ─┐
│ ⏳ 启动DataFileWatcher...
│ ✅ 文件监控器已启动
│   - 监控目录: C:\Users\USER\Desktop\terminal_v0.50\data\kline
│   - 监控模式: 实时变更检测
└─────────────────────────────┘
✅ 步骤8完成 (100ms) [进度: 100%]

✅ 缓存验证与感知流程完成 (总耗时: 5.2s)
✅ 数据引擎完全就绪

┌──────────────────────────────────────────────────────────────────┐
│ 分支C: 业务服务初始化                                            │
└──────────────────────────────────────────────────────────────────┘

📍 阶段3.4: 交易服务初始化开始
✅ TradingGatewayService初始化完成
✅ 网关配置加载完成
  - 可用网关类型: CTP, MINI, SOPT, TTS, IB, PAPERACCOUNT
✅ 风控引擎准备完成
✅ 交易服务就绪

📍 阶段3.5: 策略服务初始化开始
✅ StrategyCenterService初始化完成
✅ AIAssistantService初始化完成
  - AI模型: DeepSeek
  - API状态: 可用
✅ 策略模板加载完成
  - CTA策略: 1个模板
  - 算法交易: 1个模板
  - 组合策略: 1个模板
  - 期权策略: 1个模板
  - 价差策略: 1个模板
  - 脚本交易: 1个模板
✅ 策略服务就绪

📍 阶段3.6: 辅助服务初始化开始
✅ PortfolioService初始化完成
✅ MarketBoardService初始化完成
✅ SystemManagerService初始化完成
  └─ 连接监控进程native_ipc管道 ✅
✅ 服务健康检查通过
  - 数据中心服务: 运行中
  - 交易网关服务: 运行中
  - 策略中心服务: 运行中
  - AI助手服务: 运行中
  - 组合投资服务: 运行中
  - 行情看板服务: 运行中
  - 系统管理服务: 运行中
✅ 辅助服务就绪

======================================================================
【阶段4: UI主窗口】 (90-100%)
======================================================================

📍 阶段4: UI主窗口创建开始
✅ MainWindow创建完成
✅ 六大功能模块注册完成
  ├─ DataCenterView ✅
  ├─ MarketBoardView ✅
  ├─ TradingGatewayView ✅
  ├─ PortfolioView ✅
  ├─ StrategyCenterView ✅
  └─ SystemManagerView ✅
✅ 快捷键系统注册完成
  - 全局快捷键: 15个
✅ 增强状态栏初始化完成
✅ 主窗口显示
✅ 启动画面关闭
✅ UI就绪 (1.8s)

======================================================================
🎉 星辰金融终端启动成功！
======================================================================

启动统计:
  - 总耗时: 5.2s
  - 环境准备: 85ms
  - 日志系统: 180ms
  - Qt框架: 450ms
  - 监控进程: 2.3s (并行)
  - 六阶段服务: 0.3s (并行)
  - UI主窗口: 1.8s

服务状态:
  - 后端服务: 7个运行中
  - 监控进程: 运行中 (PID: 12345)
  - native_ipc管道: 3条正常

系统资源:
  - CPU使用率: 5.2%
  - 内存占用: 420MB
  - 线程数: 25

======================================================================
应用已就绪，等待用户操作...
======================================================================
```

---

## 📊 输出特点说明

### 1. 分阶段展示
- 使用分隔线清晰区分不同阶段
- 每个阶段显示进度百分比
- 阶段完成后显示耗时统计

### 2. 层级结构
- 主阶段使用 `【】` 包裹
- 子阶段使用 `📍` 标记开始，`✅` 标记完成
- 详细信息使用缩进和 `├─`、`└─` 展示树形结构

### 3. 视觉元素
- ✅ 成功标记
- ⏳ 后台任务标记
- 📍 阶段节点标记
- 🎉 完成标记
- ├─、└─ 树形结构

### 4. 信息密度
- Terminal输出：仅显示关键节点和统计信息
- AI日志文件：包含完整的DEBUG级别日志（位于 `logs/ai/application_startup_20251102_103015.log`）

### 5. 并行标注
- 明确标注哪些任务是并行执行的
- 显示并行任务各自的耗时
- 总耗时取最长分支的时间

---

## 🔧 日志级别对应关系

### Terminal输出（简洁）
- **STAGE_NODE** 日志类型：阶段节点、流程开始/完成
- **WARNING及以上**：警告、错误、严重问题
- **NOTIFICATION**：任务完成通知
- **ALERT**：系统告警

### AI日志文件（详细）
- **DEBUG**：详细调试信息（如变量值、中间结果）
- **INFO**：常规信息日志（如函数调用、配置加载）
- **WARNING**：警告信息（如资源使用率高、降级运行）
- **ERROR**：错误信息（如初始化失败、异常捕获）
- **CRITICAL**：严重错误（如服务崩溃、数据损坏）

---

## 📁 相关AI日志文件

启动过程中自动生成的AI日志文件：

```
C:\Users\USER\Desktop\terminal_v0.50\logs\ai\
├── application_startup_20251102_103015.log       # 主启动流程
├── monitor_process_startup_20251102_103017.log   # 监控进程启动
└── backend_services_init_20251102_103018.log     # 后端服务初始化
```

每个AI日志文件包含：
- **文件头**：流程说明、开始时间、元数据
- **完整日志**：DEBUG及以上所有级别，带图标和分隔线
- **异常堆栈**：完整的Python堆栈跟踪
- **文件尾**：流程结束时间、执行结果、日志统计

---

## ⚠️ 常见错误输出示例

### 错误示例1：监控进程启动失败

```
📍 监控进程启动开始
✅ monitor_system.py进程已启动 (PID: 12345)
❌ 创建native_ipc管道失败
  └─ 错误: OSError: [WinError 231] 所有的管道实例都在忙
  └─ 原因: 管道名称冲突或上次未正常关闭
  └─ 建议: 1. 检查是否有残留进程占用管道
          2. 尝试重启应用
          3. 查看详细日志: logs\ai\monitor_process_startup_20251102_103017.log
```

### 错误示例2：服务初始化失败

```
📍 阶段3.3: 数据服务初始化开始
✅ DataCenterService初始化完成
❌ 本地数据索引扫描失败
  └─ 错误: PermissionError: [WinError 5] 拒绝访问
  └─ 原因: 缺少管理员权限或文件被占用
  └─ 建议: 以管理员身份运行应用
❌ 数据服务就绪（降级模式）
  └─ 功能受限：仅可查询缓存数据，无法扫描本地文件
```

---

## 🎯 使用说明

### 对比工具
将实际Terminal输出与本文档对比，可快速定位启动问题：
1. 哪个阶段耗时异常
2. 哪个服务初始化失败
3. 是否有并行任务卡住

### 调试建议
1. **正常情况**：对照本文档，确认所有阶段都有对应输出
2. **异常情况**：查看错误标记 `❌`，根据建议操作
3. **性能问题**：对比耗时统计，找出瓶颈阶段
4. **深度分析**：查看对应的AI日志文件，获取完整上下文

---

## 📝 Log输出设计

### 核心策略：使用STAGE_NODE日志类型 + AI日志流程管理

项目使用 `unified_log_system.py` 统一日志系统，具备以下功能：
1. **四层路由规则**：场景 > 模块 > 阶段 > 全局
2. **LogType分类**：SYSTEM, PROGRESS, NOTIFICATION, ALERT, USER_FEEDBACK, DEBUG, **STAGE_NODE**
3. **AI日志管理**：`ai_log_process()` 上下文管理器，自动创建流程日志文件
4. **控制台过滤**：只输出特定类型到Terminal（STAGE_NODE、NOTIFICATION、ALERT、WARNING及以上）

### 实现方案：利用STAGE_NODE + 阶段切换

#### 1. 主启动流程（start_async_fixed.py）

```python
def main():
    """主启动流程 - 带分阶段日志"""
    from backend.infrastructure.system_vnpy import get_logging_hub, ai_log_process
    
    # 获取LoggingHub
    hub = get_logging_hub()
    
    # 🎯 启动AI日志流程（整个启动过程）
    with ai_log_process("application_startup"):
        logger = logging.getLogger("startup")
        
        # 阶段0: 环境准备
        hub.set_stage("env_setup")  # 切换阶段
        logger.info("📍 阶段0: 环境准备开始", extra={"log_type": "STAGE_NODE"})
        elapsed = setup_environment()
        logger.info(f"✅ 环境准备完成 ({elapsed:.0f}ms)", extra={"log_type": "STAGE_NODE"})
        
        # 阶段1: 日志系统
        hub.set_stage("logging_init")
        logger.info("📍 阶段1: 日志系统初始化开始", extra={"log_type": "STAGE_NODE"})
        # ... 初始化逻辑 ...
        logger.info("✅ 日志系统就绪", extra={"log_type": "STAGE_NODE"})
        
        # 阶段2: Qt应用
        hub.set_stage("qt_init")
        logger.info("📍 阶段2: Qt应用框架开始", extra={"log_type": "STAGE_NODE"})
        # ... 初始化逻辑 ...
        logger.info("✅ Qt应用框架就绪", extra={"log_type": "STAGE_NODE"})
        
        # 阶段3: 后端服务
        hub.set_stage("backend_init")
        logger.info("📍 阶段3: 后端服务初始化开始", extra={"log_type": "STAGE_NODE"})
        coordinator = StartupCoordinator(app, config_already_initialized=True)
        # ... 初始化逻辑 ...
        
        # 阶段4: UI主窗口
        hub.set_stage("ui_init")
        logger.info("📍 阶段4: UI主窗口创建开始", extra={"log_type": "STAGE_NODE"})
        # ... 初始化逻辑 ...
        logger.info("✅ UI主窗口就绪", extra={"log_type": "STAGE_NODE"})
        
        # 启动完成
        hub.set_stage("running")
        logger.info("🎉 星辰金融终端启动成功！", extra={"log_type": "STAGE_NODE"})
```

#### 2. 后端服务初始化（ui/startup_coordinator.py）

```python
class BackendInitializerWorker(QObject):
    def run(self):
        """后端服务初始化 - 使用STAGE_NODE标记关键节点"""
        from backend.infrastructure.system_vnpy import get_logging_hub
        
        hub = get_logging_hub()
        logger = logging.getLogger("backend.startup.worker")
        
        # 分支A: 监控进程（并行）
        # ... 监控进程启动逻辑 ...
        
        # 分支B: 数据引擎初始化
        hub.set_stage("data_engine_init")
        logger.info("📍 阶段3.1: ChinaStockEngine初始化开始", extra={"log_type": "STAGE_NODE"})
        
        # 创建CacheValidationWorker
        validation_worker = CacheValidationWorker(china_stock_engine)
        validation_thread = QThread()
        validation_worker.moveToThread(validation_thread)
        
        # 连接进度信号，使用STAGE_NODE输出8步流程
        validation_worker.step_completed.connect(
            lambda num, name, result: logger.info(
                f"✅ 步骤{num}完成 ({result.get('elapsed', 0):.0f}ms) [进度: {result.get('progress', 0)}%]",
                extra={"log_type": "STAGE_NODE"}
            )
        )
        
        validation_thread.started.connect(validation_worker.run)
        validation_thread.start()
        validation_thread.wait()  # 同步等待完成
        
        logger.info("✅ 数据引擎完全就绪", extra={"log_type": "STAGE_NODE"})
        
        # 分支C: 业务服务初始化
        hub.set_stage("business_services_init")
        
        # 阶段3.4: 交易服务
        logger.info("📍 阶段3.4: 交易服务初始化开始", extra={"log_type": "STAGE_NODE"})
        # ... TradingGatewayService初始化 ...
        logger.info("✅ 交易服务就绪", extra={"log_type": "STAGE_NODE"})
        
        # 阶段3.5: 策略服务
        logger.info("📍 阶段3.5: 策略服务初始化开始", extra={"log_type": "STAGE_NODE"})
        # ... StrategyCenterService和AIAssistantService初始化 ...
        logger.info("✅ 策略服务就绪", extra={"log_type": "STAGE_NODE"})
        
        # 阶段3.6: 辅助服务
        logger.info("📍 阶段3.6: 辅助服务初始化开始", extra={"log_type": "STAGE_NODE"})
        # ... PortfolioService、MarketBoardService、SystemManagerService初始化 ...
        logger.info("✅ 辅助服务就绪", extra={"log_type": "STAGE_NODE"})
```

#### 3. 监控进程启动（backend/infrastructure/system_vnpy/monitor_system.py）

```python
async def main():
    """监控进程启动 - 使用STAGE_NODE标记"""
    from backend.infrastructure.system_vnpy import get_logging_hub, ai_log_process
    
    # 监控进程有独立的AI日志流程
    with ai_log_process("monitor_process_startup"):
        hub = get_logging_hub()
        logger = logging.getLogger("backend.monitor.startup")
        
        hub.set_stage("monitor_init")
        logger.info("📍 监控进程启动开始", extra={"log_type": "STAGE_NODE"})
        
        # native_ipc管道创建
        logger.info("✅ monitor_system.py进程已启动 (PID: 12345)", extra={"log_type": "STAGE_NODE"})
        logger.info("✅ 创建native_ipc管道", extra={"log_type": "STAGE_NODE"})
        # ... 管道创建逻辑 ...
        logger.info("✅ Level 1就绪 (管道就绪)", extra={"log_type": "STAGE_NODE"})
        
        # 监控组件初始化
        logger.info("✅ 监控组件初始化", extra={"log_type": "STAGE_NODE"})
        # ... 组件初始化逻辑 ...
        logger.info("✅ 监控进程完全就绪", extra={"log_type": "STAGE_NODE"})
        
        hub.set_stage("monitoring")
```

#### 4. 8步缓存验证流程（backend/infrastructure/data_module_vnpy/core_engine.py）

```python
def _smart_cache_validation_and_sensing(
    self,
    progress_callback: Optional[Callable[[str, int], None]] = None,
    step_callback: Optional[Callable[[int, str, dict], None]] = None
) -> Dict[str, Any]:
    """智能缓存验证与感知（8步流程 + 离线降级）"""
    
    from backend.infrastructure.system_vnpy import get_logging_hub
    hub = get_logging_hub()
    logger = logging.getLogger("backend.data_module.cache_validation")
    
    start_time = time.time()
    step_results = []
    
    try:
        # 步骤1: 服务器池验证与测速
        hub.set_stage("cache_validation_step1")
        logger.info("┌─ 步骤1: 服务器池验证与测速 ─┐", extra={"log_type": "STAGE_NODE"})
        if progress_callback:
            progress_callback("步骤1/8: 验证服务器池缓存并测速...", 5)
        
        step1_result = self._validate_server_pool_and_test_speed()
        step1_result["elapsed"] = (time.time() - start_time) * 1000
        step1_result["progress"] = 12
        
        if step_callback:
            step_callback(1, "服务器池验证与测速", step1_result)
        
        logger.info("✅ 步骤1完成", extra={"log_type": "STAGE_NODE"})
        
        # 离线降级检查点
        if step1_result.get("offline_mode"):
            logger.warning("🔴 触发离线降级: " + step1_result.get("offline_reason", ""), 
                         extra={"log_type": "STAGE_NODE"})
            # 跳转到步骤8
            if progress_callback:
                progress_callback("离线模式: 跳过步骤2-7,直接启动文件监控", 90)
            goto_step_8 = True
        else:
            goto_step_8 = False
            # 初始化LoadBalancer
            self.load_balancer = LoadBalancer.get_instance(self.event_engine)
        
        if not goto_step_8:
            # 步骤2-7: 正常验证流程
            # ... 步骤2-7的实现 ...
        
        # 步骤8: 启动文件监控
        hub.set_stage("cache_validation_step8")
        logger.info("┌─ 步骤8: 启动文件监控 ─┐", extra={"log_type": "STAGE_NODE"})
        if progress_callback:
            progress_callback("步骤8/8: 启动文件监控...", 90)
        
        step8_result = self._start_file_watcher()
        step8_result["elapsed"] = (time.time() - start_time) * 1000
        step8_result["progress"] = 100
        
        if step_callback:
            step_callback(8, "启动文件监控", step8_result)
        
        logger.info("✅ 步骤8完成", extra={"log_type": "STAGE_NODE"})
        
        elapsed_time = time.time() - start_time
        logger.info(f"✅ 缓存验证与感知流程完成 (总耗时: {elapsed_time:.1f}s)", 
                   extra={"log_type": "STAGE_NODE"})
        
        return {
            "success": True,
            "offline_mode": step1_result.get("offline_mode", False),
            "steps_completed": len(step_results),
            "total_time": elapsed_time
        }
    
    except Exception as e:
        logger.exception("❌ 缓存验证与感知失败: %s", e)
        return {"success": False, "error": str(e)}
```

### 为什么这个方案有效？

**1. Terminal输出简洁**：
- `STAGE_NODE` 日志类型会自动输出到Terminal（在 `_console_enabled_types` 中）
- 其他DEBUG/INFO日志不会输出到Terminal，只写入AI日志文件
- 效果：Terminal只显示流程节点，不刷屏

**2. AI日志完整**：
- `ai_log_process("application_startup")` 会创建专门的AI日志文件
- 所有DEBUG、INFO、WARNING、ERROR、CRITICAL日志都写入该文件
- 效果：AI分析时有完整上下文

**3. 自动分阶段**：
- `hub.set_stage()` 切换阶段后，路由规则自动应用该阶段的配置
- 效果：不同阶段的日志可以有不同的路由策略

---

## 🏗️ 底层代码设计

### 架构概述

启动流程的核心架构分为三个层次：
1. **主进程层（MainThread）**：`start_async_fixed.py` 负责阶段0-2和阶段4
2. **后端初始化层（BackendInitializerWorker QThread）**：`ui/startup_coordinator.py` 负责阶段3的并行任务协调
3. **数据验证层（CacheValidationWorker QThread）**：`backend/infrastructure/data_module_vnpy/core_engine.py` 负责8步缓存验证流程

### 1. CacheValidationWorker设计

**文件位置**: `backend/infrastructure/data_module_vnpy/core_engine.py`

**核心类设计**:

```python
from PySide6.QtCore import QObject, Signal, QThread
import logging
from typing import Dict, Any, Optional

logger = logging.getLogger("backend.data_module.validation_worker")

class CacheValidationWorker(QObject):
    """缓存验证工作线程（Qt后台线程）

    负责在启动时执行完整的缓存验证与数据感知流程,
    确保数据模块处于可用状态。

    信号:
        validation_started: 验证开始
        validation_progress(str, int): 进度更新(步骤描述, 进度百分比)
        step_completed(int, str, dict): 步骤完成(步骤号, 步骤名, 步骤结果)
        validation_finished(dict): 验证完成(完整结果)
        validation_error(str): 验证失败(错误信息)
        offline_mode_triggered(str): 离线模式触发(原因)
    """

    # 信号定义
    validation_started = Signal()
    validation_progress = Signal(str, int)  # (描述, 百分比)
    step_completed = Signal(int, str, dict)  # (步骤号, 步骤名, 结果)
    validation_finished = Signal(dict)
    validation_error = Signal(str)
    offline_mode_triggered = Signal(str)  # 离线模式触发信号

    def __init__(self, china_stock_engine):
        """初始化验证工作线程

        Args:
            china_stock_engine: ChinaStockEngine实例
        """
        super().__init__()
        self.engine = china_stock_engine
        self._cancelled = False
        self._offline_mode = False

    def run(self):
        """执行验证流程（在QThread中调用）"""
        try:
            self.validation_started.emit()
            logger.info("=" * 70)
            logger.info("🚀 开始缓存验证与感知流程（8步）")
            logger.info("=" * 70)

            # 调用核心验证逻辑
            result = self.engine._smart_cache_validation_and_sensing(
                progress_callback=self._on_progress,
                step_callback=self._on_step_completed
            )

            # 发送完成信号
            if result.get("offline_mode"):
                self.offline_mode_triggered.emit(result.get("offline_reason", "未知原因"))

            self.validation_finished.emit(result)
            logger.info("✅ 缓存验证与感知流程完成")

        except Exception as e:
            logger.exception("❌ 缓存验证失败: %s", e)
            self.validation_error.emit(str(e))

    def _on_progress(self, description: str, percent: int):
        """进度回调"""
        if not self._cancelled:
            self.validation_progress.emit(description, percent)

    def _on_step_completed(self, step_num: int, step_name: str, step_result: dict):
        """步骤完成回调"""
        if not self._cancelled:
            self.step_completed.emit(step_num, step_name, step_result)

    def cancel(self):
        """取消验证（外部调用）"""
        self._cancelled = True
        logger.warning("⚠️ 缓存验证被取消")
```

**优势**:
- ✅ Qt信号槽机制,线程安全
- ✅ 丰富的进度反馈
- ✅ 支持取消操作
- ✅ 离线模式专用信号

### 2. _smart_cache_validation_and_sensing核心流程设计

**文件位置**: `backend/infrastructure/data_module_vnpy/core_engine.py`

**完整方法签名**:

```python
def _smart_cache_validation_and_sensing(
    self,
    progress_callback: Optional[Callable[[str, int], None]] = None,
    step_callback: Optional[Callable[[int, str, dict], None]] = None
) -> Dict[str, Any]:
    """智能缓存验证与感知（8步流程 + 离线降级）

    这是启动项的核心主干流程,负责:
    1. 验证和初始化所有缓存
    2. 初始化核心组件(LoadBalancer, UnifiedDataManager等)
    3. TDX离线降级检测
    4. 启动文件监控

    Args:
        progress_callback: 进度回调函数(description, percent)
        step_callback: 步骤完成回调(step_num, step_name, result)

    Returns:
        Dict[str, Any]: {
            "success": bool,
            "offline_mode": bool,
            "offline_reason": str,
            "steps_completed": int,
            "step_results": List[dict],
            "total_time": float
        }

    异常:
        不抛出异常,所有错误封装在返回值中
    """
    
    start_time = time.time()
    step_results = []
    offline_mode = False
    offline_reason = ""

    # 辅助函数
    def _progress(desc: str, pct: int):
        if progress_callback:
            progress_callback(desc, pct)

    def _step_done(num: int, name: str, result: dict):
        step_results.append(result)
        if step_callback:
            step_callback(num, name, result)

    try:
        # ========== 步骤1: 服务器池缓存验证与测速 ==========
        _progress("步骤1/8: 验证服务器池缓存并测速...", 5)
        step1_result = self._validate_server_pool_and_test_speed()
        step1_result["elapsed"] = (time.time() - start_time) * 1000
        step1_result["progress"] = 12
        _step_done(1, "服务器池验证与测速", step1_result)

        # 离线降级检查点
        if step1_result.get("offline_mode"):
            offline_mode = True
            offline_reason = step1_result.get("offline_reason")
            logger.critical(f"🔴 触发离线降级: {offline_reason}")
            # 跳转到步骤8
            _progress("离线模式: 跳过步骤2-7,直接启动文件监控", 90)
            goto_step_8 = True
        else:
            goto_step_8 = False
            # 初始化LoadBalancer
            _progress("步骤1/8: 初始化LoadBalancer...", 10)
            self.load_balancer = LoadBalancer.get_instance(self.event_engine)

        if not goto_step_8:
            # ========== 步骤2: 获取当前日期(网络时间) ==========
            _progress("步骤2/8: 获取当前日期(网络时间)...", 15)
            step2_result = self._get_current_date()
            step2_result["elapsed"] = (time.time() - start_time) * 1000
            step2_result["progress"] = 25
            _step_done(2, "获取当前日期", step2_result)
            current_date = step2_result.get("current_date")

            # ========== 步骤3: 验证交易日历缓存 ==========
            _progress("步骤3/8: 验证交易日历缓存...", 25)
            step3_result = self._validate_trade_calendar_cache(current_date)
            step3_result["elapsed"] = (time.time() - start_time) * 1000
            step3_result["progress"] = 37
            _step_done(3, "验证交易日历缓存", step3_result)

            # ========== 步骤4: 验证品种列表缓存 ==========
            _progress("步骤4/8: 验证品种列表缓存...", 35)
            step4_result = self._validate_symbol_list_cache(current_date)
            step4_result["elapsed"] = (time.time() - start_time) * 1000
            step4_result["progress"] = 50
            _step_done(4, "验证品种列表缓存", step4_result)

            # 初始化SymbolLoader
            if not step4_result.get("cache_valid"):
                _progress("步骤4/8: 重新加载品种列表...", 40)
                self.symbol_loader = SymbolLoader(self.event_engine)
                await self.symbol_loader.reload_and_classify_async()

            # ========== 步骤5: 验证IPO日期缓存 ==========
            _progress("步骤5/8: 验证IPO日期缓存...", 50)
            step5_result = self._validate_ipo_cache(current_date)
            step5_result["elapsed"] = (time.time() - start_time) * 1000
            step5_result["progress"] = 62
            _step_done(5, "验证IPO日期缓存", step5_result)

            # ========== 步骤6: 更新本地数据索引 ==========
            _progress("步骤6/8: 更新本地数据索引...", 65)
            step6_result = self._update_local_data_index()
            step6_result["elapsed"] = (time.time() - start_time) * 1000
            step6_result["progress"] = 75
            _step_done(6, "更新本地数据索引", step6_result)

            # 初始化StorageManager
            _progress("步骤6/8: 初始化StorageManager...", 70)
            self.storage_manager = StorageManager()

            # ========== 步骤7: 检查数据更新状态 ==========
            _progress("步骤7/8: 检查数据更新状态...", 75)
            step7_result = self._check_data_update_status()
            step7_result["elapsed"] = (time.time() - start_time) * 1000
            step7_result["progress"] = 87
            _step_done(7, "检查数据更新状态", step7_result)

            # 初始化DataSensor和UnifiedDataManager
            _progress("步骤7/8: 初始化DataSensor和UnifiedDataManager...", 85)
            self.data_sensor = DataSensor(self.event_engine)
            self.unified_data_manager = UnifiedDataManager(self)

        # ========== 步骤8: 启动文件监控 ==========
        _progress("步骤8/8: 启动文件监控...", 90)
        step8_result = self._start_file_watcher()
        step8_result["elapsed"] = (time.time() - start_time) * 1000
        step8_result["progress"] = 100
        _step_done(8, "启动文件监控", step8_result)

        # 完成
        _progress("缓存验证与感知完成", 100)
        elapsed_time = time.time() - start_time

        return {
            "success": True,
            "offline_mode": offline_mode,
            "offline_reason": offline_reason,
            "steps_completed": len(step_results),
            "step_results": step_results,
            "total_time": elapsed_time
        }

    except Exception as e:
        logger.exception("❌ 缓存验证与感知失败: %s", e)
        return {
            "success": False,
            "offline_mode": False,
            "offline_reason": "",
            "steps_completed": len(step_results),
            "step_results": step_results,
            "error": str(e)
        }
```

### 3. 启动项集成点设计

**修改文件**: `ui/startup_coordinator.py`

**关键修改点**:

```python
class BackendInitializerWorker(QObject):
    def run(self):
        """后端服务初始化 - 并行任务协调"""
        from backend.infrastructure.system_vnpy import get_logging_hub
        from backend.infrastructure.data_module_vnpy.core_engine import CacheValidationWorker
        
        hub = get_logging_hub()
        logger = logging.getLogger("backend.startup.worker")
        
        # 分支A: 监控进程（并行）
        with ThreadPoolExecutor(max_workers=2) as executor:
            # 任务A: 初始化ChinaStockEngine
            future_a = executor.submit(self._init_china_stock_engine)
            
            # 任务B: 启动监控进程
            future_b = executor.submit(self._start_monitoring_subprocess)
            
            # 等待任务A完成（数据引擎必须优先）
            china_stock_engine = future_a.result()
            
            # 创建CacheValidationWorker
            validation_worker = CacheValidationWorker(china_stock_engine)
            validation_thread = QThread()
            validation_worker.moveToThread(validation_thread)
            
            # 连接信号，使用STAGE_NODE输出
            validation_worker.step_completed.connect(
                lambda num, name, result: logger.info(
                    f"✅ 步骤{num}完成 ({result.get('elapsed', 0):.0f}ms) [进度: {result.get('progress', 0)}%]",
                    extra={"log_type": "STAGE_NODE"}
                )
            )
            
            validation_worker.offline_mode_triggered.connect(
                lambda reason: logger.warning(
                    f"🔴 触发离线降级: {reason}",
                    extra={"log_type": "STAGE_NODE"}
                )
            )
            
            validation_thread.started.connect(validation_worker.run)
            validation_thread.finished.connect(lambda: logger.info(
                "✅ 数据引擎完全就绪",
                extra={"log_type": "STAGE_NODE"}
            ))
            
            # 启动验证线程（同步等待完成）
            validation_thread.start()
            validation_thread.wait()  # 阻塞等待完成
            
            # 等待任务B完成（监控进程）
            future_b.result()
        
        # 分支C: 业务服务初始化
        hub.set_stage("business_services_init")
        logger.info("┌──────────────────────────────────────────────────────────────────┐",
                   extra={"log_type": "STAGE_NODE"})
        logger.info("│ 分支C: 业务服务初始化                                            │",
                   extra={"log_type": "STAGE_NODE"})
        logger.info("└──────────────────────────────────────────────────────────────────┘",
                   extra={"log_type": "STAGE_NODE"})
        
        # 阶段3.4: 交易服务
        logger.info("📍 阶段3.4: 交易服务初始化开始", extra={"log_type": "STAGE_NODE"})
        # ... TradingGatewayService初始化 ...
        logger.info("✅ 交易服务就绪", extra={"log_type": "STAGE_NODE"})
        
        # 阶段3.5: 策略服务
        logger.info("📍 阶段3.5: 策略服务初始化开始", extra={"log_type": "STAGE_NODE"})
        # ... StrategyCenterService和AIAssistantService初始化 ...
        logger.info("✅ 策略服务就绪", extra={"log_type": "STAGE_NODE"})
        
        # 阶段3.6: 辅助服务
        logger.info("📍 阶段3.6: 辅助服务初始化开始", extra={"log_type": "STAGE_NODE"})
        # ... PortfolioService、MarketBoardService、SystemManagerService初始化 ...
        logger.info("✅ 辅助服务就绪", extra={"log_type": "STAGE_NODE"})
```

### 4. 服务初始化时序设计

**整体初始化时序**:

```
主进程(MainThread)
    │
    ├─ 阶段0: 环境准备(<100ms)
    │   └─ setup_environment()
    │
    ├─ 阶段1: Qt框架初始化(<500ms)
    │   ├─ QApplication创建
    │   ├─ 加载配置
    │   └─ 初始化LoggingHub
    │
    ├─ 阶段2: UI框架创建(<1s)
    │   ├─ 创建MainWindow
    │   ├─ 显示主窗口
    │   ├─ 初始化EventEngine
    │   ├─ 初始化MainEngine
    │   └─ 注入占位方法
    │
    ├─ 阶段3: 启动后端初始化(异步,2-5s)
    │   │
    │   └─→ BackendInitializerWorker (QThread)
    │         │
    │         ├─ 并行任务A: 初始化ChinaStockEngine
    │         │   ├─ initialize()
    │         │   └─→ CacheValidationWorker (QThread)
    │         │         │
    │         │         └─ _smart_cache_validation_and_sensing()
    │         │               ├─ 步骤1: 服务器池验证与测速
    │         │               │   └─ 初始化LoadBalancer
    │         │               │
    │         │               ├─ 离线降级检查点
    │         │               │   └─ 如失败→跳到步骤8
    │         │               │
    │         │               ├─ 步骤2-7: 缓存验证与组件初始化
    │         │               │   ├─ 获取当前日期
    │         │               │   ├─ 验证交易日历
    │         │               │   ├─ 验证品种列表
    │         │               │   │   └─ 初始化SymbolLoader
    │         │               │   ├─ 验证IPO日期
    │         │               │   ├─ 更新本地数据索引
    │         │               │   │   └─ 初始化StorageManager
    │         │               │   └─ 检查数据更新状态
    │         │               │       └─ 初始化DataSensor
    │         │               │       └─ 初始化UnifiedDataManager
    │         │               │
    │         │               └─ 步骤8: 启动文件监控
    │         │
    │         ├─ 并行任务B: 启动监控进程
    │         │   └─ start_monitoring_subprocess()
    │         │
    │         └─ 初始化业务服务
    │               ├─ TradingGatewayService (阶段3.4)
    │               ├─ StrategyCenterService (阶段3.5)
    │               ├─ AIAssistantService (阶段3.5)
    │               ├─ PortfolioService (阶段3.6)
    │               ├─ MarketBoardService (阶段3.6)
    │               └─ SystemManagerService (阶段3.6)
    │
    ├─ 阶段4: UI功能激活(主线程,1-2s)
    │   ├─ initialize_function_interfaces_after_backend()
    │   ├─ 隐藏启动画面
    │   └─ 启动可选服务后台加载器
    │
    └─ 启动Qt事件循环
```

**关键并行优化点**:

1. **CacheValidationWorker与监控进程启动并行**：
   - 使用 `ThreadPoolExecutor` 同时启动ChinaStockEngine初始化和监控进程
   - 等待ChinaStockEngine初始化完成后，再启动CacheValidationWorker
   - 监控进程在后台并行运行

2. **步骤1服务器测速多进程**：
   - 使用 `LoadBalancer.test_servers(max_workers=4)` 进行多进程并行测速
   - 大幅缩短步骤1耗时（从3-5秒降到<1秒）

---

## 🔄 TDX离线降级机制设计

### 概述

当TDX服务器池全部不可用时，系统自动进入离线降级模式，跳过步骤2-7，直接执行步骤8，确保系统可以正常启动并有限运行。

### 1. 降级触发条件

**主触发点**: 步骤1服务器池验证失败

**判断逻辑**:

```python
def _validate_server_pool_and_test_speed(self) -> Dict[str, Any]:
    """步骤1: 服务器池缓存验证与测速

    Returns:
        Dict[str, Any]: {
            "success": bool,
            "ipv4_available": bool,
            "ipv6_available": bool,
            "offline_mode": bool,
            "offline_reason": str
        }
    """
    try:
        from .load_balancer import ServerPoolManager

        # 初始化服务器池管理器
        pool_manager = ServerPoolManager.get_instance()

        # 测试IPv4服务器池
        ipv4_results = pool_manager.test_servers(pool_type="ipv4", max_workers=4)
        ipv4_available = len([s for s in ipv4_results if s.get("latency", 999) < 500]) > 0

        # 测试IPv6服务器池
        ipv6_results = pool_manager.test_servers(pool_type="ipv6", max_workers=4)
        ipv6_available = len([s for s in ipv6_results if s.get("latency", 999) < 500]) > 0

        # 判断是否需要进入离线模式
        if not ipv4_available and not ipv6_available:
            logger.critical("🔴 所有TDX服务器不可用,进入离线降级模式")
            return {
                "success": False,
                "ipv4_available": False,
                "ipv6_available": False,
                "offline_mode": True,
                "offline_reason": "所有TDX服务器(IPv4/IPv6)不可用"
            }

        # 至少一个服务器池可用
        logger.info(f"✅ TDX服务器池可用: IPv4={ipv4_available}, IPv6={ipv6_available}")
        return {
            "success": True,
            "ipv4_available": ipv4_available,
            "ipv6_available": ipv6_available,
            "offline_mode": False
        }

    except Exception as e:
        logger.exception("❌ 服务器池验证失败: %s", e)
        return {
            "success": False,
            "ipv4_available": False,
            "ipv6_available": False,
            "offline_mode": True,
            "offline_reason": f"服务器池验证异常: {e}"
        }
```

**触发条件**:
- IPv4和IPv6服务器池均不可用（所有服务器延迟 > 500ms）
- 服务器池验证异常

### 2. 离线模式状态管理

**在ChinaStockEngine中添加**:

```python
class ChinaStockEngine:
    """中国股票数据引擎（核心引擎）"""

    def __init__(self, main_engine: MainEngine, event_engine: EventEngine):
        # ... 现有代码 ...

        # 离线模式标记
        self._offline_mode: bool = False
        self._offline_reason: str = ""

    def is_offline_mode(self) -> bool:
        """检查是否处于离线模式"""
        return self._offline_mode

    def get_offline_reason(self) -> str:
        """获取离线原因"""
        return self._offline_reason

    def set_offline_mode(self, offline: bool, reason: str = ""):
        """设置离线模式（内部使用）"""
        self._offline_mode = offline
        self._offline_reason = reason

        if offline:
            logger.warning(f"⚠️ 系统已进入离线降级模式: {reason}")
            # 发布离线模式事件
            self.event_engine.put(Event("eSystemOfflineMode", {
                "offline": True,
                "reason": reason,
                "timestamp": datetime.now()
            }))
```

### 3. 离线模式流程跳转

**在_smart_cache_validation_and_sensing中实现**:

```python
# 步骤1: 服务器池验证与测速
step1_result = self._validate_server_pool_and_test_speed()

# 离线降级检查点
if step1_result.get("offline_mode"):
    offline_mode = True
    offline_reason = step1_result.get("offline_reason")
    logger.critical(f"🔴 触发离线降级: {offline_reason}")
    
    # 设置离线模式状态
    self.set_offline_mode(True, offline_reason)
    
    # 跳转到步骤8
    _progress("离线模式: 跳过步骤2-7,直接启动文件监控", 90)
    goto_step_8 = True
else:
    goto_step_8 = False
    # 初始化LoadBalancer
    self.load_balancer = LoadBalancer.get_instance(self.event_engine)

if not goto_step_8:
    # 步骤2-7: 正常验证流程
    # ... 执行步骤2-7 ...
    
# 步骤8: 启动文件监控（正常模式和离线模式都执行）
step8_result = self._start_file_watcher()
```

**离线模式输出示例**:

```
┌─ 步骤1: 服务器池验证与测速 ─┐
│ ⏳ 正在测速IPv4服务器池...（20个服务器）
│ ❌ IPv4服务器池不可用: 0个可用，所有服务器超时
│ ⏳ 正在测速IPv6服务器池...（5个服务器）
│ ❌ IPv6服务器池不可用: 0个可用，所有服务器超时
│ 🔴 所有TDX服务器(IPv4/IPv6)不可用
└─────────────────────────────┘
✅ 步骤1完成 (3000ms) [进度: 12%]
🔴 触发离线降级: 所有TDX服务器(IPv4/IPv6)不可用

⏳ 离线模式: 跳过步骤2-7,直接启动文件监控

┌─ 步骤8: 启动文件监控 ─┐
│ ⏳ 启动DataFileWatcher...
│ ✅ 文件监控器已启动
│   - 监控目录: C:\Users\USER\Desktop\terminal_v0.50\data\kline
│   - 监控模式: 实时变更检测
└─────────────────────────────┘
✅ 步骤8完成 (100ms) [进度: 100%]

✅ 缓存验证与感知流程完成 (总耗时: 3.1s)
⚠️ 系统已进入离线降级模式: 所有TDX服务器(IPv4/IPv6)不可用
✅ 数据引擎完全就绪（离线模式）
```

### 4. UnifiedDataManager离线降级

**修改`data_runtime.py`中的UnifiedDataManager**:

```python
class UnifiedDataManager:
    """统一数据管理器（四层数据融合）"""

    def __init__(self, china_stock_engine: ChinaStockEngine):
        self.engine = china_stock_engine
        self.event_engine = china_stock_engine.event_engine

        # 检查离线模式
        self.offline_mode = china_stock_engine.is_offline_mode()

        if self.offline_mode:
            logger.warning("⚠️ UnifiedDataManager以离线模式初始化")
            # 只初始化本地存储层
            self.storage_manager = china_stock_engine.storage_manager
            self.preload_service = None  # 禁用预加载
            self.tdx_data_source = None  # 禁用TDX数据源
            self.virtual_data_source = None  # 禁用虚拟数据源
        else:
            # 正常初始化所有层
            self.storage_manager = china_stock_engine.storage_manager
            self.preload_service = PreloadService(self.storage_manager)
            self.tdx_data_source = TdxDataSource(self.event_engine)
            self.virtual_data_source = VirtualDataSource(self.event_engine)

    async def query_kline_async(
        self,
        symbol: str,
        interval: str,
        start_date: Optional[str] = None,
        end_date: Optional[str] = None
    ) -> Optional[pd.DataFrame]:
        """异步查询K线数据（四层融合 or 离线模式）"""

        if self.offline_mode:
            # 离线模式:仅查询本地存储
            logger.debug(f"[离线模式] 仅查询本地数据: {symbol}/{interval}")
            return await self.storage_manager.load_data_async(
                symbol, interval, start_date, end_date
            )

        # 正常模式:四层融合查询
        # Layer 1: 预加载缓存
        if self.preload_service:
            cached = self.preload_service.get_from_cache(symbol, interval)
            if cached is not None:
                logger.debug(f"[预加载缓存] 命中: {symbol}/{interval}")
                return self._filter_by_date(cached, start_date, end_date)

        # Layer 2: 本地存储
        df = await self.storage_manager.load_data_async(symbol, interval, start_date, end_date)
        if df is not None and not df.empty:
            logger.debug(f"[本地存储] 命中: {symbol}/{interval}")
            # 更新预加载缓存
            if self.preload_service:
                self.preload_service.add_to_cache(symbol, interval, df)
            return df

        # Layer 3 & 4: TDX数据源和实时推送
        # ... 正常模式逻辑 ...
```

### 5. 服务降级策略

**修改`backend/core/base.py`中的`ServiceInitializer`**:

```python
class ServiceInitializer:
    """服务初始化器"""

    def initialize_all_services(
        self,
        event_engine: EventEngine,
        main_engine: MainEngine,
        china_stock_engine: ChinaStockEngine,
        progress_callback=None,
        fast_startup: bool = True
    ) -> Dict[str, bool]:
        """初始化所有服务（支持离线降级）"""

        results = {}
        offline_mode = china_stock_engine.is_offline_mode()

        if offline_mode:
            logger.warning("⚠️ 离线模式: 部分服务将跳过初始化")

        # 核心服务(始终初始化)
        results["data_center"] = self._init_data_center(event_engine, china_stock_engine)
        results["market_board"] = self._init_market_board(event_engine, china_stock_engine)
        results["strategy_center"] = self._init_strategy_center(event_engine)
        results["ai_assistant"] = self._init_ai_assistant(event_engine)
        results["system_manager"] = self._init_system_manager(event_engine)

        # 网络依赖服务(离线模式禁用)
        if offline_mode:
            logger.warning("⚠️ 离线模式: 跳过交易网关和组合投资服务初始化")
            results["trading_gateway"] = False
            results["portfolio"] = False
        else:
            results["trading_gateway"] = self._init_trading_gateway(event_engine, main_engine)
            results["portfolio"] = self._init_portfolio(event_engine)

        return results
```

**离线模式服务初始化输出示例**:

```
📍 阶段3.4: 交易服务初始化开始
⚠️ 离线模式: 跳过交易网关服务初始化
✅ 交易服务就绪（已跳过）

📍 阶段3.5: 策略服务初始化开始
✅ StrategyCenterService初始化完成
✅ AIAssistantService初始化完成
✅ 策略服务就绪

📍 阶段3.6: 辅助服务初始化开始
✅ MarketBoardService初始化完成
✅ SystemManagerService初始化完成
⚠️ 离线模式: 跳过组合投资服务初始化
✅ 辅助服务就绪（部分服务已跳过）
```

### 6. UI层降级提示

**修改`ui/main_window.py`**:

```python
class MainWindow(QMainWindow):
    """主窗口"""

    def __init__(self, backend_ready: bool = False):
        super().__init__()
        # ... 现有代码 ...

        # 监听离线模式事件
        event_engine = get_event_engine()
        if event_engine:
            event_engine.register("eSystemOfflineMode", self._on_offline_mode)

    def _on_offline_mode(self, event: Event):
        """处理离线模式事件"""
        data = event.data
        reason = data.get("reason", "未知原因")

        # 显示离线模式通知
        from PySide6.QtWidgets import QMessageBox
        QMessageBox.warning(
            self,
            "离线降级模式",
            f"系统已进入离线降级模式:\n\n{reason}\n\n"
            "以下功能将不可用:\n"
            "- 数据下载\n"
            "- 实时行情推送\n"
            "- 交易网关\n"
            "- 组合投资\n\n"
            "本地历史数据查询仍可正常使用。"
        )

        # 在状态栏显示离线标记
        self.statusBar().showMessage(f"⚠️ 离线模式: {reason}", 0)

        # 禁用相关菜单和按钮
        self._disable_online_features()

    def _disable_online_features(self):
        """禁用在线功能"""
        # 禁用数据下载菜单
        self.data_download_action.setEnabled(False)
        # 禁用交易网关菜单
        self.trading_gateway_action.setEnabled(False)
        # 禁用组合投资菜单
        self.portfolio_action.setEnabled(False)
        # ... 其他在线功能 ...
```

### 7. 离线模式功能限制

**可用功能**:
- ✅ 本地历史数据查询（通过StorageManager）
- ✅ 数据可视化（MarketBoardService）
- ✅ 策略回测（StrategyCenterService，使用本地数据）
- ✅ AI助手（AIAssistantService）
- ✅ 系统监控（SystemManagerService）
- ✅ 文件监控（DataFileWatcher）

**不可用功能**:
- ❌ 数据下载（TDX数据源）
- ❌ 实时行情推送
- ❌ 交易网关（TradingGatewayService）
- ❌ 组合投资（PortfolioService）
- ❌ 四层数据融合（仅本地存储层可用）

### 8. 离线模式恢复

**自动恢复检测**（可选实现）:

```python
class ChinaStockEngine:
    def check_offline_recovery(self):
        """检查是否可以恢复在线模式"""
        if not self._offline_mode:
            return
        
        # 定期检测服务器池是否恢复
        recovery_result = self._validate_server_pool_and_test_speed()
        
        if not recovery_result.get("offline_mode"):
            # 服务器池已恢复
            logger.info("✅ TDX服务器池已恢复,退出离线模式")
            self.set_offline_mode(False, "")
            
            # 重新初始化UnifiedDataManager
            self.unified_data_manager = UnifiedDataManager(self)
            
            # 发布恢复事件
            self.event_engine.put(Event("eSystemOfflineMode", {
                "offline": False,
                "recovered": True,
                "timestamp": datetime.now()
            }))
```

---

**文档版本**: v2.1  
**创建日期**: 2025-11-02  
**更新时间**: 2025-11-02  
**维护者**: AI Assistant  
**状态**: ✅ 启动完整设计文档（包含标准输出示例 + Log输出设计 + 底层代码设计 + 降级机制设计）
