# 启动完整设计文档

## 📋 文档说明

**用途**: 完整的应用启动设计文档，包含标准输出示例、Log输出设计、底层代码设计和降级机制设计
**更新时间**: 2025-11-08
**对应版本**: v0.50
**日志系统**: logging_system.py v6.0（简化重构版）
**启动架构**: backend/startup v1.3（三进程新架构）
**原生扩展**: native_ipc、native_indicator、native_dataframe_ops、native_rpc_bridge（按可用性自动降级）

**文档内容**:
1. **Terminal标准输出**: 展示应用正常启动时Terminal的标准输出格式（格式和规则不变）
2. **Log输出设计**: 基于STAGE_NODE日志类型的分阶段日志输出设计（规则不变，实现更新为新架构）
3. **底层代码设计**: 启动流程的核心架构、类和时序设计（更新为新架构实现）
4. **TDX离线降级机制设计**: 当TDX服务器不可用时的自动降级机制（规则不变，实现更新为新架构）

---

## 🎯 Terminal标准输出

```
C:\Users\USER\Desktop\terminal_v0.50>python start_new.py

======================================================================
【阶段0: 环境准备】 (0-5%)
======================================================================

📍 阶段0: 环境准备开始
✅ 环境参数设置完成（DPI/路径/解释器/配置）
✅ 环境准备完成 (85ms)

======================================================================
【阶段1: 日志系统初始化】 (5-10%)
======================================================================

📍 阶段1: 日志系统初始化开始
✅ MemoryHandler已启用（拦截早期日志）
✅ LoggingHub创建完成
✅ 有序日志队列（OrderedLogQueue）已启用
✅ 事件日志流程（start_event_process）已启动
✅ MemoryHandler日志重放完成
✅ 日志系统就绪 (180ms)

======================================================================
【阶段2: Qt应用框架】 (10-20%)
======================================================================

📍 阶段2: Qt应用框架开始
✅ QApplication创建完成
✅ EventEngine预创建完成
✅ MainEngine预创建完成
✅ 主题系统加载完成
✅ Qt框架就绪 (450ms)

======================================================================
【阶段3: 后端服务初始化】 (20-90%) - 三进程并行
======================================================================

📍 阶段3: 后端服务初始化开始
✅ 并行启动 MonitorLauncherWorker / DataLauncherWorker / BackendInitializerWorker

┌──────────────────────────────────────────────────────────────────┐
│ 分支A: 监控进程（独立进程）                                     │
└──────────────────────────────────────────────────────────────────┘

📍 Level 0: monitor_system.py 启动
✅ MultiProcessLogCollector 接入 monitor_process 日志队列
✅ Level 1: IPC 管道就绪（monitor_alerts / monitor_status / monitor_query）
✅ Level 2: Watchdog与硬件监控就绪（心跳 2s）
✅ 监控进程完成

┌──────────────────────────────────────────────────────────────────┐
│ 分支B: 数据进程（独立进程）                                     │
└──────────────────────────────────────────────────────────────────┘

📍 Level 0: data_process_main.py 启动
✅ MultiProcessLogCollector 接入 data_process 日志队列
✅ Level 1: IPC 管道就绪（data_query / data_calculation）
✅ data_process_ready.signal 写入（含 PID / timestamp / pipes）
✅ Level 2: 数据服务装载完成，RPC握手成功（native_rpc_bridge 优先）

📍 8步缓存验证开始（数据进程执行，主进程按序回放）
📍 步骤1: 服务器池验证与测速 → ✅ 完成 (780ms) [进度 12%]
📍 步骤2: 初始化负载均衡器 → ✅ 完成 (120ms) [进度 18%]
📍 步骤3: 本地缓存有效性检查 → ✅ 完成 (90ms) [进度 24%]
📍 步骤4: 数据目录与索引扫描 → ✅ 完成 (260ms) [进度 40%]
📍 步骤5: 数据新鲜度检测 → ✅ 完成 (140ms) [进度 52%]
📍 步骤6: 异步本地数据索引 → ✅ 完成 (1200ms) [进度 78%]
📍 步骤7: 延迟数据更新检查 → ✅ 完成 (680ms) [进度 90%]
📍 步骤8: 启动文件监控 → ✅ 完成 (110ms) [进度 100%]
✅ 缓存验证流程结束（总耗时 3.1s）

┌──────────────────────────────────────────────────────────────────┐
│ 分支C: 主进程业务服务                                           │
└──────────────────────────────────────────────────────────────────┘

📍 业务服务骨架初始化开始
✅ TradingGateway / StrategyCenter / AIAssistant 注册完成
✅ Portfolio / MarketBoard / SystemManager 注册完成
✅ 远程依赖注入完成（健康检查通过）
✅ 业务服务初始化结束

📍 UI 预加载开始（与缓存验证并行）
✅ UI 预加载完成

✅ 后端服务完全就绪

======================================================================
【阶段4: UI主窗口】 (90-100%)
======================================================================

📍 阶段4: UI主窗口创建开始
✅ MainWindow创建完成
✅ 六大功能模块注册完成
✅ 增强状态栏初始化完成
✅ 主窗口显示
✅ UI就绪 (1.8s)

======================================================================
🎉 星辰金融终端启动成功！
======================================================================

启动统计:
  - 总耗时: 5.2s
  - 环境准备: 85ms
  - 日志系统: 180ms
  - Qt框架: 450ms
  - 监控进程: 2.3s (并行)
  - 数据进程缓存验证: 3.1s (并行)
  - UI主窗口: 1.8s

服务状态:
  - 后端服务: 7个运行中
  - 监控进程: 运行中
  - IPC管道: 正常（monitor_* / data_*）

系统资源:
  - CPU使用率: 5.2%
  - 内存占用: 420MB
  - 线程数: 25

======================================================================
应用已就绪，等待用户操作...
======================================================================
```

```

---

## 📊 输出特点说明

### 1. 分阶段展示
- 使用分隔线清晰区分不同阶段
- 每个阶段显示进度百分比
- 阶段完成后显示耗时统计

### 2. 层级结构
- 主阶段使用 `【】` 包裹
- 子阶段使用 `📍` 标记开始，`✅` 标记完成
- 详细信息使用缩进和 `├─`、`└─` 展示树形结构

### 3. 视觉元素
- ✅ 成功标记
- ⏳ 后台任务标记
- 📍 阶段节点标记
- 🎉 完成标记
- ├─、└─ 树形结构

### 4. 信息密度
- Terminal输出：仅显示关键节点和统计信息
- 事件日志文件：包含完整的DEBUG级别日志（位于 `logs/application_startup_YYYYMMDD_HHMMSS.log`）

### 5. 并行标注
- 明确标注哪些任务是并行执行的
- 显示并行任务各自的耗时
- 总耗时取最长分支的时间

### 6. 多进程一致性
- 通过 `MultiProcessLogCollector` 聚合子进程日志，Terminal 仍呈现单一时间轴。
- 子进程的 DEBUG/INFO 仅写入事件日志，避免多进程输出干扰用户观感。
- 就绪级别（Level 0/1/2）在 Terminal 中以阶段节点形式输出，便于对照设计指标。

---

## 🔧 日志级别对应关系

### Terminal输出（简洁）
- **STAGE_NODE** 日志类型：阶段节点、流程开始/完成
- **WARNING及以上**：警告、错误、严重问题（全量输出）
- **NOTIFICATION**：任务完成通知
- **ALERT**：系统告警

### 事件日志文件（详细）
- **DEBUG**：详细调试信息（如变量值、中间结果）
- **INFO**：常规信息日志（如函数调用、配置加载）
- **WARNING**：警告信息（如资源使用率高、降级运行）
- **ERROR**：错误信息（如初始化失败、异常捕获）
- **CRITICAL**：严重错误（如服务崩溃、数据损坏）

**详细规则说明请参考**：[统一日志系统说明文档.md](./backend/infrastructure/system_vnpy/统一日志系统说明文档.md) 的"十、启动输出规则"章节。

---

## 📁 相关事件日志文件

启动过程中自动生成的事件日志文件：

```
C:\Users\USER\Desktop\terminal_v0.50\logs\
├── terminal.log                                    # 常规日志（所有日志全量）
├── application_startup_YYYYMMDD_HHMMSS.log         # 主启动流程
├── monitor_process_startup_YYYYMMDD_HHMMSS.log    # 监控进程启动（如果启动）
└── backend_services_init_YYYYMMDD_HHMMSS.log      # 后端服务初始化（如果启动）
```

每个事件日志文件包含：
- **文件头**：流程说明、开始时间、元数据
- **完整日志**：DEBUG及以上所有级别，带图标和分隔线
- **异常堆栈**：完整的Python堆栈跟踪
- **文件尾**：流程结束时间、执行结果、日志统计

---

## ⚠️ 常见错误输出示例

### 错误示例1：监控进程启动失败

```
📍 监控进程启动开始
✅ monitor_system.py进程已启动 (PID: 12345)
❌ 创建native_ipc管道失败
  └─ 错误: OSError: [WinError 231] 所有的管道实例都在忙
  └─ 原因: 管道名称冲突或上次未正常关闭
  └─ 建议: 1. 检查是否有残留进程占用管道
          2. 尝试重启应用
          3. 查看详细日志: logs\monitor_process_startup_YYYYMMDD_HHMMSS.log
```

### 错误示例2：服务初始化失败

```
📍 阶段3.3: 数据服务初始化开始
✅ DataCenterService初始化完成
❌ 本地数据索引扫描失败
  └─ 错误: PermissionError: [WinError 5] 拒绝访问
  └─ 原因: 缺少管理员权限或文件被占用
  └─ 建议: 以管理员身份运行应用
❌ 数据服务就绪（降级模式）
  └─ 功能受限：仅可查询缓存数据，无法扫描本地文件
```

---

## 🎯 使用说明

### 对比工具
将实际Terminal输出与本文档对比，可快速定位启动问题：
1. 哪个阶段耗时异常
2. 哪个服务初始化失败
3. 是否有并行任务卡住

### 调试建议
1. **正常情况**：对照本文档，确认所有阶段都有对应输出
2. **异常情况**：查看错误标记 `❌`，根据建议操作
3. **性能问题**：对比耗时统计，找出瓶颈阶段
4. **深度分析**：查看对应的事件日志文件，获取完整上下文

---

## 📝 Log输出设计

### 核心策略：使用STAGE_NODE日志类型 + 事件日志流程管理（规则不变，新架构实现）

项目使用 `logging_system.py` 统一日志系统（v6.0简化重构版），具备以下功能：
1. **硬编码路由规则**：简化架构，无需配置文件（原四层路由规则已简化）
2. **LogType分类**：SYSTEM, PROGRESS, NOTIFICATION, ALERT, USER_FEEDBACK, DEBUG, **STAGE_NODE**
3. **事件日志管理**：`event_log_process()` 上下文管理器，自动创建流程日志文件
4. **控制台过滤**：只输出特定类型到Terminal（STAGE_NODE、NOTIFICATION、ALERT、WARNING及以上）
5. **有序日志队列**：`OrderedLogQueue` 确保并发启动时日志按顺序展示
6. **MemoryHandler缓冲**：缓冲日志系统初始化前的日志，确保不丢失

**启动输出规则详细说明请参考**：[统一日志系统说明文档.md](./backend/infrastructure/system_vnpy/统一日志系统说明文档.md) 的"八、启动输出规则"章节（已引用到该文档）。

### 实现方案：利用STAGE_NODE + 阶段切换 + 启动架构（新架构实现）

#### 1. 主启动流程（start_new.py - 新架构入口）

```python
import asyncio
from backend.startup import (
    StartupOrchestrator,
    EnvSetupStage,
    LoggingInitStage,
    QtFrameworkStage,
    BackendInitStage,
    UIActivationStage,
)

async def main():
    """主启动流程 - 使用StartupOrchestrator统一管理"""
    # 创建启动编排器
    orchestrator = StartupOrchestrator()

    # 添加所有启动阶段（按顺序）
    orchestrator.add_stage(EnvSetupStage())        # 阶段0: 环境准备
    orchestrator.add_stage(LoggingInitStage())      # 阶段1: 日志系统初始化
    orchestrator.add_stage(QtFrameworkStage())      # 阶段2: Qt框架初始化
    orchestrator.add_stage(BackendInitStage())      # 阶段3: 后端服务初始化
    orchestrator.add_stage(UIActivationStage())     # 阶段4: UI主窗口激活

    # 执行启动流程（自动处理日志和错误）
    result = await orchestrator.startup()

    if result.success:
        context = orchestrator.get_context()
        if context.app:
            return context.app.exec()
    else:
        print(f"❌ 启动失败: {result.message}")
        return 1
```

#### 2. 日志系统初始化阶段（backend/startup/stages/logging_init.py - 新架构）

```python
class LoggingInitStage(StartupStage):
    """日志系统初始化阶段 - 集成OrderedLogQueue和MemoryHandler"""

    async def _execute(self, context: StartupContext) -> StageResult:
        """执行日志系统初始化逻辑"""
        # 1. 设置MemoryHandler缓冲日志系统初始化前的日志
        memory_handler = self._setup_memory_logging()

        # 2. 初始化LoggingHub（核心逻辑在logging_system.py中）
        from backend.infrastructure.system_vnpy.logging_system import (
            get_logging_hub,
            start_event_process,
        )
        logging_hub = get_logging_hub()

        # 3. 启动事件日志流程（整个启动过程）
        start_event_process(
            "application_startup",
            metadata={
                "python_version": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
                "platform": sys.platform,
            },
        )

        # 4. 集成有序日志队列（启动阶段，已在logging_system.py中初始化）
        # 有序队列已在initialize_logging_hub_complete()中自动设置

        # 5. 重放MemoryHandler缓冲的日志
        memory_handler.setTarget(logging_hub)
        memory_handler.flush()

        # 6. 输出阶段1开始标记（使用STAGE_NODE）
        stage_logger = logging.getLogger("startup.stage")
        stage_logger.info("📍 阶段1: 日志系统初始化开始", extra={"log_type": "STAGE_NODE", "scenario": "application_startup"})
        stage_logger.info("✅ LoggingHub创建完成", extra={"log_type": "STAGE_NODE", "scenario": "application_startup"})
        stage_logger.info("✅ 路由规则系统初始化完成（硬编码规则）", extra={"log_type": "STAGE_NODE", "scenario": "application_startup"})
        stage_logger.info("✅ 事件日志Handler初始化完成", extra={"log_type": "STAGE_NODE", "scenario": "application_startup"})
        stage_logger.info("✅ MemoryHandler日志重放完成 (1250条)", extra={"log_type": "STAGE_NODE", "scenario": "application_startup"})
        stage_logger.info("✅ 有序日志队列已启用（启动阶段日志将按顺序输出）", extra={"log_type": "STAGE_NODE", "scenario": "application_startup"})
        stage_logger.info("✅ 日志系统就绪 (180ms)", extra={"log_type": "STAGE_NODE", "scenario": "application_startup"})

        return StageResult(success=True, message="日志系统初始化完成")
```

#### 3. 后端服务初始化阶段（backend/startup/stages/backend_init.py - 新架构）

```python
class BackendInitStage(StartupStage):
    """后端服务初始化阶段 - 链接前端和底层功能的关键文件"""

    async def _execute(self, context: StartupContext) -> StageResult:
        """执行后端初始化逻辑"""
        from backend.infrastructure.system_vnpy import get_logging_hub

        hub = get_logging_hub()
        hub.set_stage("backend_init")
        stage_logger = logging.getLogger("startup.stage")

        # 阶段3标题
        stage_logger.info("【阶段3: 后端服务初始化】 (20-90%) - 并行执行",
                         extra={"log_type": "STAGE_NODE"})
        stage_logger.info("📍 阶段3: 后端服务初始化开始", extra={"log_type": "STAGE_NODE"})

        # 1. 初始化VNPY核心框架
        await self._initialize_vnpy_core(context)

        # 2. 并行启动两个任务：
        #    - 任务A：启动监控进程（MonitorLauncherWorker）
        #    - 任务B：初始化后端服务（BackendInitializerWorker）
        monitor_worker = MonitorLauncherWorker()
        backend_worker = BackendInitializerWorker()

        # 并行执行
        monitor_task = monitor_worker.run(context)
        backend_task = backend_worker.run(context)
        monitor_result, backend_result = await asyncio.gather(monitor_task, backend_task)

        # 3. 执行8步缓存验证流程（CacheValidatorWorker）
        if context.china_stock_engine:
            cache_worker = CacheValidatorWorker()
            cache_result = await cache_worker.run(context)

        # 4. 初始化业务服务（阶段3.4-3.6）
        await self._initialize_business_services(context)

        stage_logger.info("✅ 后端服务完全就绪", extra={"log_type": "STAGE_NODE"})

        return StageResult(success=True, message="后端服务初始化完成")
```

#### 4. 缓存验证Worker（backend/startup/workers/cache_validator.py - 新架构）

```python
class CacheValidatorWorker(StartupWorker):
    """缓存验证Worker - 执行8步缓存验证流程"""

    async def _run(self, context: StartupContext) -> WorkerResult:
        """执行缓存验证逻辑"""
        engine = context.china_stock_engine
        stage_logger = logging.getLogger("startup.stage")

        # 显示分支B标题
        stage_logger.info("┌──────────────────────────────────────────────────────────────────┐",
                         extra={"log_type": "STAGE_NODE"})
        stage_logger.info("│ 分支B: 数据引擎初始化（smart_cache_validation_and_sensing）      │",
                         extra={"log_type": "STAGE_NODE"})
        stage_logger.info("└──────────────────────────────────────────────────────────────────┘",
                         extra={"log_type": "STAGE_NODE"})

        stage_logger.info("📍 阶段3.1: ChinaStockEngine初始化开始",
                         extra={"log_type": "STAGE_NODE"})
        stage_logger.info("📍 开始缓存验证与感知流程（8步）",
                         extra={"log_type": "STAGE_NODE"})

        # 创建步骤完成回调函数
        def step_callback(step_num: int, step_name: str, step_result: dict):
            """步骤完成回调函数 - 使用STAGE_NODE输出"""
            elapsed = step_result.get("elapsed", 0)
            progress = step_result.get("progress", 0)
            stage_logger.info(
                f"✅ 步骤{step_num}完成 ({elapsed:.0f}ms) [进度: {progress}%]",
                extra={"log_type": "STAGE_NODE"}
            )

        # 执行8步验证流程（在后台线程执行，因为_smart_cache_validation_and_sensing是同步方法）
        loop = asyncio.get_event_loop()
        result = await loop.run_in_executor(
            None,
            engine._smart_cache_validation_and_sensing,
            None,  # progress_callback
            step_callback,
        )

        # 检查是否进入离线模式
        if result.get("offline_mode"):
            stage_logger.warning(
                f"🔴 触发离线降级: {result.get('offline_reason', '未知原因')}",
                extra={"log_type": "STAGE_NODE"}
            )

        stage_logger.info("✅ 数据引擎完全就绪", extra={"log_type": "STAGE_NODE"})

        return WorkerResult(success=True, message="缓存验证完成", data={"validation_result": result})
```

#### 5. 8步缓存验证流程（backend/infrastructure/data_module_vnpy/core_engine.py - 规则不变）

```python
def _smart_cache_validation_and_sensing(
    self,
    progress_callback: Optional[Callable[[str, int], None]] = None,
    step_callback: Optional[Callable[[int, str, dict], None]] = None
) -> Dict[str, Any]:
    """智能缓存验证与感知（8步流程 + 离线降级）- 规则不变"""

    from backend.infrastructure.system_vnpy import get_logging_hub
    hub = get_logging_hub()
    logger = logging.getLogger("backend.data_module.cache_validation")

    # 步骤1: 服务器池验证与测速
    hub.set_stage("cache_validation_step1")
    logger.info("┌─ 步骤1: 服务器池验证与测速 ─┐", extra={"log_type": "STAGE_NODE"})
    # ... 验证逻辑 ...

    # 离线降级检查点
    if step1_result.get("offline_mode"):
        logger.warning("🔴 触发离线降级: " + step1_result.get("offline_reason", ""),
                     extra={"log_type": "STAGE_NODE"})
        goto_step_8 = True
    else:
        goto_step_8 = False

    if not goto_step_8:
        # 步骤2-7: 正常验证流程
        # ... 步骤2-7的实现 ...

    # 步骤8: 启动文件监控
    hub.set_stage("cache_validation_step8")
    logger.info("┌─ 步骤8: 启动文件监控 ─┐", extra={"log_type": "STAGE_NODE"})
    # ... 文件监控逻辑 ...
    logger.info("✅ 步骤8完成", extra={"log_type": "STAGE_NODE"})

    return {"success": True, "offline_mode": offline_mode, ...}
```

### 为什么这个方案有效？

**1. Terminal输出简洁**：
- `STAGE_NODE` 日志类型会自动输出到Terminal（在 `_console_enabled_types` 中）
- 其他DEBUG/INFO日志不会输出到Terminal，只写入事件日志文件
- 效果：Terminal只显示流程节点，不刷屏

**2. 事件日志文件完整**：
- `event_log_process("application_startup")` 会创建专门的事件日志文件
- 所有DEBUG、INFO、WARNING、ERROR、CRITICAL日志都写入该文件
- 效果：分析时有完整上下文

**3. 自动分阶段**：
- `hub.set_stage()` 切换阶段后，路由规则自动应用该阶段的配置
- 效果：不同阶段的日志可以有不同的路由策略

---

## 🏗️ 底层代码设计（新架构实现）

### 架构概述（新架构）

启动流程的核心架构分为四个层次（新架构）：
1. **启动编排层（StartupOrchestrator）**：`backend/startup/orchestrator.py` 负责统一管理所有启动阶段
2. **阶段层（Stages）**：`backend/startup/stages/` 负责分阶段初始化（EnvSetupStage、LoggingInitStage、QtFrameworkStage、BackendInitStage、UIActivationStage）
3. **工作线程层（Workers）**：`backend/startup/workers/` 负责后台任务（BackendInitializerWorker、MonitorLauncherWorker、CacheValidatorWorker）
4. **业务层（Services）**：`backend/services/` 负责业务逻辑（DataCenterService、TradingGatewayService等）

### 1. StartupOrchestrator设计（启动编排器）

**文件位置**: `backend/startup/orchestrator.py`

**核心类设计**:

```python
from typing import List, Dict
import asyncio
import logging
from backend.startup.context import StartupContext
from backend.startup.stages.base import StartupStage, StageResult

class StartupOrchestrator:
    """启动编排器 - 统一管理所有启动阶段

    职责：
    - 管理所有启动阶段
    - 协调阶段间的依赖关系
    - 处理阶段错误和降级
    - 统一日志输出
    """

    def __init__(self):
        """初始化启动编排器"""
        self.stages: List[StartupStage] = []
        self.context = StartupContext()
        self.logger = logging.getLogger("backend.startup.orchestrator")

    def add_stage(self, stage: StartupStage):
        """添加启动阶段

        Args:
            stage: 启动阶段实例
        """
        self.stages.append(stage)
        self.logger.debug(f"添加启动阶段: {stage.name}")

    async def startup(self) -> StartupResult:
        """执行启动流程

        Returns:
            StartupResult: 启动结果
        """
        start_time = time.time()
        stage_results: Dict[str, StageResult] = {}

        # 执行所有阶段
        for stage in self.stages:
            try:
                result = await stage.execute(self.context)
                stage_results[stage.name] = result

                if not result.success:
                    # 处理阶段失败
                    self.logger.error(f"阶段 {stage.name} 失败: {result.message}")
                    return StartupResult(success=False, message=result.message, ...)
            except Exception as e:
                self.logger.exception(f"阶段 {stage.name} 异常: {e}")
                return StartupResult(success=False, message=str(e), ...)

        elapsed_ms = (time.time() - start_time) * 1000
        return StartupResult(success=True, elapsed_ms=elapsed_ms, ...)
```

### 2. CacheValidatorWorker设计（新架构）

**文件位置**: `backend/startup/workers/cache_validator.py`

**核心类设计**:

```python
import logging
import asyncio
from typing import Dict, Any

from backend.startup.workers.base import StartupWorker, WorkerResult
from backend.startup.context import StartupContext

class CacheValidatorWorker(StartupWorker):
    """缓存验证Worker - 执行8步缓存验证流程（新架构）

    职责：
    - 封装_smart_cache_validation_and_sensing的8步验证流程
    - 使用async/await支持异步执行
    - 报告进度（通过STAGE_NODE日志）

    优势（新架构）：
    - ✅ async/await支持，更清晰的异步代码
    - ✅ 集成到启动架构中，统一管理
    - ✅ 通过StartupContext访问依赖
    - ✅ 自动错误处理和报告
    """

    def __init__(self):
        """初始化缓存验证Worker"""
        super().__init__(
            name="cache_validator",
            description="缓存验证Worker - 执行8步验证流程",
        )
        self._cancelled = False

    async def _run(self, context: StartupContext) -> WorkerResult:
        """执行缓存验证逻辑（异步）

        Args:
            context: 启动上下文

        Returns:
            WorkerResult: Worker执行结果
        """
        start_time = time.time()

        try:
            # 获取ChinaStockEngine
            if not context.china_stock_engine:
                raise RuntimeError("ChinaStockEngine未初始化")

            engine = context.china_stock_engine
            stage_logger = logging.getLogger("startup.stage")

            # 显示分支B标题
            stage_logger.info("┌──────────────────────────────────────────────────────────────────┐",
                             extra={"log_type": "STAGE_NODE"})
            stage_logger.info("│ 分支B: 数据引擎初始化（smart_cache_validation_and_sensing）      │",
                             extra={"log_type": "STAGE_NODE"})
            stage_logger.info("└──────────────────────────────────────────────────────────────────┘",
                             extra={"log_type": "STAGE_NODE"})

            # 创建步骤完成回调函数
            def step_callback(step_num: int, step_name: str, step_result: dict):
                """步骤完成回调函数 - 使用STAGE_NODE输出"""
                elapsed = step_result.get("elapsed", 0)
                progress = step_result.get("progress", 0)
                stage_logger.info(
                    f"✅ 步骤{step_num}完成 ({elapsed:.0f}ms) [进度: {progress}%]",
                    extra={"log_type": "STAGE_NODE"}
                )

            # 执行8步验证流程（在后台线程执行，因为_smart_cache_validation_and_sensing是同步方法）
            loop = asyncio.get_event_loop()
            result = await loop.run_in_executor(
                None,
                engine._smart_cache_validation_and_sensing,
                None,  # progress_callback
                step_callback,
            )

            # 检查是否进入离线模式
            if result.get("offline_mode"):
                stage_logger.warning(
                    f"🔴 触发离线降级: {result.get('offline_reason', '未知原因')}",
                    extra={"log_type": "STAGE_NODE"}
                )

            elapsed_ms = (time.time() - start_time) * 1000
            return WorkerResult(
                success=result.get("success", False),
                message="缓存验证完成" if result.get("success", False) else "缓存验证失败",
                elapsed_ms=elapsed_ms,
                data={"validation_result": result},
            )

        except Exception as e:
            elapsed_ms = (time.time() - start_time) * 1000
            self.logger.exception(f"缓存验证Worker异常: {e}")
            return WorkerResult(
                success=False,
                message=f"缓存验证Worker异常: {str(e)}",
                elapsed_ms=elapsed_ms,
                error=e,
            )

    def cancel(self):
        """取消验证（外部调用）"""
        self._cancelled = True
        self.logger.warning("⚠️ 缓存验证被取消")
```

### 2. _smart_cache_validation_and_sensing核心流程设计

**文件位置**: `backend/infrastructure/data_module_vnpy/core_engine.py`

**完整方法签名**:

```python
def _smart_cache_validation_and_sensing(
    self,
    progress_callback: Optional[Callable[[str, int], None]] = None,
    step_callback: Optional[Callable[[int, str, dict], None]] = None
) -> Dict[str, Any]:
    """智能缓存验证与感知（8步流程 + 离线降级）

    这是启动项的核心主干流程,负责:
    1. 验证和初始化所有缓存
    2. 初始化核心组件(LoadBalancer, UnifiedDataManager等)
    3. TDX离线降级检测
    4. 启动文件监控

    Args:
        progress_callback: 进度回调函数(description, percent)
        step_callback: 步骤完成回调(step_num, step_name, result)

    Returns:
        Dict[str, Any]: {
            "success": bool,
            "offline_mode": bool,
            "offline_reason": str,
            "steps_completed": int,
            "step_results": List[dict],
            "total_time": float
        }

    异常:
        不抛出异常,所有错误封装在返回值中
    """

    start_time = time.time()
    step_results = []
    offline_mode = False
    offline_reason = ""

    # 辅助函数
    def _progress(desc: str, pct: int):
        if progress_callback:
            progress_callback(desc, pct)

    def _step_done(num: int, name: str, result: dict):
        step_results.append(result)
        if step_callback:
            step_callback(num, name, result)

    try:
        # ========== 步骤1: 服务器池缓存验证与测速 ==========
        _progress("步骤1/8: 验证服务器池缓存并测速...", 5)
        step1_result = self._validate_server_pool_and_test_speed()
        step1_result["elapsed"] = (time.time() - start_time) * 1000
        step1_result["progress"] = 12
        _step_done(1, "服务器池验证与测速", step1_result)

        # 离线降级检查点
        if step1_result.get("offline_mode"):
            offline_mode = True
            offline_reason = step1_result.get("offline_reason")
            logger.critical(f"🔴 触发离线降级: {offline_reason}")
            # 跳转到步骤8
            _progress("离线模式: 跳过步骤2-7,直接启动文件监控", 90)
            goto_step_8 = True
        else:
            goto_step_8 = False
            # 初始化LoadBalancer
            _progress("步骤1/8: 初始化LoadBalancer...", 10)
            self.load_balancer = LoadBalancer.get_instance(self.event_engine)

        if not goto_step_8:
            # ========== 步骤2: 获取当前日期(网络时间) ==========
            _progress("步骤2/8: 获取当前日期(网络时间)...", 15)
            step2_result = self._get_current_date()
            step2_result["elapsed"] = (time.time() - start_time) * 1000
            step2_result["progress"] = 25
            _step_done(2, "获取当前日期", step2_result)
            current_date = step2_result.get("current_date")

            # ========== 步骤3: 验证交易日历缓存 ==========
            _progress("步骤3/8: 验证交易日历缓存...", 25)
            step3_result = self._validate_trade_calendar_cache(current_date)
            step3_result["elapsed"] = (time.time() - start_time) * 1000
            step3_result["progress"] = 37
            _step_done(3, "验证交易日历缓存", step3_result)

            # ========== 步骤4: 验证品种列表缓存 ==========
            _progress("步骤4/8: 验证品种列表缓存...", 35)
            step4_result = self._validate_symbol_list_cache(current_date)
            step4_result["elapsed"] = (time.time() - start_time) * 1000
            step4_result["progress"] = 50
            _step_done(4, "验证品种列表缓存", step4_result)

            # 初始化SymbolLoader
            if not step4_result.get("cache_valid"):
                _progress("步骤4/8: 重新加载品种列表...", 40)
                self.symbol_loader = SymbolLoader(self.event_engine)
                await self.symbol_loader.reload_and_classify_async()

            # ========== 步骤5: 验证IPO日期缓存 ==========
            _progress("步骤5/8: 验证IPO日期缓存...", 50)
            step5_result = self._validate_ipo_cache(current_date)
            step5_result["elapsed"] = (time.time() - start_time) * 1000
            step5_result["progress"] = 62
            _step_done(5, "验证IPO日期缓存", step5_result)

            # ========== 步骤6: 更新本地数据索引 ==========
            _progress("步骤6/8: 更新本地数据索引...", 65)
            step6_result = self._update_local_data_index()
            step6_result["elapsed"] = (time.time() - start_time) * 1000
            step6_result["progress"] = 75
            _step_done(6, "更新本地数据索引", step6_result)

            # 初始化StorageManager
            _progress("步骤6/8: 初始化StorageManager...", 70)
            self.storage_manager = StorageManager()

            # ========== 步骤7: 检查数据更新状态 ==========
            _progress("步骤7/8: 检查数据更新状态...", 75)
            step7_result = self._check_data_update_status()
            step7_result["elapsed"] = (time.time() - start_time) * 1000
            step7_result["progress"] = 87
            _step_done(7, "检查数据更新状态", step7_result)

            # 初始化DataSensor和UnifiedDataManager
            _progress("步骤7/8: 初始化DataSensor和UnifiedDataManager...", 85)
            self.data_sensor = DataSensor(self.event_engine)
            self.unified_data_manager = UnifiedDataManager(self)

        # ========== 步骤8: 启动文件监控 ==========
        _progress("步骤8/8: 启动文件监控...", 90)
        step8_result = self._start_file_watcher()
        step8_result["elapsed"] = (time.time() - start_time) * 1000
        step8_result["progress"] = 100
        _step_done(8, "启动文件监控", step8_result)

        # 完成
        _progress("缓存验证与感知完成", 100)
        elapsed_time = time.time() - start_time

        return {
            "success": True,
            "offline_mode": offline_mode,
            "offline_reason": offline_reason,
            "steps_completed": len(step_results),
            "step_results": step_results,
            "total_time": elapsed_time
        }

    except Exception as e:
        logger.exception("❌ 缓存验证与感知失败: %s", e)
        return {
            "success": False,
            "offline_mode": False,
            "offline_reason": "",
            "steps_completed": len(step_results),
            "step_results": step_results,
            "error": str(e)
        }
```

### 3. BackendInitStage设计（后端服务初始化阶段 - 新架构）

**文件位置**: `backend/startup/stages/backend_init.py`

**核心类设计**:

```python
import asyncio
import logging
from backend.startup.stages.base import StartupStage, StageResult
from backend.startup.context import StartupContext
from backend.startup.workers.backend_initializer import BackendInitializerWorker
from backend.startup.workers.cache_validator import CacheValidatorWorker
from backend.startup.workers.monitor_launcher import MonitorLauncherWorker

class BackendInitStage(StartupStage):
    """后端服务初始化阶段 - 链接前端和底层功能的关键文件（新架构）

    职责：
    - 初始化底层基础设施（EventEngine、MainEngine、ChinaStockEngine）
    - 调用Worker初始化后端服务和监控进程（并行）
    - 将服务注册到ServiceManager（暴露给前端）
    - 管理服务依赖关系
    """

    async def _execute(self, context: StartupContext) -> StageResult:
        """执行后端初始化逻辑"""
        from backend.infrastructure.system_vnpy import get_logging_hub

        hub = get_logging_hub()
        hub.set_stage("backend_init")
        stage_logger = logging.getLogger("startup.stage")

        # 阶段3标题
        stage_logger.info("【阶段3: 后端服务初始化】 (20-90%) - 并行执行",
                         extra={"log_type": "STAGE_NODE"})
        stage_logger.info("📍 阶段3: 后端服务初始化开始", extra={"log_type": "STAGE_NODE"})

        # 1. 初始化VNPY核心框架
        await self._initialize_vnpy_core(context)

        # 2. 并行启动两个任务：
        #    - 任务A：启动监控进程（MonitorLauncherWorker）
        #    - 任务B：初始化后端服务（BackendInitializerWorker）
        monitor_worker = MonitorLauncherWorker()
        backend_worker = BackendInitializerWorker()

        # 并行执行
        monitor_task = monitor_worker.run(context)
        backend_task = backend_worker.run(context)
        monitor_result, backend_result = await asyncio.gather(monitor_task, backend_task)

        # 3. 执行8步缓存验证流程（CacheValidatorWorker）
        if context.china_stock_engine:
            cache_worker = CacheValidatorWorker()
            cache_result = await cache_worker.run(context)

        # 4. 初始化业务服务（阶段3.4-3.6）
        await self._initialize_business_services(context)

        stage_logger.info("✅ 后端服务完全就绪", extra={"log_type": "STAGE_NODE"})

        return StageResult(success=True, message="后端服务初始化完成")
```

### 4. 服务初始化时序设计（新架构，最佳实践版）

**目标与原则**
- UI首屏尽快激活（1–2s），关键路径不阻塞。
- 三进程职责清晰：UI/业务、数据服务、系统监控独立运行。
- 原生扩展渐进增强：优先使用`native_ipc`/`native_indicator`/`native_dataframe_ops`/`native_rpc_bridge`，不可用时自动降级到Python实现。
- 强可观测性：分阶段`STAGE_NODE`日志、统一`LoggingHub`路由、多进程日志汇聚。
- 失败可预期：严格超时与重试、健康检查、离线降级策略一致。

**初始化总览时序（最佳实践）**

```
StartupOrchestrator
    │
    ├─ 阶段0: EnvSetupStage（<100ms）
    │   ├─ 读取配置与特性开关（如 `FAST_STARTUP`）
    │   ├─ 探测原生扩展可用性（IPC/Indicator/DataFrame/RPC）
    │   └─ 设置降级标志与平台参数（Windows/IOCP/CPU亲和）
    │
    ├─ 阶段1: LoggingInitStage（<500ms）
    │   ├─ 初始化 `LoggingHub` + MemoryHandler（缓冲早期日志）
    │   ├─ 启动事件日志流程（`start_event_process`）
    │   ├─ 建立多进程日志汇聚：优先 `native_ipc.AsyncIPCPipe`，回退 Manager.Queue
    │   └─ 重放缓冲日志，启用按阶段路由（`STAGE_NODE` 仅入Terminal）
    │
    ├─ 阶段2: QtFrameworkStage（<500ms）
    │   ├─ 创建 `QApplication`
    │   ├─ 预创建 `EventEngine`/`MainEngine`
    │   └─ 加载主题系统与轻量UI资源
    │
    ├─ 阶段3: BackendInitStage（三进程，2–5s）
    │   ├─ 0) 验证阶段2实例（不重复创建）
    │   ├─ 1) 并行三条子流程（推荐 Python 3.11 `TaskGroup`）
    │   │   ├─→ MonitorLauncherWorker
    │   │   │     Level 0: 启动监控进程、记录PID
    │   │   │     Level 1: 建立 `monitor_alerts/status/query` IPC管道
    │   │   │     Level 2: Watchdog/硬件监控就绪，发布心跳
    │   │   ├─→ DataLauncherWorker
    │   │   │     Level 0: 启动数据进程，写入 `ready.signal`
    │   │   │     Level 1: 建立 `data_query/data_calculation` IPC管道
    │   │   │     Level 2: 装载数据服务，完成 RPC 握手（`native_rpc_bridge` 优先）
    │   │   └─→ BackendInitializerWorker
    │   │         初始化交易/策略/辅助服务骨架；
    │   │         等待数据进程 Level 2 后进行远程依赖注入。
    │   ├─ 2) MultiProcessLogCollector 桥接子进程日志（队列回压与速率限制）
    │   ├─ 3) 缓存验证在数据进程执行（RPC触发、主进程仅消费进度事件）
    │   ├─ 4) 远程依赖注入与健康检查（失败即标记降级）
    │   └─ 5) UI 预加载任务（异步，不阻塞主线程）
    │
    └─ 阶段4: UIActivationStage（1–2s）
        ├─ 创建 MainWindow、注册六大模块
        ├─ 显示窗口（首屏尽快出现）
        └─ 功能界面按就绪信号渐进启用
```

**并行与容错策略**
- 并行执行采用 `asyncio.TaskGroup`（3.11+），统一取消语义：任一子流程失败即取消其他并收敛错误。
- 关键等待点使用“就绪等级”事件：`Level 2` 代表跨进程RPC握手完成，方可注入依赖。
- 默认超时建议：监控就绪 `5s`，数据就绪 `15s`；超时退避重试（指数退避，最多3次）。
- 所有阶段输出 `STAGE_NODE` 日志节点，失败路径必须包含原因与下一步动作（重试/降级/终止）。

**跨进程日志与RPC最佳实践**
- 日志汇聚优先 `native_ipc.AsyncIPCPipe`，不可用时回退到 `multiprocessing.Manager.Queue`。
- 统一令牌与通道命名：`LOGGING_QUEUE_TOKEN`、`DATA_QUERY_PIPE`、`DATA_CALC_PIPE`、`MONITOR_STATUS_PIPE`。
- RPC 首选 `native_rpc_bridge`（二进制序列化 + 零拷贝），不可用时使用轻量JSON协议回退。

**远程依赖注入与健康检查**
- `ServiceManager` 注册远程客户端（DataCenter/UnifiedDataManager等），并维护健康状态。
- 周期性 `health_check` + 首次强制 `probe`；失败时标记降级并在UI显式提示。
- 离线模式下禁用交易网关与组合投资服务，仅保留本地数据与策略模拟。

**UI预加载与资源管理**
- 预加载不阻塞UI：图标/主题/静态资源在后台加载，首屏优先绘制。
- 数据预取遵循配额与速率限制，避免首屏卡顿；进度通过 `BackgroundTaskIndicator` 呈现。

**原生扩展加载策略**
- 延迟加载、按需加载：首次调用相关能力时再导入原生模块。
- 能力检测：`IPC_AVAILABLE`、`NATIVE_INDICATOR_AVAILABLE`、`DATAFRAME_OPS_AVAILABLE`、`importorskip('native_rpc_bridge')`。
- 统一指标与性能计量：记录原生/回退路径的耗时与吞吐，纳入负载均衡评估。

**示例伪代码（Python 3.11 TaskGroup）**

```python
async def run_backend_init(context: StartupContext) -> StageResult:
    stage_logger = logging.getLogger("startup.stage")
    from asyncio import TaskGroup

    async with TaskGroup() as tg:
        m = tg.create_task(MonitorLauncherWorker().run(context))
        d = tg.create_task(DataLauncherWorker().run(context))
        b = tg.create_task(BackendInitializerWorker().run(context))

    # 等待数据进程Level 2（RPC握手完成）
    await context.wait_for_level(process="data", level=2, timeout=15.0)

    # 远程依赖注入与健康检查
    await inject_remote_dependencies(context)
    await run_health_checks(context, timeout=5.0)

    # 启动UI预加载（后台进行）
    context.ui_preload_task = asyncio.create_task(run_ui_preload(context))

    stage_logger.info("✅ 后端服务完全就绪", extra={"log_type": "STAGE_NODE"})
    return StageResult(success=True, message="后端服务初始化完成")
```

**兼容说明**
- Python < 3.11 时使用 `asyncio.gather` 替代 `TaskGroup`；取消语义需手动实现。
- 当 `native_ipc` 不可用时，优先使用 Manager.Queue；在生产环境建议尽快编译并启用原生IPC以获得更低延迟与更高吞吐。

注：Terminal标准输出的格式与示例将于下一版集中更新，本次仅更新服务初始化时序设计。

## 🔄 TDX离线降级机制设计（规则不变，新架构实现）

### 概述

当TDX服务器池全部不可用时，系统自动进入离线降级模式，跳过步骤2-7，直接执行步骤8，确保系统可以正常启动并有限运行。

**规则不变**：降级触发条件、离线模式状态管理、流程跳转、服务降级策略、UI层降级提示等规则均保持不变。

**实现更新**：在新架构中，离线降级通过 `CacheValidatorWorker` 检测并通过 `step_callback` 回调函数报告，`BackendInitStage` 处理Worker结果。

### 1. 降级触发条件

**主触发点**: 步骤1服务器池验证失败

**判断逻辑**:

```python
def _validate_server_pool_and_test_speed(self) -> Dict[str, Any]:
    """步骤1: 服务器池缓存验证与测速

    Returns:
        Dict[str, Any]: {
            "success": bool,
            "ipv4_available": bool,
            "ipv6_available": bool,
            "offline_mode": bool,
            "offline_reason": str
        }
    """
    try:
        from .load_balancer import ServerPoolManager

        # 初始化服务器池管理器
        pool_manager = ServerPoolManager.get_instance()

        # 测试IPv4服务器池
        ipv4_results = pool_manager.test_servers(pool_type="ipv4", max_workers=4)
        ipv4_available = len([s for s in ipv4_results if s.get("latency", 999) < 500]) > 0

        # 测试IPv6服务器池
        ipv6_results = pool_manager.test_servers(pool_type="ipv6", max_workers=4)
        ipv6_available = len([s for s in ipv6_results if s.get("latency", 999) < 500]) > 0

        # 判断是否需要进入离线模式
        if not ipv4_available and not ipv6_available:
            logger.critical("🔴 所有TDX服务器不可用,进入离线降级模式")
            return {
                "success": False,
                "ipv4_available": False,
                "ipv6_available": False,
                "offline_mode": True,
                "offline_reason": "所有TDX服务器(IPv4/IPv6)不可用"
            }

        # 至少一个服务器池可用
        logger.info(f"✅ TDX服务器池可用: IPv4={ipv4_available}, IPv6={ipv6_available}")
        return {
            "success": True,
            "ipv4_available": ipv4_available,
            "ipv6_available": ipv6_available,
            "offline_mode": False
        }

    except Exception as e:
        logger.exception("❌ 服务器池验证失败: %s", e)
        return {
            "success": False,
            "ipv4_available": False,
            "ipv6_available": False,
            "offline_mode": True,
            "offline_reason": f"服务器池验证异常: {e}"
        }
```

**触发条件**:
- IPv4和IPv6服务器池均不可用（所有服务器延迟 > 500ms）
- 服务器池验证异常

### 2. 离线模式状态管理

**在ChinaStockEngine中添加**:

```python
class ChinaStockEngine:
    """中国股票数据引擎（核心引擎）"""

    def __init__(self, main_engine: MainEngine, event_engine: EventEngine):
        # ... 现有代码 ...

        # 离线模式标记
        self._offline_mode: bool = False
        self._offline_reason: str = ""

    def is_offline_mode(self) -> bool:
        """检查是否处于离线模式"""
        return self._offline_mode

    def get_offline_reason(self) -> str:
        """获取离线原因"""
        return self._offline_reason

    def set_offline_mode(self, offline: bool, reason: str = ""):
        """设置离线模式（内部使用）"""
        self._offline_mode = offline
        self._offline_reason = reason

        if offline:
            logger.warning(f"⚠️ 系统已进入离线降级模式: {reason}")
            # 发布离线模式事件
            self.event_engine.put(Event("eSystemOfflineMode", {
                "offline": True,
                "reason": reason,
                "timestamp": datetime.now()
            }))
```

### 3. 离线模式流程跳转（规则不变，新架构实现）

**规则不变**：在`_smart_cache_validation_and_sensing`中实现（文件位置：`backend/infrastructure/data_module_vnpy/core_engine.py`）

**新架构实现**：通过`CacheValidatorWorker`执行，离线降级通过`step_callback`回调函数报告

```python
# 步骤1: 服务器池验证与测速（规则不变）
step1_result = self._validate_server_pool_and_test_speed()

# 离线降级检查点（规则不变）
if step1_result.get("offline_mode"):
    offline_mode = True
    offline_reason = step1_result.get("offline_reason")
    logger.critical(f"🔴 触发离线降级: {offline_reason}")

    # 设置离线模式状态
    self.set_offline_mode(True, offline_reason)

    # 跳转到步骤8
    _progress("离线模式: 跳过步骤2-7,直接启动文件监控", 90)
    goto_step_8 = True
else:
    goto_step_8 = False
    # 初始化LoadBalancer
    self.load_balancer = LoadBalancer.get_instance(self.event_engine)

if not goto_step_8:
    # 步骤2-7: 正常验证流程
    # ... 执行步骤2-7 ...

# 步骤8: 启动文件监控（正常模式和离线模式都执行）
step8_result = self._start_file_watcher()
```

**新架构中离线降级的处理**（`backend/startup/workers/cache_validator.py`）：

```python
class CacheValidatorWorker(StartupWorker):
    async def _run(self, context: StartupContext) -> WorkerResult:
        """执行缓存验证逻辑（异步）"""
        engine = context.china_stock_engine
        stage_logger = logging.getLogger("startup.stage")

        # 创建步骤完成回调函数
        def step_callback(step_num: int, step_name: str, step_result: dict):
            """步骤完成回调函数 - 使用STAGE_NODE输出"""
            elapsed = step_result.get("elapsed", 0)
            progress = step_result.get("progress", 0)
            stage_logger.info(
                f"✅ 步骤{step_num}完成 ({elapsed:.0f}ms) [进度: {progress}%]",
                extra={"log_type": "STAGE_NODE"}
            )

        # 执行8步验证流程（在后台线程执行）
        loop = asyncio.get_event_loop()
        result = await loop.run_in_executor(
            None,
            engine._smart_cache_validation_and_sensing,
            None,  # progress_callback
            step_callback,
        )

        # 检查是否进入离线模式（新架构处理）
        if result.get("offline_mode"):
            stage_logger.warning(
                f"🔴 触发离线降级: {result.get('offline_reason', '未知原因')}",
                extra={"log_type": "STAGE_NODE"}
            )

        return WorkerResult(success=True, data={"validation_result": result})
```

**离线模式输出示例**:

```
┌─ 步骤1: 服务器池验证与测速 ─┐
│ ⏳ 正在测速IPv4服务器池...（20个服务器）
│ ❌ IPv4服务器池不可用: 0个可用，所有服务器超时
│ ⏳ 正在测速IPv6服务器池...（5个服务器）
│ ❌ IPv6服务器池不可用: 0个可用，所有服务器超时
│ 🔴 所有TDX服务器(IPv4/IPv6)不可用
└─────────────────────────────┘
✅ 步骤1完成 (3000ms) [进度: 12%]
🔴 触发离线降级: 所有TDX服务器(IPv4/IPv6)不可用

⏳ 离线模式: 跳过步骤2-7,直接启动文件监控

┌─ 步骤8: 启动文件监控 ─┐
│ ⏳ 启动DataFileWatcher...
│ ✅ 文件监控器已启动
│   - 监控目录: C:\Users\USER\Desktop\terminal_v0.50\data\kline
│   - 监控模式: 实时变更检测
└─────────────────────────────┘
✅ 步骤8完成 (100ms) [进度: 100%]

✅ 缓存验证与感知流程完成 (总耗时: 3.1s)
⚠️ 系统已进入离线降级模式: 所有TDX服务器(IPv4/IPv6)不可用
✅ 数据引擎完全就绪（离线模式）
```

### 4. UnifiedDataManager离线降级

**修改`data_runtime.py`中的UnifiedDataManager**:

```python
class UnifiedDataManager:
    """统一数据管理器（四层数据融合）"""

    def __init__(self, china_stock_engine: ChinaStockEngine):
        self.engine = china_stock_engine
        self.event_engine = china_stock_engine.event_engine

        # 检查离线模式
        self.offline_mode = china_stock_engine.is_offline_mode()

        if self.offline_mode:
            logger.warning("⚠️ UnifiedDataManager以离线模式初始化")
            # 只初始化本地存储层
            self.storage_manager = china_stock_engine.storage_manager
            self.preload_service = None  # 禁用预加载
            self.tdx_data_source = None  # 禁用TDX数据源
            self.virtual_data_source = None  # 禁用虚拟数据源
        else:
            # 正常初始化所有层
            self.storage_manager = china_stock_engine.storage_manager
            self.preload_service = PreloadService(self.storage_manager)
            self.tdx_data_source = TdxDataSource(self.event_engine)
            self.virtual_data_source = VirtualDataSource(self.event_engine)

    async def query_kline_async(
        self,
        symbol: str,
        interval: str,
        start_date: Optional[str] = None,
        end_date: Optional[str] = None
    ) -> Optional[pd.DataFrame]:
        """异步查询K线数据（四层融合 or 离线模式）"""

        if self.offline_mode:
            # 离线模式:仅查询本地存储
            logger.debug(f"[离线模式] 仅查询本地数据: {symbol}/{interval}")
            return await self.storage_manager.load_data_async(
                symbol, interval, start_date, end_date
            )

        # 正常模式:四层融合查询
        # Layer 1: 预加载缓存
        if self.preload_service:
            cached = self.preload_service.get_from_cache(symbol, interval)
            if cached is not None:
                logger.debug(f"[预加载缓存] 命中: {symbol}/{interval}")
                return self._filter_by_date(cached, start_date, end_date)

        # Layer 2: 本地存储
        df = await self.storage_manager.load_data_async(symbol, interval, start_date, end_date)
        if df is not None and not df.empty:
            logger.debug(f"[本地存储] 命中: {symbol}/{interval}")
            # 更新预加载缓存
            if self.preload_service:
                self.preload_service.add_to_cache(symbol, interval, df)
            return df

        # Layer 3 & 4: TDX数据源和实时推送
        # ... 正常模式逻辑 ...
```

### 5. 服务降级策略（规则不变，新架构实现）

**规则不变**：服务初始化器根据离线模式状态决定是否初始化网络依赖服务。

**新架构实现**：通过`BackendInitializerWorker`调用`ServiceInitializer`（文件位置：`backend/startup/workers/backend_initializer.py`）

```python
# ServiceInitializer实现（规则不变，backend/core/base.py）
class ServiceInitializer:
    """服务初始化器"""

    def initialize_all_services(
        self,
        event_engine: EventEngine,
        main_engine: MainEngine,
        china_stock_engine: ChinaStockEngine,
        progress_callback=None,
        fast_startup: bool = True
    ) -> Dict[str, bool]:
        """初始化所有服务（支持离线降级）"""

        results = {}
        offline_mode = china_stock_engine.is_offline_mode()

        if offline_mode:
            logger.warning("⚠️ 离线模式: 部分服务将跳过初始化")

        # 核心服务(始终初始化)
        results["data_center"] = self._init_data_center(event_engine, china_stock_engine)
        results["market_board"] = self._init_market_board(event_engine, china_stock_engine)
        results["strategy_center"] = self._init_strategy_center(event_engine)
        results["ai_assistant"] = self._init_ai_assistant(event_engine)
        results["system_manager"] = self._init_system_manager(event_engine)

        # 网络依赖服务(离线模式禁用)
        if offline_mode:
            logger.warning("⚠️ 离线模式: 跳过交易网关和组合投资服务初始化")
            results["trading_gateway"] = False
            results["portfolio"] = False
        else:
            results["trading_gateway"] = self._init_trading_gateway(event_engine, main_engine)
            results["portfolio"] = self._init_portfolio(event_engine)

        return results

# BackendInitializerWorker调用（新架构，backend/startup/workers/backend_initializer.py）
class BackendInitializerWorker(StartupWorker):
    async def _run(self, context: StartupContext) -> WorkerResult:
        """执行后端初始化逻辑（异步）"""
        from backend.startup.initializers.service_initializer import ServiceInitializer

        # 初始化后端服务（通过ServiceInitializer）
        initializer = ServiceInitializer()
        results = initializer.initialize_all_services(
            event_engine=context.event_engine,
            main_engine=context.main_engine,
            china_stock_engine=context.china_stock_engine,
        )

        # 将服务注册到ServiceManager（暴露给前端）
        for service_name, service_instance in results.items():
            if service_instance:
                context.service_manager.register_service(service_name, service_instance)

        return WorkerResult(success=True, data={"services": results})
```

**离线模式服务初始化输出示例**:

```
📍 阶段3.4: 交易服务初始化开始
⚠️ 离线模式: 跳过交易网关服务初始化
✅ 交易服务就绪（已跳过）

📍 阶段3.5: 策略服务初始化开始
✅ StrategyCenterService初始化完成
✅ AIAssistantService初始化完成
✅ 策略服务就绪

📍 阶段3.6: 辅助服务初始化开始
✅ MarketBoardService初始化完成
✅ SystemManagerService初始化完成
⚠️ 离线模式: 跳过组合投资服务初始化
✅ 辅助服务就绪（部分服务已跳过）
```

### 6. UI层降级提示

**修改`ui/main_window.py`**:

```python
class MainWindow(QMainWindow):
    """主窗口"""

    def __init__(self, backend_ready: bool = False):
        super().__init__()
        # ... 现有代码 ...

        # 监听离线模式事件
        event_engine = get_event_engine()
        if event_engine:
            event_engine.register("eSystemOfflineMode", self._on_offline_mode)

    def _on_offline_mode(self, event: Event):
        """处理离线模式事件"""
        data = event.data
        reason = data.get("reason", "未知原因")

        # 显示离线模式通知
        from PySide6.QtWidgets import QMessageBox
        QMessageBox.warning(
            self,
            "离线降级模式",
            f"系统已进入离线降级模式:\n\n{reason}\n\n"
            "以下功能将不可用:\n"
            "- 数据下载\n"
            "- 实时行情推送\n"
            "- 交易网关\n"
            "- 组合投资\n\n"
            "本地历史数据查询仍可正常使用。"
        )

        # 在状态栏显示离线标记
        self.statusBar().showMessage(f"⚠️ 离线模式: {reason}", 0)

        # 禁用相关菜单和按钮
        self._disable_online_features()

    def _disable_online_features(self):
        """禁用在线功能"""
        # 禁用数据下载菜单
        self.data_download_action.setEnabled(False)
        # 禁用交易网关菜单
        self.trading_gateway_action.setEnabled(False)
        # 禁用组合投资菜单
        self.portfolio_action.setEnabled(False)
        # ... 其他在线功能 ...
```

### 7. 离线模式功能限制

**可用功能**:
- ✅ 本地历史数据查询（通过StorageManager）
- ✅ 数据可视化（MarketBoardService）
- ✅ 策略回测（StrategyCenterService，使用本地数据）
- ✅ AI助手（AIAssistantService）
- ✅ 系统监控（SystemManagerService）
- ✅ 文件监控（DataFileWatcher）

**不可用功能**:
- ❌ 数据下载（TDX数据源）
- ❌ 实时行情推送
- ❌ 交易网关（TradingGatewayService）
- ❌ 组合投资（PortfolioService）
- ❌ 四层数据融合（仅本地存储层可用）

### 8. 离线模式恢复

**自动恢复检测**（可选实现）:

```python
class ChinaStockEngine:
    def check_offline_recovery(self):
        """检查是否可以恢复在线模式"""
        if not self._offline_mode:
            return

        # 定期检测服务器池是否恢复
        recovery_result = self._validate_server_pool_and_test_speed()

        if not recovery_result.get("offline_mode"):
            # 服务器池已恢复
            logger.info("✅ TDX服务器池已恢复,退出离线模式")
            self.set_offline_mode(False, "")

            # 重新初始化UnifiedDataManager
            self.unified_data_manager = UnifiedDataManager(self)

            # 发布恢复事件
            self.event_engine.put(Event("eSystemOfflineMode", {
                "offline": False,
                "recovered": True,
                "timestamp": datetime.now()
            }))
```

---

**文档版本**: v3.2
**创建日期**: 2025-11-02
**更新时间**: 2025-11-08
**维护者**: AI Assistant
**状态**: ✅ 启动完整设计文档（新架构版，包含标准输出示例 + Log输出设计 + 底层代码设计 + 降级机制设计）

**更新说明**:
- v3.0: 更新为新架构实现（backend/startup v1.0）
  - Terminal标准输出格式不变（规则不变）
  - Log输出设计规则不变，实现更新为新架构
  - 底层代码设计更新为新架构（4层架构）
  - TDX离线降级机制规则不变，实现更新为新架构
- v3.1: 启动速度优化（backend/startup v1.3）
  - 优化1：步骤1服务器池缓存检查（节省约750ms）
  - 优化2：步骤6异步本地数据索引（节省约2.4s）
  - 优化3：步骤7延迟数据更新检查（节省约700ms）
  - 优化4：UI预加载并行执行（节省约300ms）
  - 总启动时间从5.2s降至约2.4s，提升约54%
- v3.2: 服务初始化时序（第4节）重写为最佳实践
  - 并行编排采用 `TaskGroup`（3.11+），统一取消与错误收敛
  - 数据进程承担缓存验证与RPC服务，主进程仅消费进度事件
  - 多进程日志与RPC优先 `native_ipc`/`native_rpc_bridge`，提供回退方案
  - 明确就绪等级（Level 0/1/2）与超时重试策略，增强可观测性与稳定性
