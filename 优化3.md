# -*- coding: utf-8 -*-
# 优化3.md

## 综述

本轮建议聚焦于 C 扩展在三条关键链路上的进一步落地：数据下载任务队列、日志多进程桥接、系统监控采集。它们均在高频路径仍大量依赖 Python 多进程/线程原语或 `psutil`，带来 GIL 争用与上下文切换开销。本文件按照“现状 → 优化方向 → 预期收益 → 风险/跟进”结构梳理，并避免复述 `优化2.md` 中的任务。

## 建议一：数据下载任务队列改用原生无锁/批量 IPC

### 现状

当前 `MultiProcessStockFetcher` 在 Stage 3 的数据进程启动后仍通过 `multiprocessing.Manager` 构建队列与事件，所有任务与结果都经由 Pickle 序列化传输，GIL 争用与上下文切换频繁。

```3324:3343:backend/infrastructure/data_module_vnpy/data_acquisition.py
        manager = Manager()
        task_queue = manager.Queue()
        result_queue = manager.Queue()
        self._stop_event = manager.Event()
        self._pause_event = manager.Event()

        # 加载任务到进程队列
        ...
        task_queue.put(task)
```

### 优化方向

1. **改造通信层**：引入新的 `native_ipc` 扩展（或 `native_gil` 下的 `LockFreeQueue` 封装）为数据下载构建无锁环形缓冲区，支持批量推送。结合 `native_memory.MemoryPool` 为任务结构分配固定大小的共享内存槽位。
2. **批量消费/生产接口**：在 `_download_worker_async` 内部新增批量弹出接口，减少 Python 循环次数；主进程端也使用批量推送降低 IPC 成本。
3. **任务结构瘦身**：利用 C 扩展定义的结构化数据（如 `struct TaskPayload { uint32_t symbol_idx; uint8_t interval; ... }`）替代 Python dict，沿用现有 `task_logger` 仅在需要时做文字映射。

### 预期收益

- 去除 `Manager.Queue` 带来的单进程序列化瓶颈，预计在 5k+ 任务批量下载时减少 20%-30% 的 CPU 占用，并可提升进程间通信吞吐量。
- 减少任务落地时间抖动，协助 LoadBalancer 在第二阶段快速伸缩。
- 为后续数据进程完全 C 化（如缓存验证迁移）打基础，避免重复搬迁。

### 风险与跟进

- 需要补充 `native_ipc` 的批量 API，并在 Windows 下完整测试超时、关闭、异常处理逻辑。
- Shared memory 与 Python GC 的交互需仔细管理生命周期，建议引入 RAII 封装确保释放。
- 首轮上线建议只在数据下载链路启用，其他 Manager 仍保留回退通道。

## 建议二：日志多进程桥接引入原生队列/共享内存

### 现状

日志统一路由仍依赖 `SyncManager.Queue` 暴露 `QueueProxy` 给子进程；QueueListener 每次需反序列化记录并转发。随着日志量增大（尤其是三进程启动阶段、批量扫描），CPU 消耗显著。

```515:568:backend/infrastructure/system_vnpy/logging_system.py
class MultiProcessLogCollector:
    ...
    def _create_managed_queue(self) -> Any:
        authkey = secrets.token_bytes(32)
        manager = SyncManager(address=("127.0.0.1", 0), authkey=authkey)
        manager.start()

        queue_proxy = manager.Queue(-1)
        ...
        self._manager = manager
        return queue_proxy
```

### 优化方向

1. **原生队列实现**：在 `native_gil` 新增“多生产者多消费者”队列（以 IOCP 事件+自旋锁结合），或直接复用 `native_ipc` 的双向管道扩展出流控队列接口。
2. **共享内存批量日志块**：将日志 `UnifiedLogRecord` 序列化为固定大小块，使用 `native_memory.MemoryPool` 管理，QueueListener 仅需拷贝指针即可完成转发。
3. **回退策略**：保持现有 `SyncManager` 为 fallback，当 C 扩展不可用时自动降级，兼容现有 `LOGGING_QUEUE_TOKEN` 流程。

### 预期收益

- 降低子进程日志输出的序列化与上下文切换成本，按现有启动日志量测算，预计可缩短 Stage 3 日志桥接耗时 40% 左右。
- 减少 `QueueListener` 队列阻塞概率，避免在高压场景下出现日志延迟导致的假死误判。
- 统一日志与数据进程的 IPC 技术栈，简化维护。

### 风险与跟进

- 需要在 `LoggingHub` 侧补充批量读取逻辑，确保顺序与原有 `OrderedLogQueue` 兼容。
- 子进程在异常退出时，应提供明确的降级/清理钩子，避免内存池泄露。

## 建议三：系统监控采集扩展 native_metrics 模块

### 现状

`MonitoringProcessV2` 在多处频繁调用 `psutil` 获取网络连接、磁盘/网络吞吐等指标，尤其是 `get_socket_buffer_info()` 会遍历所有连接并临时创建 socket 对象。该逻辑在监控进程高频执行，CPU 占比可观。

```6126:6299:backend/infrastructure/system_vnpy/monitor_system.py
            if SOCKET_METRICS_AVAILABLE and native_get_socket_metrics is not None:
                ...

            connections = psutil.net_connections(kind='inet')
            ...
                    sock = socket_module.fromfd(
                        conn.fd,
                        socket_module.AF_INET,
                        socket_module.SOCK_STREAM
                    )
                    ...
            recv_buffer_size_avg = int(sum(recv_buffer_sizes) / len(recv_buffer_sizes)) if recv_buffer_sizes else 0
            ...
            net_io_1: Any = psutil.net_io_counters()
            time.sleep(0.05)
            net_io_2: Any = psutil.net_io_counters()
```

### 优化方向

1. **扩展 native_socket_metrics**：在现有 C 扩展基础上增加更多指标（缓冲区统计、聚合收发速率），避免 Python 层循环与 `time.sleep`。
2. **新增 native_process_metrics 模块**：直接基于 Windows PDH / NT API 抓取 CPU、内存、句柄等指标，减少 `psutil.process_iter` 的 O(N) 扫描，同时提供批量结果结构体。
3. **监控架构调整**：`ProcessMonitor` 接口改为优先使用 C 扩展批量结果，只有在扩展不可用时才回退到 `psutil`。同时为扩展提供一次性缓冲复用接口，减少分配。

### 预期收益

- 监控进程 CPU 占用可望下降 30% 以上，减少对主机资源的竞争，提升监控数据实时性。
- 避免 `time.sleep(0.05)` 带来的阻塞，降低事件循环延迟。
- 为未来监控数据跨进程共享（通过 `native_ipc` 推送）提供轻量数据格式。

### 风险与跟进

- 需保证扩展在无管理员权限场景下仍能安全运行，必要时自动降级。
- Windows API 兼容性测试必不可少，特别是旧版本系统的 PDH 支持。
- 在 UI 层需验证新指标的格式，确保历史数据展示与告警阈值逻辑兼容。

## 结语

以上建议可以与 `优化2.md` 中的 RPC 编解码、缓存验证迁移等工作并行推进。建议优先排期数据下载与日志桥接改造，它们复用度高、风险可控；监控扩展则可在完成原生队列后逐步试点，以减少同时变更带来的复杂度。

