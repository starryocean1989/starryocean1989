# -*- coding: utf-8 -*-
# 优化4：C 扩展替换评估报告

## 背景与现状
- 最新启动信号显示监控进程虽达到 Level 2，但 `monitor_alerts` 管道仍为 `null`，系统监控只能依赖主进程侧采集。
- 现有 Python 热点主要集中在系统监控与 TDX 数据解析环节，仍存在大量逐条循环、GIL 竞争与阻塞式采样。
- 目标：识别仍可由 C 扩展（`native_*` 系列）接管的高频路径，并评估收益 / 风险与落地优先级。

## 优化候选概览
| 优化项 | 现状热点 | 建议 C 扩展 | 预期收益 | 风险/注意事项 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| Socket 缓冲区采集 | `SystemMonitor.get_socket_buffer_info` 中逐连接 `socket.fromfd` 与 `time.sleep` 采样 | `native_socket_metrics`（基于 `GetExtendedTcpTable`/`WSAIoctl`） | 采样耗时从 80~120ms 降至 <10ms，解除 event loop 阻塞 | Windows API 版本差异、需要管理员权限兜底 | 高 |
| 进程快照生成 | `SystemMonitor.get_process_list` 逐进程 `psutil.process_iter` | `native_process_snapshot`（封装 `NtQuerySystemInformation`） | 单次扫描成本降低 >50%，可支持更频繁刷新 | 需处理句柄权限与 32/64 位兼容性 | 中高 |
| TDX K 线解析 | `tdx_asyncio` 解析器逐条构建 `dict` / `Timestamp` | `native_tdx_parser`（零拷贝批量解码 + Arrow 缓冲） | 大批量解析吞吐提升 2~3 倍，减少 GC 压力 | 需要严格验证协议字段 & 回退路径 | 中 |

## 详细分析

### 1. Socket 缓冲区原生采集
当前 `SystemMonitor.get_socket_buffer_info` 仍以 Python 循环遍历所有连接，逐个从文件描述符构建 socket 对象并调用 `getsockopt`，同时通过 `time.sleep(0.05)` 二次取样估算吞吐，阻塞事件循环并放大 GIL 竞争：

```6188:6295:backend/infrastructure/system_vnpy/monitor_system.py
        for conn in connections:
            ...
            if hasattr(conn, 'fd') and conn.fd is not None and conn.fd >= 0:
                sock = socket_module.fromfd(conn.fd, socket_module.AF_INET, socket_module.SOCK_STREAM)
                recv_buf = sock.getsockopt(socket_module.SOL_SOCKET, socket_module.SO_RCVBUF)
                send_buf = sock.getsockopt(socket_module.SOL_SOCKET, socket_module.SO_SNDBUF)
                ...
        recv_buffer_size_avg = int(sum(recv_buffer_sizes) / len(recv_buffer_sizes)) if recv_buffer_sizes else 0
        ...
        net_io_1 = psutil.net_io_counters(); time.sleep(0.05); net_io_2 = psutil.net_io_counters()
```

**建议**：
- 实现 `native_socket_metrics` C 扩展，直接调用 `GetExtendedTcpTable`/`GetPerTcp6Table` 或 `WSAIoctl(SIO_TCP_INFO)` 获取套接字缓冲与 RTT 指标，一次性返回结构化数组。
- 在扩展内部完成统计与占用估算，提供 `PyObject*` 字典/`dataclass` 输出，Python 侧只负责格式化。
- 同时在扩展中封装短时间吞吐采样，替代 Python `time.sleep`。

**预期收益**：监控周期可安全缩短至 1 秒以下，CPU 占用显著下降，并为后续跨进程告警管道 (`monitor_alerts`) 留出带宽。

### 2. 进程列表快照原生化
`get_process_list` 每次通过 `psutil.process_iter` 拉取所有进程，再逐个访问 `memory_info()` 计算内存占用：

```6506:6555:backend/infrastructure/system_vnpy/monitor_system.py
    processes = []
    for proc in psutil.process_iter(['pid', 'name', 'cpu_percent', 'memory_percent', 'status']):
        proc_info = proc.info
        proc_info['memory_mb'] = proc.memory_info().rss / 1024 / 1024
        processes.append(proc_info)
```

**建议**：
- 使用 Windows `NtQuerySystemInformation(SystemProcessInformation)`（或 Linux `/proc` 扫描）在 C 扩展中一次性抓取所有进程快照，并在原生层完成 Top-N 排序。
- 输出结构化 `PyList` / `PyArray`，Python 端仅用于展示与过滤，减少大量 `psutil` 调用与对象构造。
- 可结合 `native_collections.HighPerfPriorityQueue`，在扩展中直接维护按 CPU/内存排序的前 50 条记录。

**预期收益**：将进程采样平均耗时从 150~200ms 降至约 30ms，并减少频繁调用时的 GIL 竞争；同时也为未来跨进程告警提供更低延迟基础数据。

### 3. TDX 二进制解析管线原生化
在 `tdx_asyncio` 中，日线/分钟线解析虽已使用 `batch_compute`，但仍需 Python 逐条构建记录与 `pd.Timestamp`，大规模下载时成为瓶颈：

```320:405:backend/infrastructure/tdx_asyncio/readers/binary_reader.py
for i in range(record_count):
    offset = i * self.DAY_RECORD_SIZE
    date_ints.append(date_int)
    open_prices.append(open_price)
    ...
for idx, (year, month, day) in enumerate(zip(years, months, days)):
    records.append({
        'datetime': pd.Timestamp(year, month, day),
        'open': open_prices[i],
        ...
    })
```

**建议**：
- 实现 `native_tdx_parser` 扩展，直接在 C/C++ 中解包 TDX 二进制，并输出 Arrow Array 或 NumPy ndarray；结合 `pyarrow` 可零拷贝构建 `Table`/`DataFrame`。
- 在扩展中完成日期解码与价格缩放，避免 Python 层循环与 `pd.Timestamp` 构造。
- 支持批量输入（例如一次 800 条日线/分钟线）并与现有 `native_memory`/`native_serialization` 协同，减少 GC 压力。

**预期收益**：日线批量解码吞吐预计提升 2~3 倍，CPU 峰值下降，DOWNLOAD/SCAN 任务的进程池可承载更多并发；同时减少 `records` 列表暂存导致的内存峰值。

## 其他观察
- `DataSensor._scan_async_coroutine` 仍按顺序 `await` 每个子任务，可在完成上述 C 化后同步改造为 `asyncio.as_completed`，以充分利用 `native_tdx_parser` 带来的吞吐提升。
- `monitor_ready.signal` 中 `alerts` 管道为 `null`，建议在实现原生采集器时同步补齐跨进程告警传输（例如在扩展内直接推送统计阈值结果）。

## 后续建议
1. 先落地 `native_socket_metrics`，验证在监控进程内加载的稳定性，并补齐 `monitor_alerts` 管道接入。
2. 以 `native_process_snapshot` 替换 `psutil.process_iter` 后，再评估是否回收已有 Python 统计逻辑（例如 `ResourceMonitor` 阈值判定）。
3. `native_tdx_parser` 可分阶段实现：先支持日线/分钟线，再扩展至 `transaction` / `finance` 数据类型，期间保持现有 Python 实现作为回退。

通过以上三个方向，可在保证兼容性的前提下进一步压缩关键路径的 Python CPU 占比，为三进程架构释放更多资源配额。

