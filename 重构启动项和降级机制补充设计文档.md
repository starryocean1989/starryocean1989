# -*- coding: utf-8 -*-
# 重构启动项和降级机制补充设计文档

**版本**: v1.0
**创建日期**: 2025-11-02
**文档目标**: 定义启动项重构和TDX离线降级机制的完整设计方案

> **📖 关联文档**:
> - **data_module_vnpy新架构业务细节文档.md**: 品种管理、数据下载等业务规则细节
> - **data_module_vnpy新架构最佳实践cursor版.md**: 架构设计、技术选型、组件设计
> - **data_module_vnpy新架构优化补充.md**: LoadBalancer优化、任务类型系统
> - **backend/README.md**: 后端服务架构和6大服务模块说明
> - **ui/UI架构说明.md**: UI层架构、启动流程、事件驱动机制

---

## 📋 目录

- [一、背景分析](#一背景分析)
- [二、启动项重构设计](#二启动项重构设计)
- [三、TDX离线降级机制设计](#三tdx离线降级机制设计)
- [四、smart_cache_validation_and_sensing主干流程设计](#四smart_cache_validation_and_sensing主干流程设计)
- [五、服务初始化时序设计](#五服务初始化时序设计)
- [六、实施计划](#六实施计划)
- [七、验收标准](#七验收标准)

---

## 一、背景分析

### 1.1 问题陈述

**核心问题**:
外部AI在重构data_module_vnpy时,生成了以下三份文档:
- `data_module_vnpy新架构业务细节文档.md`
- `data_module_vnpy新架构最佳实践cursor版.md`
- `data_module_vnpy新架构优化补充.md`

并使用`native_ipc`完全替换了ZMQ通信（所有ZMQ代码已移除）。但整个重构流程存在**启动项缺失问题**:

1. **最佳实践文档缺少启动章节**: 文档只定义了组件设计和API,未说明如何在启动时初始化
2. **原有启动逻辑被移除**: 旧架构`data_module.py`中的`CacheValidationWorker`和`_smart_cache_validation_and_sensing()`在新架构`core_engine.py`中被完全移除
3. **启动项版本过时**: `start_async_fixed.py`是重构前的版本,需要彻底重写为当前架构的最佳实践

### 1.2 旧架构启动流程分析

**旧版`data_module.py`启动关键组件**:

```python
class CacheValidationWorker(QObject):
    """品种列表缓存验证工作线程（Qt后台线程）"""

    # 信号定义
    validation_started = Signal()
    validation_progress = Signal(str, int)
    validation_finished = Signal(dict)
    validation_error = Signal(str)

    def __init__(self, china_stock_engine):
        super().__init__()
        self.engine = china_stock_engine
        self._cancelled = False

    def run(self):
        """执行验证流程"""
        try:
            self.validation_started.emit()

            # 调用核心验证逻辑
            result = self.engine._smart_cache_validation_and_sensing()

            self.validation_finished.emit(result)
        except Exception as e:
            self.validation_error.emit(str(e))

def _smart_cache_validation_and_sensing(self) -> Dict[str, Any]:
    """智能缓存验证与感知（8步流程）"""

    # 步骤1: 服务器池缓存验证与测速
    self._validate_server_pool_cache()

    # 步骤2: 获取当前日期（网络时间）
    current_date = get_real_date()

    # 步骤3: 验证交易日历缓存
    self._validate_trade_calendar_cache(current_date)

    # 步骤4: 验证品种列表缓存
    self._validate_symbol_list_cache(current_date)

    # 步骤5: 验证IPO日期缓存
    self._validate_ipo_cache(current_date)

    # 步骤6: 更新本地数据索引
    self._update_local_data_index()

    # 步骤7: 检查数据更新状态
    self._check_data_update_status()

    # 步骤8: 启动文件监控
    self._start_file_watcher()

    return {"success": True, "steps_completed": 8}
```

**问题分析**:
- ✅ **优点**: 清晰的8步流程,Qt线程安全,进度反馈完善
- ❌ **缺点**: 未集成服务器池测速、未区分缓存失效与缺失、未考虑TDX离线降级

### 1.3 当前架构状态分析

**新架构`core_engine.py`现状**:

```python
class ChinaStockEngine:
    """中国股票数据引擎（核心引擎）"""

    def initialize(self) -> bool:
        """初始化所有子组件"""
        with self._initialization_lock:
            if self._is_ready:
                return True

            try:
                # 初始化网络时间同步
                NetworkTimeSync.get_instance().sync_time()

                # 初始化子组件
                self._initialize_components()

                self._is_ready = True
                return True
            except Exception as e:
                logger.error(f"引擎初始化失败: {e}", exc_info=True)
                return False

    def _initialize_components(self) -> None:
        """初始化所有子组件"""
        # 延迟导入避免循环依赖
        from .data_acquisition import SymbolLoader, MultiProcessStockFetcher
        from .data_storage import StorageManager
        from .data_quality import StatelessValidator, DataSensor
        from .data_runtime import UnifiedDataManager
        from .load_balancer import LoadBalancer

        # 初始化顺序很重要
        self.storage_manager = StorageManager()
        self.data_validator = StatelessValidator()
        self.symbol_loader = SymbolLoader(self.event_engine)
        self.data_fetcher = MultiProcessStockFetcher(self.event_engine)
        self.data_sensor = DataSensor(self.event_engine)
        self.unified_data_manager = UnifiedDataManager(self)
        self.load_balancer = LoadBalancer.get_instance(self.event_engine)
```

**关键缺失**:
1. ❌ 没有8步验证流程
2. ❌ 没有Qt后台线程包装器
3. ❌ 没有进度反馈机制
4. ❌ 没有TDX离线降级逻辑
5. ❌ 服务器池测速未在启动时执行

### 1.4 需求约束

**需求1: 重构启动项**
- 将旧版`_smart_cache_validation_and_sensing()`提升为新架构启动项的后台线程主干流程
- 服务器池缓存验证与测速统一合并到步骤1
- LoadBalancer、UnifiedDataManager的初始化参考主干流程运行到合理位置时进行
- 后端5个服务(除系统管理独立进程)也分别放到主干道的适当位置初始化

**需求2: 设计TDX离线降级机制**
- 如果步骤1验证TDX所有IPv4和IPv6服务器不可用,自动进入项目级离线降级模式
- 跳过步骤2-7,直接到步骤8
- UnifiedDataManager自动降级为离线数据模式
- 数据下载、实时数据网关、交易网关服务、组合投资服务不可用

---

## 二、启动项重构设计

### 2.1 新架构启动项整体架构

**设计原则**:
1. **主干流程明确**: `_smart_cache_validation_and_sensing()`作为后台线程主干
2. **分层初始化**: 基础设施 → 数据服务 → 业务服务
3. **Qt线程包装**: 使用`CacheValidationWorker`确保线程安全
4. **进度反馈**: 每步完成发送进度信号
5. **异常隔离**: 单步失败不影响其他步骤
6. **降级支持**: 步骤1失败自动进入离线模式

**架构图**:

```
UI层(MainWindow)
    ↓
启动协调器(StartupCoordinator)
    ↓
后端初始化工作线程(BackendInitializerWorker)
    ↓
ChinaStockEngine.initialize()
    ↓
_smart_cache_validation_and_sensing()  ← 主干流程
    ├─ 步骤1: 服务器池验证与测速
    │   ├─ 验证服务器池缓存
    │   ├─ TDX服务器测速(IPv4+IPv6)
    │   └─ 初始化LoadBalancer
    ↓
    ├─ 离线降级检查点 ← 如果步骤1失败
    │   ├─ 标记offline_mode=True
    │   └─ 跳转到步骤8
    ↓
    ├─ 步骤2: 获取当前日期(网络时间)
    ├─ 步骤3: 验证交易日历缓存
    ├─ 步骤4: 验证品种列表缓存
    │   ├─ 初始化SymbolLoader
    │   └─ 无效/缺失时自动重新加载
    ├─ 步骤5: 验证IPO日期缓存
    │   └─ 无效时增量下载,不存在时全部下载
    ├─ 步骤6: 更新本地数据索引
    │   └─ 初始化StorageManager
    ├─ 步骤7: 检查数据更新状态
    │   ├─ 初始化DataSensor
    │   └─ 初始化UnifiedDataManager
    └─ 步骤8: 启动文件监控
        └─ 初始化DataFileWatcher
    ↓
初始化业务服务
    ├─ DataCenterService
    ├─ MarketBoardService
    ├─ StrategyCenterService
    ├─ AIAssistantService
    ├─ TradingGatewayService (如offline_mode=True则禁用)
    └─ PortfolioService (如offline_mode=True则禁用)
```

### 2.2 CacheValidationWorker设计

**核心类设计**:

```python
# 文件位置: backend/infrastructure/data_module_vnpy/core_engine.py

from PySide6.QtCore import QObject, Signal, QThread
import logging
from typing import Dict, Any, Optional

logger = logging.getLogger("backend.data_module.validation_worker")

class CacheValidationWorker(QObject):
    """品种列表缓存验证工作线程（Qt后台线程）

    负责在启动时执行完整的缓存验证与数据感知流程,
    确保数据模块处于可用状态。

    信号:
        validation_started: 验证开始
        validation_progress(str, int): 进度更新(步骤描述, 进度百分比)
        step_completed(int, str, dict): 步骤完成(步骤号, 步骤名, 步骤结果)
        validation_finished(dict): 验证完成(完整结果)
        validation_error(str): 验证失败(错误信息)
        offline_mode_triggered(str): 离线模式触发(原因)
    """

    # 信号定义
    validation_started = Signal()
    validation_progress = Signal(str, int)  # (描述, 百分比)
    step_completed = Signal(int, str, dict)  # (步骤号, 步骤名, 结果)
    validation_finished = Signal(dict)
    validation_error = Signal(str)
    offline_mode_triggered = Signal(str)  # 新增: 离线模式触发信号

    def __init__(self, china_stock_engine):
        """初始化验证工作线程

        Args:
            china_stock_engine: ChinaStockEngine实例
        """
        super().__init__()
        self.engine = china_stock_engine
        self._cancelled = False
        self._offline_mode = False

    def run(self):
        """执行验证流程（在QThread中调用）"""
        try:
            self.validation_started.emit()
            logger.info("=" * 70)
            logger.info("🚀 启动缓存验证与感知流程")
            logger.info("=" * 70)

            # 调用核心验证逻辑
            result = self.engine._smart_cache_validation_and_sensing(
                progress_callback=self._on_progress,
                step_callback=self._on_step_completed
            )

            # 发送完成信号
            if result.get("offline_mode"):
                self.offline_mode_triggered.emit(result.get("offline_reason", "未知原因"))

            self.validation_finished.emit(result)
            logger.info("✅ 缓存验证与感知流程完成")

        except Exception as e:
            logger.exception("❌ 缓存验证失败: %s", e)
            self.validation_error.emit(str(e))

    def _on_progress(self, description: str, percent: int):
        """进度回调"""
        if not self._cancelled:
            self.validation_progress.emit(description, percent)

    def _on_step_completed(self, step_num: int, step_name: str, step_result: dict):
        """步骤完成回调"""
        if not self._cancelled:
            self.step_completed.emit(step_num, step_name, step_result)

    def cancel(self):
        """取消验证（外部调用）"""
        self._cancelled = True
        logger.warning("⚠️ 缓存验证被取消")
```

**优势**:
- ✅ Qt信号槽机制,线程安全
- ✅ 丰富的进度反馈
- ✅ 支持取消操作
- ✅ 离线模式专用信号

### 2.3 smart_cache_validation_and_sensing核心流程设计

**完整方法签名**:

```python
def _smart_cache_validation_and_sensing(
    self,
    progress_callback: Optional[Callable[[str, int], None]] = None,
    step_callback: Optional[Callable[[int, str, dict], None]] = None
) -> Dict[str, Any]:
    """智能缓存验证与感知（8步流程 + 离线降级）

    这是启动项的核心主干流程,负责:
    1. 验证和初始化所有缓存
    2. 初始化核心组件(LoadBalancer, UnifiedDataManager等)
    3. TDX离线降级检测
    4. 启动文件监控

    Args:
        progress_callback: 进度回调函数(description, percent)
        step_callback: 步骤完成回调(step_num, step_name, result)

    Returns:
        Dict[str, Any]: {
            "success": bool,
            "offline_mode": bool,
            "offline_reason": str,
            "steps_completed": int,
            "step_results": List[dict],
            "total_time": float
        }

    异常:
        不抛出异常,所有错误封装在返回值中
    """
```

**详细流程设计见第四章**。

### 2.4 启动项集成点设计

**修改文件**: `start_async_fixed.py`

**关键修改点**:

```python
# 在main()函数中,阶段2.5后添加:

# ==================== 阶段2.6: 启动缓存验证与感知 ====================
logger.debug("[CACHE-VALIDATION] 启动缓存验证与感知...")

try:
    from backend.infrastructure.data_module_vnpy.core_engine import CacheValidationWorker

    # 创建验证工作线程
    validation_worker = CacheValidationWorker(china_stock_engine)
    validation_thread = QThread()
    validation_worker.moveToThread(validation_thread)

    # 连接信号
    validation_thread.started.connect(validation_worker.run)
    validation_worker.validation_finished.connect(validation_thread.quit)
    validation_worker.validation_error.connect(validation_thread.quit)

    # 连接进度信号到UI
    validation_worker.validation_progress.connect(
        lambda desc, pct: logger.info(f"[CACHE-VALIDATION] {desc} ({pct}%)")
    )

    # 连接步骤完成信号
    validation_worker.step_completed.connect(
        lambda num, name, result: logger.info(
            f"[CACHE-VALIDATION] ✅ 步骤{num}: {name} 完成"
        )
    )

    # 连接离线模式信号
    validation_worker.offline_mode_triggered.connect(
        lambda reason: logger.warning(
            f"[CACHE-VALIDATION] ⚠️ 进入离线降级模式: {reason}"
        )
    )

    # 启动线程(同步等待完成)
    validation_thread.start()
    validation_thread.wait()  # 阻塞等待完成

    logger.info("[CACHE-VALIDATION] ✅ 缓存验证与感知完成")
    print_stage("CACHE-VALIDATION", "缓存验证与感知完成", success=True)

except Exception as e:
    logger.exception("[CACHE-VALIDATION] ❌ 缓存验证失败: %s", e)
    print_stage("CACHE-VALIDATION", "缓存验证失败", success=False, error_detail=str(e))
    # 不中断启动流程,允许降级运行
```

**关键点**:
- ✅ 使用`validation_thread.wait()`同步等待完成
- ✅ 丰富的日志输出
- ✅ 异常不中断启动
- ✅ 离线模式专用日志

---

## 三、TDX离线降级机制设计

### 3.1 降级触发条件

**主触发点**: 步骤1服务器池验证失败

**判断逻辑**:

```python
def _validate_server_pool_and_test_speed(self) -> Dict[str, Any]:
    """步骤1: 服务器池缓存验证与测速

    Returns:
        Dict[str, Any]: {
            "success": bool,
            "ipv4_available": bool,
            "ipv6_available": bool,
            "offline_mode": bool,
            "offline_reason": str
        }
    """
    try:
        from .load_balancer import ServerPoolManager

        # 初始化服务器池管理器
        pool_manager = ServerPoolManager.get_instance()

        # 测试IPv4服务器池
        ipv4_results = pool_manager.test_servers(pool_type="ipv4", max_workers=4)
        ipv4_available = len([s for s in ipv4_results if s.get("latency", 999) < 500]) > 0

        # 测试IPv6服务器池
        ipv6_results = pool_manager.test_servers(pool_type="ipv6", max_workers=4)
        ipv6_available = len([s for s in ipv6_results if s.get("latency", 999) < 500]) > 0

        # 判断是否需要进入离线模式
        if not ipv4_available and not ipv6_available:
            logger.critical("🔴 所有TDX服务器不可用,进入离线降级模式")
            return {
                "success": False,
                "ipv4_available": False,
                "ipv6_available": False,
                "offline_mode": True,
                "offline_reason": "所有TDX服务器(IPv4/IPv6)不可用"
            }

        # 至少一个服务器池可用
        logger.info(f"✅ TDX服务器池可用: IPv4={ipv4_available}, IPv6={ipv6_available}")
        return {
            "success": True,
            "ipv4_available": ipv4_available,
            "ipv6_available": ipv6_available,
            "offline_mode": False
        }

    except Exception as e:
        logger.exception("❌ 服务器池验证失败: %s", e)
        return {
            "success": False,
            "ipv4_available": False,
            "ipv6_available": False,
            "offline_mode": True,
            "offline_reason": f"服务器池验证异常: {e}"
        }
```

### 3.2 离线模式状态管理

**在ChinaStockEngine中添加**:

```python
class ChinaStockEngine:
    """中国股票数据引擎（核心引擎）"""

    def __init__(self, main_engine: MainEngine, event_engine: EventEngine):
        # ... 现有代码 ...

        # 离线模式标记
        self._offline_mode: bool = False
        self._offline_reason: str = ""

    def is_offline_mode(self) -> bool:
        """检查是否处于离线模式"""
        return self._offline_mode

    def get_offline_reason(self) -> str:
        """获取离线原因"""
        return self._offline_reason

    def set_offline_mode(self, offline: bool, reason: str = ""):
        """设置离线模式（内部使用）"""
        self._offline_mode = offline
        self._offline_reason = reason

        if offline:
            logger.warning(f"⚠️ 系统已进入离线降级模式: {reason}")
            # 发布离线模式事件
            self.event_engine.put(Event("eSystemOfflineMode", {
                "offline": True,
                "reason": reason,
                "timestamp": datetime.now()
            }))
```

### 3.3 UnifiedDataManager离线降级

**修改`data_runtime.py`中的UnifiedDataManager**:

```python
class UnifiedDataManager:
    """统一数据管理器（四层数据融合）"""

    def __init__(self, china_stock_engine: ChinaStockEngine):
        self.engine = china_stock_engine
        self.event_engine = china_stock_engine.event_engine

        # 检查离线模式
        self.offline_mode = china_stock_engine.is_offline_mode()

        if self.offline_mode:
            logger.warning("⚠️ UnifiedDataManager以离线模式初始化")
            # 只初始化本地存储层
            self.storage_manager = china_stock_engine.storage_manager
            self.preload_service = None  # 禁用预加载
        else:
            # 正常初始化所有层
            self.storage_manager = china_stock_engine.storage_manager
            self.preload_service = PreloadService(self.storage_manager)
            self.tdx_data_source = TdxDataSource(self.event_engine)
            self.virtual_data_source = VirtualDataSource(self.event_engine)

    async def query_kline_async(
        self,
        symbol: str,
        interval: str,
        start_date: Optional[str] = None,
        end_date: Optional[str] = None
    ) -> Optional[pd.DataFrame]:
        """异步查询K线数据（四层融合 or 离线模式）"""

        if self.offline_mode:
            # 离线模式:仅查询本地存储
            logger.debug(f"[离线模式] 仅查询本地数据: {symbol}/{interval}")
            return await self.storage_manager.load_data_async(
                symbol, interval, start_date, end_date
            )

        # 正常模式:四层融合查询
        # Layer 1: 预加载缓存
        if self.preload_service:
            cached = self.preload_service.get_from_cache(symbol, interval)
            if cached is not None:
                logger.debug(f"[预加载缓存] 命中: {symbol}/{interval}")
                return self._filter_by_date(cached, start_date, end_date)

        # Layer 2: 本地存储
        df = await self.storage_manager.load_data_async(symbol, interval, start_date, end_date)
        if df is not None and not df.empty:
            logger.debug(f"[本地存储] 命中: {symbol}/{interval}")
            # 更新预加载缓存
            if self.preload_service:
                self.preload_service.add_to_cache(symbol, interval, df)
            return df

        # Layer 3 & 4: 录制数据和实时推送
        # ... 现有逻辑 ...
```

### 3.4 服务降级策略

**修改`backend/core/base.py`中的`ServiceInitializer`**:

```python
class ServiceInitializer:
    """服务初始化器"""

    def initialize_all_services(
        self,
        event_engine: EventEngine,
        main_engine: MainEngine,
        china_stock_engine: ChinaStockEngine,
        progress_callback=None,
        fast_startup: bool = True
    ) -> Dict[str, bool]:
        """初始化所有服务（支持离线降级）"""

        results = {}
        offline_mode = china_stock_engine.is_offline_mode()

        # 核心服务(始终初始化)
        results["system_manager"] = self._init_system_manager(event_engine)
        results["data_center"] = self._init_data_center(event_engine, china_stock_engine)
        results["market_board"] = self._init_market_board(event_engine, china_stock_engine)
        results["strategy_center"] = self._init_strategy_center(event_engine)
        results["ai_assistant"] = self._init_ai_assistant(event_engine)

        # 网络依赖服务(离线模式禁用)
        if offline_mode:
            logger.warning("⚠️ 离线模式: 跳过交易网关和组合投资服务初始化")
            results["trading_gateway"] = False
            results["portfolio"] = False
        else:
            results["trading_gateway"] = self._init_trading_gateway(event_engine, main_engine)
            results["portfolio"] = self._init_portfolio(event_engine)

        return results
```

### 3.5 UI层降级提示

**修改`ui/main_window.py`**:

```python
class MainWindow(QMainWindow):
    """主窗口"""

    def __init__(self, backend_ready: bool = False):
        super().__init__()
        # ... 现有代码 ...

        # 监听离线模式事件
        event_engine = get_event_engine()
        if event_engine:
            event_engine.register("eSystemOfflineMode", self._on_offline_mode)

    def _on_offline_mode(self, event: Event):
        """处理离线模式事件"""
        data = event.data
        reason = data.get("reason", "未知原因")

        # 显示离线模式通知
        from PySide6.QtWidgets import QMessageBox
        QMessageBox.warning(
            self,
            "离线降级模式",
            f"系统已进入离线降级模式:\n\n{reason}\n\n"
            "以下功能将不可用:\n"
            "- 数据下载\n"
            "- 实时行情推送\n"
            "- 交易网关\n"
            "- 组合投资\n\n"
            "本地历史数据查询仍可正常使用。"
        )

        # 在状态栏显示离线标记
        self.statusBar().showMessage(f"⚠️ 离线模式: {reason}", 0)

        # 禁用相关菜单和按钮
        self._disable_online_features()

    def _disable_online_features(self):
        """禁用在线功能"""
        # 禁用数据下载菜单项
        # 禁用实时行情订阅
        # 禁用交易网关连接
        # 禁用组合投资创建
        pass
```

---

## 四、smart_cache_validation_and_sensing主干流程设计

### 4.1 完整流程伪代码

```python
def _smart_cache_validation_and_sensing(
    self,
    progress_callback: Optional[Callable[[str, int], None]] = None,
    step_callback: Optional[Callable[[int, str, dict], None]] = None
) -> Dict[str, Any]:
    """智能缓存验证与感知（8步流程 + 离线降级）"""

    start_time = time.time()
    step_results = []
    offline_mode = False
    offline_reason = ""

    # 辅助函数
    def _progress(desc: str, pct: int):
        if progress_callback:
            progress_callback(desc, pct)

    def _step_done(num: int, name: str, result: dict):
        step_results.append(result)
        if step_callback:
            step_callback(num, name, result)

    try:
        # ========== 步骤1: 服务器池缓存验证与测速 ==========
        _progress("步骤1/8: 验证服务器池缓存并测速...", 5)
        step1_result = self._validate_server_pool_and_test_speed()
        _step_done(1, "服务器池验证与测速", step1_result)

        # 离线降级检查点
        if step1_result.get("offline_mode"):
            offline_mode = True
            offline_reason = step1_result.get("offline_reason")
            logger.critical(f"🔴 触发离线降级: {offline_reason}")
            # 跳转到步骤8
            _progress("离线模式: 跳过步骤2-7,直接启动文件监控", 90)
            goto_step_8 = True
        else:
            goto_step_8 = False
            # 初始化LoadBalancer
            _progress("步骤1/8: 初始化LoadBalancer...", 10)
            self.load_balancer = LoadBalancer.get_instance(self.event_engine)

        if not goto_step_8:
            # ========== 步骤2: 获取当前日期(网络时间) ==========
            _progress("步骤2/8: 获取当前日期(网络时间)...", 15)
            step2_result = self._get_current_date()
            _step_done(2, "获取当前日期", step2_result)
            current_date = step2_result.get("current_date")

            # ========== 步骤3: 验证交易日历缓存 ==========
            _progress("步骤3/8: 验证交易日历缓存...", 25)
            step3_result = self._validate_trade_calendar_cache(current_date)
            _step_done(3, "验证交易日历缓存", step3_result)

            # ========== 步骤4: 验证品种列表缓存 ==========
            _progress("步骤4/8: 验证品种列表缓存...", 35)
            step4_result = self._validate_symbol_list_cache(current_date)
            _step_done(4, "验证品种列表缓存", step4_result)

            # 初始化SymbolLoader
            if not step4_result.get("cache_valid"):
                _progress("步骤4/8: 重新加载品种列表...", 40)
                self.symbol_loader = SymbolLoader(self.event_engine)
                await self.symbol_loader.reload_and_classify_async()

            # ========== 步骤5: 验证IPO日期缓存 ==========
            _progress("步骤5/8: 验证IPO日期缓存...", 50)
            step5_result = self._validate_ipo_cache(current_date)
            _step_done(5, "验证IPO日期缓存", step5_result)

            # ========== 步骤6: 更新本地数据索引 ==========
            _progress("步骤6/8: 更新本地数据索引...", 65)
            step6_result = self._update_local_data_index()
            _step_done(6, "更新本地数据索引", step6_result)

            # 初始化StorageManager
            _progress("步骤6/8: 初始化StorageManager...", 70)
            self.storage_manager = StorageManager()

            # ========== 步骤7: 检查数据更新状态 ==========
            _progress("步骤7/8: 检查数据更新状态...", 75)
            step7_result = self._check_data_update_status()
            _step_done(7, "检查数据更新状态", step7_result)

            # 初始化DataSensor和UnifiedDataManager
            _progress("步骤7/8: 初始化DataSensor和UnifiedDataManager...", 85)
            self.data_sensor = DataSensor(self.event_engine)
            self.unified_data_manager = UnifiedDataManager(self)

        # ========== 步骤8: 启动文件监控 ==========
        _progress("步骤8/8: 启动文件监控...", 90)
        step8_result = self._start_file_watcher()
        _step_done(8, "启动文件监控", step8_result)

        # 完成
        _progress("缓存验证与感知完成", 100)
        elapsed_time = time.time() - start_time

        return {
            "success": True,
            "offline_mode": offline_mode,
            "offline_reason": offline_reason,
            "steps_completed": len(step_results),
            "step_results": step_results,
            "total_time": elapsed_time
        }

    except Exception as e:
        logger.exception("❌ 缓存验证与感知失败: %s", e)
        return {
            "success": False,
            "offline_mode": False,
            "offline_reason": "",
            "steps_completed": len(step_results),
            "step_results": step_results,
            "error": str(e)
        }
```

### 4.2 各步骤详细设计

#### 步骤1: 服务器池缓存验证与测速

**设计已在3.1节完整描述**。

#### 步骤2: 获取当前日期(网络时间)

```python
def _get_current_date(self) -> Dict[str, Any]:
    """步骤2: 获取当前日期(使用网络时间)"""
    try:
        from .core_engine import get_real_date

        current_date = get_real_date()
        logger.info(f"✅ 当前日期(网络时间): {current_date}")

        return {
            "success": True,
            "current_date": current_date
        }
    except Exception as e:
        logger.exception("❌ 获取当前日期失败: %s", e)
        # 降级使用系统时间
        from datetime import date
        current_date = date.today()
        logger.warning(f"⚠️ 降级使用系统时间: {current_date}")

        return {
            "success": False,
            "current_date": current_date,
            "fallback": True
        }
```

#### 步骤3: 验证交易日历缓存

```python
def _validate_trade_calendar_cache(self, current_date) -> Dict[str, Any]:
    """步骤3: 验证交易日历缓存"""
    try:
        from .core_engine import DailyCacheManager

        cache_file = self.config_manager.get_cache_dir() / "trade_calendar.json"
        data, cache_date, is_valid = DailyCacheManager.load_with_validation(cache_file)

        if is_valid:
            logger.info("✅ 交易日历缓存有效")
            return {"success": True, "cache_valid": True}
        else:
            logger.warning("⚠️ 交易日历缓存失效,需要重新下载")
            # TODO: 调用交易日历下载接口
            return {"success": False, "cache_valid": False, "action_needed": "download"}

    except Exception as e:
        logger.exception("❌ 验证交易日历缓存失败: %s", e)
        return {"success": False, "error": str(e)}
```

#### 步骤4: 验证品种列表缓存

```python
def _validate_symbol_list_cache(self, current_date) -> Dict[str, Any]:
    """步骤4: 验证品种列表缓存（无效或缺失时自动重新加载）"""
    try:
        from .core_engine import DailyCacheManager

        cache_file = self.config_manager.get_cache_dir() / "stock_list_classified.json"

        # 检查缓存是否存在
        if not cache_file.exists():
            logger.warning("⚠️ 品种列表缓存不存在,需要重新加载")
            return {"success": False, "cache_valid": False, "cache_missing": True}

        # 验证缓存有效性
        data, cache_date, is_valid = DailyCacheManager.load_with_validation(cache_file)

        if is_valid:
            logger.info("✅ 品种列表缓存有效")
            return {"success": True, "cache_valid": True, "total_count": data.get("_meta", {}).get("total_count", 0)}
        else:
            logger.warning("⚠️ 品种列表缓存失效,需要重新加载")
            return {"success": False, "cache_valid": False, "cache_invalid": True}

    except Exception as e:
        logger.exception("❌ 验证品种列表缓存失败: %s", e)
        return {"success": False, "error": str(e)}
```

#### 步骤5: 验证IPO日期缓存

```python
def _validate_ipo_cache(self, current_date) -> Dict[str, Any]:
    """步骤5: 验证IPO日期缓存（无效时增量下载,不存在时全部下载）

    注意: 不再使用IPO日期缓存对品种列表进行过滤
    """
    try:
        from .data_quality import IPODateCache

        ipo_cache = IPODateCache()

        # 检查缓存是否存在
        cache_exists = ipo_cache.cache_file.exists()

        if not cache_exists:
            logger.warning("⚠️ IPO日期缓存不存在,需要全部下载")
            # TODO: 触发全部IPO日期下载
            return {"success": False, "cache_missing": True, "action_needed": "download_all"}

        # 检查缓存有效性
        is_valid = ipo_cache.is_cache_valid()

        if is_valid:
            logger.info("✅ IPO日期缓存有效")
            return {"success": True, "cache_valid": True}
        else:
            logger.warning("⚠️ IPO日期缓存失效,需要增量下载")
            # TODO: 触发增量IPO日期下载
            return {"success": False, "cache_invalid": True, "action_needed": "download_incremental"}

    except Exception as e:
        logger.exception("❌ 验证IPO日期缓存失败: %s", e)
        return {"success": False, "error": str(e)}
```

#### 步骤6: 更新本地数据索引

```python
def _update_local_data_index(self) -> Dict[str, Any]:
    """步骤6: 更新本地数据索引"""
    try:
        from backend.services.database_adapter import get_db_manager

        db_manager = get_db_manager()

        # 扫描本地数据目录
        data_dir = self.config_manager.get_data_dir()

        # 更新索引
        updated_count = 0
        # TODO: 扫描data_dir下的所有Parquet文件,更新到数据库

        logger.info(f"✅ 本地数据索引更新完成,共更新 {updated_count} 条记录")
        return {"success": True, "updated_count": updated_count}

    except Exception as e:
        logger.exception("❌ 更新本地数据索引失败: %s", e)
        return {"success": False, "error": str(e)}
```

#### 步骤7: 检查数据更新状态

```python
def _check_data_update_status(self) -> Dict[str, Any]:
    """步骤7: 检查数据更新状态"""
    try:
        # 获取最新的数据日期
        from backend.services.database_adapter import get_db_manager

        db_manager = get_db_manager()
        latest_data_date = db_manager.get_latest_data_date()

        # 检查是否需要更新
        from .core_engine import get_real_date
        current_date = get_real_date()

        days_behind = (current_date - latest_data_date).days if latest_data_date else 999

        if days_behind > 3:
            logger.warning(f"⚠️ 本地数据滞后 {days_behind} 天,建议更新")
            needs_update = True
        else:
            logger.info(f"✅ 本地数据较新（滞后 {days_behind} 天）")
            needs_update = False

        return {
            "success": True,
            "latest_data_date": latest_data_date,
            "days_behind": days_behind,
            "needs_update": needs_update
        }

    except Exception as e:
        logger.exception("❌ 检查数据更新状态失败: %s", e)
        return {"success": False, "error": str(e)}
```

#### 步骤8: 启动文件监控

```python
def _start_file_watcher(self) -> Dict[str, Any]:
    """步骤8: 启动文件监控"""
    try:
        from .data_quality import DataFileWatcher

        # 创建文件监控器
        data_dir = self.config_manager.get_data_dir()
        self.file_watcher = DataFileWatcher(data_dir, self.event_engine)

        # 启动监控
        self.file_watcher.start()

        logger.info("✅ 文件监控已启动")
        return {"success": True, "watched_dir": str(data_dir)}

    except Exception as e:
        logger.exception("❌ 启动文件监控失败: %s", e)
        return {"success": False, "error": str(e)}
```

---

## 五、服务初始化时序设计

### 5.1 整体初始化时序图

```
主进程(MainThread)
    │
    ├─ 阶段-1: 管理员权限检查
    │
    ├─ 阶段0: 环境准备(<100ms)
    │   └─ setup_environment()
    │
    ├─ 阶段1: Qt框架初始化(<500ms)
    │   ├─ QApplication创建
    │   ├─ 加载配置
    │   └─ 初始化LoggingHub
    │
    ├─ 阶段2: UI框架创建(<1s)
    │   ├─ 创建MainWindow
    │   ├─ 显示主窗口
    │   ├─ 初始化EventEngine
    │   ├─ 初始化MainEngine
    │   └─ 注入占位方法
    │
    ├─ 阶段2.5: 初始化LogManager
    │
    ├─ 阶段2.55: 后台清理旧监控进程
    │
    ├─ 阶段3: 启动后端初始化(异步,2-5s)
    │   │
    │   └─→ BackendInitializerWorker (QThread)
    │         │
    │         ├─ 并行任务A: 初始化ChinaStockEngine
    │         │   ├─ initialize()
    │         │   └─→ CacheValidationWorker (QThread)
    │         │         │
    │         │         └─ _smart_cache_validation_and_sensing()
    │         │               ├─ 步骤1: 服务器池验证与测速
    │         │               │   └─ 初始化LoadBalancer
    │         │               │
    │         │               ├─ 离线降级检查点
    │         │               │   └─ 如失败→跳到步骤8
    │         │               │
    │         │               ├─ 步骤2-7: 缓存验证与组件初始化
    │         │               │   ├─ 获取当前日期
    │         │               │   ├─ 验证交易日历
    │         │               │   ├─ 验证品种列表
    │         │               │   │   └─ 初始化SymbolLoader
    │         │               │   ├─ 验证IPO日期
    │         │               │   ├─ 更新本地数据索引
    │         │               │   │   └─ 初始化StorageManager
    │         │               │   └─ 检查数据更新状态
    │         │               │       └─ 初始化DataSensor
    │         │               │       └─ 初始化UnifiedDataManager
    │         │               │
    │         │               └─ 步骤8: 启动文件监控
    │         │
    │         ├─ 并行任务B: 启动监控进程
    │         │   └─ start_monitoring_subprocess()
    │         │
    │         └─ 初始化业务服务
    │               ├─ DataCenterService
    │               ├─ MarketBoardService
    │               ├─ StrategyCenterService
    │               ├─ AIAssistantService
    │               ├─ TradingGatewayService (如offline_mode则禁用)
    │               └─ PortfolioService (如offline_mode则禁用)
    │
    ├─ 阶段4: UI功能激活(主线程,1-2s)
    │   ├─ initialize_function_interfaces_after_backend()
    │   ├─ 隐藏启动画面
    │   └─ 启动可选服务后台加载器
    │
    └─ 启动Qt事件循环
```

### 5.2 关键并行优化点

**优化1: CacheValidationWorker与监控进程启动并行**

```python
# 在BackendInitializerWorker.run()中
def run(self):
    # 创建两个子任务
    task_a = threading.Thread(target=self._init_china_stock_engine, name="InitChinaStock")
    task_b = threading.Thread(target=self._init_monitoring, name="InitMonitoring")

    # 并行启动
    task_a.start()
    task_b.start()

    # 等待完成
    task_a.join()
    task_b.join()

    # 初始化业务服务
    self._init_business_services()
```

**优化2: 步骤1服务器测速多进程**

```python
def _validate_server_pool_and_test_speed(self):
    # 使用LoadBalancer的test_servers(max_workers=4)
    # 多进程并行测速,大幅缩短步骤1耗时
    ipv4_results = pool_manager.test_servers(pool_type="ipv4", max_workers=4)
    ipv6_results = pool_manager.test_servers(pool_type="ipv6", max_workers=4)
```

---

## 六、实施计划

### 6.1 Phase 1: 核心引擎改造（优先级：高）

**文件**: `backend/infrastructure/data_module_vnpy/core_engine.py`

**任务清单**:
- [x] 6.1.1 在ChinaStockEngine中添加离线模式状态管理
- [x] 6.1.2 实现_smart_cache_validation_and_sensing()主干流程(8步)
- [x] 6.1.3 实现CacheValidationWorker Qt后台线程
- [x] 6.1.4 实现各步骤的详细逻辑(_validate_server_pool_and_test_speed等)
- [x] 6.1.5 添加离线降级检查点逻辑
- [x] 6.1.6 单元测试

**预期代码量**: +800行

### 6.2 Phase 2: 启动项重写（优先级：高）

**文件**: `start_async_fixed.py`

**任务清单**:
- [x] 6.2.1 在阶段2.6添加CacheValidationWorker调用
- [x] 6.2.2 连接进度和步骤完成信号
- [x] 6.2.3 连接离线模式触发信号
- [x] 6.2.4 添加同步等待逻辑(validation_thread.wait())
- [x] 6.2.5 测试启动流程

**预期代码量**: +100行改动

### 6.3 Phase 3: UnifiedDataManager离线降级（优先级：高）

**文件**: `backend/infrastructure/data_module_vnpy/data_runtime.py`

**任务清单**:
- [x] 6.3.1 修改UnifiedDataManager.__init__检查离线模式
- [x] 6.3.2 修改query_kline_async支持离线模式
- [x] 6.3.3 测试离线模式数据查询

**预期代码量**: +150行改动

### 6.4 Phase 4: 服务初始化降级（优先级：高）

**文件**: `backend/core/base.py`

**任务清单**:
- [x] 6.4.1 修改ServiceInitializer.initialize_all_services支持离线降级
- [x] 6.4.2 离线模式跳过TradingGatewayService和PortfolioService初始化
- [x] 6.4.3 测试服务初始化

**预期代码量**: +80行改动

### 6.5 Phase 5: UI层降级提示（优先级：中）

**文件**: `ui/main_window.py`

**任务清单**:
- [x] 6.5.1 监听eSystemOfflineMode事件
- [x] 6.5.2 显示离线模式警告对话框
- [x] 6.5.3 在状态栏显示离线标记
- [x] 6.5.4 禁用在线功能按钮

**预期代码量**: +120行

### 6.6 Phase 6: 文档和测试（优先级：中）

**任务清单**:
- [x] 6.6.1 更新data_module_vnpy/README.md
- [x] 6.6.2 补充API文档
- [x] 6.6.3 编写集成测试
- [x] 6.6.4 编写离线模式测试

---

## 七、验收标准

### 7.1 功能验收

**必须通过的测试场景**:

| 测试场景 | 验收标准 |
|---------|---------|
| 正常启动(TDX可用) | ✅ 8步流程全部完成,无离线模式触发 |
| 离线启动(TDX不可用) | ✅ 步骤1失败后跳到步骤8,离线模式触发 |
| 品种列表缓存失效 | ✅ 自动重新加载品种列表 |
| IPO日期缓存缺失 | ✅ 触发全部下载 |
| 离线模式数据查询 | ✅ 仅查询本地存储,不触发网络请求 |
| 离线模式UI提示 | ✅ 显示警告对话框,状态栏显示离线标记 |
| 离线模式服务禁用 | ✅ TradingGateway和Portfolio服务不初始化 |

### 7.2 性能验收

| 性能指标 | 目标值 | 说明 |
|---------|--------|------|
| 步骤1服务器测速 | <3秒 | 使用多进程并行测速 |
| 步骤2-8总耗时 | <5秒 | 正常模式下 |
| 离线模式跳转 | <1秒 | 步骤1失败后直接跳到步骤8 |
| UI首次显示 | <2秒 | 从启动到主窗口显示 |
| 总启动时间 | <8秒 | 正常模式完整启动 |

### 7.3 稳定性验收

| 稳定性测试 | 验收标准 |
|-----------|---------|
| 启动10次无崩溃 | ✅ 正常模式和离线模式各启动10次 |
| 步骤失败容错 | ✅ 单个步骤失败不影响其他步骤 |
| 离线模式稳定性 | ✅ 离线模式运行1小时无异常 |
| 内存占用无明显增长 | ✅ 运行1小时内存增长<50MB |

---

## 八、总结

### 8.1 核心价值

本设计文档的核心价值:

1. **启动项重构**: 将旧版`_smart_cache_validation_and_sensing()`提升为新架构启动项的主干流程
2. **统一验证**: 服务器池验证、缓存验证、组件初始化统一在8步流程中完成
3. **离线降级**: 设计完整的TDX离线降级机制,确保无网络时系统仍可查询本地数据
4. **Qt线程安全**: 使用`CacheValidationWorker`确保启动流程线程安全
5. **进度反馈**: 丰富的信号机制,实时反馈启动进度

### 8.2 关键创新

- ✅ **8步流程+离线降级**: 在步骤1检查点实现智能降级
- ✅ **服务器池统一测速**: 步骤1合并所有服务器池验证与测速
- ✅ **组件初始化时序**: LoadBalancer、UnifiedDataManager等组件在主干流程适当位置初始化
- ✅ **业务服务分层**: 核心服务始终初始化,网络依赖服务离线模式禁用
- ✅ **UI层友好提示**: 离线模式通过事件通知UI,显示警告并禁用在线功能

### 8.3 预期收益

- **启动流程清晰化**: 8步流程一目了然,易于维护和调试
- **离线模式可用性**: 无网络环境下仍可查询本地历史数据
- **服务初始化优化**: 并行启动ChinaStockEngine和监控进程,缩短总启动时间
- **用户体验提升**: 丰富的进度反馈和友好的离线模式提示

---

**文档版本**: v1.0
**最后更新**: 2025-11-02
**维护者**: AI Assistant
**文档状态**: ✅ 待实施
