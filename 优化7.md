# -*- coding: utf-8 -*-
# 优化7：原生扩展深化提案

## 背景概览

三进程架构已经将数据与监控能力从主进程中拆离，并大量引入 `native_*` 扩展以缓解 Python 层面的性能瓶颈。但在核心链路仍存在如下热点：

- **数据RPC路径**依赖 JSON 序列化，虽支持 Arrow，但请求/响应头仍需要多次编码；
- **指标与风险计算**落在数据进程线程池，重复构造 `numpy` 数组并经历 Python↔C 往返；
- **资源监控**仍依赖 `psutil` 轮询和 `time.sleep` 估算，阻塞事件循环并影响实时性。

本提案聚焦三个可落地的原生扩展升级方向，旨在保持降级策略的前提下进一步提升吞吐与降低延迟。

## 优化方向一：数据进程 RPC 零拷贝与批处理

### 现状

数据进程通过 `AsyncIPCPipe` 批量读取 `bytes`，在 Python 层解析、分发并再次序列化响应：

```361:417:backend/infrastructure/data_module_vnpy/data_process_main.py
    async def _process_batch_requests(self, batch_requests: list) -> list:
        responses = []
        for request_data in batch_requests:
            try:
                try:
                    if HAS_ORJSON:
                        request = orjson.loads(request_data)
                    else:
                        if isinstance(request_data, bytes):
                            request = json.loads(request_data.decode("utf-8"))
                        else:
                            request = json.loads(request_data)
                except (json.JSONDecodeError, UnicodeDecodeError) as e:
                    logger.warning(f"⚠️ 批量处理: 无效的RPC请求格式: {e}")
                    continue
                method = request.get("method")
                params = request.get("params", {})
                request_id = request.get("id")
                if method == "get_kline_data":
                    result = await self._handle_get_kline_data(params)
                elif method == "get_symbol_list":
                    result = await self._handle_get_symbol_list(params)
                else:
                    result = {"success": False, "message": f"未知方法: {method}"}
                response = {"id": request_id, "result": result}
                if HAS_ORJSON:
                    response_data = orjson.dumps(response)
                else:
                    response_data = json.dumps(response, ensure_ascii=False).encode("utf-8")
                responses.append(response_data)
            except Exception as e:
                logger.error(
                    f"❌ 批量处理请求异常: {e}",
                    exc_info=True,
                    extra={"log_type": "ALERT"},
                )
        return responses
```

客户端同样基于 JSON 编解码，存在请求头部多次拷贝。

### 优化方向

1. **原生消息帧协议**：在 `native_ipc` 中增加定长头（长度、方法ID、请求ID等）+ 可变数据区，实现 C 层解析与分发，避免 Python 解析 JSON。
2. **零拷贝数据体**：利用 `native_serialization.zero_copy_serialize` 与现有 `DataFramePayload`，将 Arrow IPC `memoryview` 直接在管道间传递，减少 DataFrame 回传的重复拷贝。
3. **RPC 批处理**：扩展客户端 `call_many` 接口，将同类请求打包发送，服务端在 C 层批量拆分与执行，提高 `_process_batch_requests` 吞吐。
4. **降级机制**：保留现有 JSON 协议，在握手时通过协议号动态选择，确保旧版本或缺省扩展时仍能工作。

### 预期收益

- 请求/响应头部序列化耗时下降，CPU 占用预期降低 20%-30%；
- DataFrame 回传减少一次拷贝，端到端延迟显著缩短；
- `DataCenterService` 高频调用（如批量K线）可获得更稳定的吞吐表现。

### 风险与落地步骤

1. 扩展 `native_ipc` 支持变长消息帧，并编写跨进程单元测试；
2. 调整 `DataFramePayload` 暴露零拷贝箭头缓冲，客户端消费路径适配；
3. 引入 RPC 协议版本号与协商逻辑；
4. 更新 `tests/test_rpc_integration.py` 覆盖双协议路径。

## 优化方向二：指标与风险计算原生化

### 现状

指标与风险计算通过线程池调用 Python/NumPy/TA-Lib 实现，存在重复数组构建与线程调度开销：

```602:686:backend/infrastructure/data_module_vnpy/data_process_main.py
    async def _handle_calculate_indicator(self, params: Dict[str, Any]) -> Dict[str, Any]:
        loop = asyncio.get_event_loop()
        with ThreadPoolExecutor(max_workers=4) as executor:
            result = await loop.run_in_executor(executor, self._do_calculate_indicator, params)

    def _do_calculate_indicator(self, params: Dict[str, Any]) -> Dict[str, Any]:
        import talib
        import numpy as np
        closes = np.array([float(d.get("close", 0)) for d in data])
        if indicator_name == "MACD":
            macd, signal_line, hist = talib.MACD(...)
```

```677:751:backend/infrastructure/data_module_vnpy/data_process_main.py
    async def _handle_calculate_risk_metrics(self, params: Dict[str, Any]) -> Dict[str, Any]:
        with ThreadPoolExecutor(max_workers=4) as executor:
            result = await loop.run_in_executor(executor, self._do_calculate_risk_metrics, params)

    def _do_calculate_risk_metrics(self, params: Dict[str, Any]) -> Dict[str, Any]:
        returns = np.diff(prices) / prices[:-1]
        volatility = np.std(returns) * np.sqrt(252)
```

### 优化方向

1. **新增 `native_indicator`**：在 C 层封装 SMA/EMA/MACD/RSI 等常用算法，支持批量序列输入，直接返回 `memoryview` 或结构化结果。
2. **风险指标原生化**：基于 `native_compute` 扩展收益率、方差、VaR、CVaR、最大回撤与夏普率计算，减少 Python/Numpy 重复运算。
3. **批量 RPC**：结合优化方向一，一次请求中传入多支品种数据，C 扩展遍历输出，提升批处理效率。
4. **兼容与开关**：保留线程池实现作为回退，并提供配置项控制是否启用原生算法。

### 预期收益

- 指标/风险计算避免 Python↔C 往返，批量计算时延降低 40%-60%；
- 数据进程线程池压力下降，为其他 CPU 密集型任务释放资源；
- 为策略中心提供更高吞吐的多指标服务能力。

### 风险与落地步骤

1. 评估 TA-Lib 许可与构建难度，必要时自行在 `native_compute` 中实现核心算法；
2. 编写 Python 对照测试验证数值一致性；
3. 扩展 RPC 协议支持 `calculate_indicator_batch` 等新方法；
4. 保留线程池兜底，并逐步扩大原生实现覆盖范围。

## 优化方向三：原生资源采样与监控去阻塞

### 现状

负载均衡与系统监控采样仍依赖 `psutil` 与 Python 循环：

```240:299:backend/infrastructure/data_module_vnpy/load_balancer.py
        cpu_usage = psutil.cpu_percent(interval=0.1)
        memory = psutil.virtual_memory()
        disk_io = psutil.disk_io_counters()
        io_rate = (read_bytes_delta + write_bytes_delta) / time_delta / (1024 * 1024)
```

```6245:6409:backend/infrastructure/system_vnpy/monitor_system.py
            connections = psutil.net_connections(kind='inet')
            for conn in connections:
                sock = socket_module.fromfd(conn.fd, ...)
                recv_buf = sock.getsockopt(...)
            net_io_1 = psutil.net_io_counters()
            time.sleep(0.05)
            net_io_2 = psutil.net_io_counters()
```

### 优化方向

1. **升级 `native_socket_metrics`**：调用 Windows `GetExtendedTcpTable`/`SIO_TCP_INFO` 直接聚合缓冲区、RTT 与吞吐，去除 Python 循环与 `time.sleep`。
2. **新增 `native_process_metrics`**：封装 PDH / `NtQuerySystemInformation`，一次获取 CPU、内存、句柄等指标，为 `ResourceMonitor` 提供更精准数据。
3. **负载均衡接入原生采样**：在 `ResourceMonitor` 中检测原生能力，优先使用新接口并输出平滑指标供调度使用。
4. **监控告警对接**：将新指标写入统一日志与告警系统，UI 可展示更精细的缓冲区使用情况。

### 预期收益

- 监控进程 CPU 占用与延迟显著降低，事件循环更平滑；
- 负载均衡调度依据更准确，避免误判；
- Socket 缓冲监控无需阻塞等待，可实时反馈网络状态。

### 风险与落地步骤

1. 处理原生接口的权限差异，确保无管理员权限时自动降级；
2. 扩展集成测试验证原生/回退模式下数据格式一致；
3. 更新文档与 UI 展示逻辑，说明依赖与平台要求；
4. 将监控进程 watchdog 与新指标联动，异常时快速回退。

## 实施优先级与验证建议

1. **优先落地 RPC 零拷贝**：改动集中、回退容易，性能收益可直接量化；
2. **同步推进指标/风险 C 化**：在 RPC 框架稳定后，引入批量计算与原生算法；
3. **监控采样原生化**：独立迭代，完成后为调度与告警提供更准确的底层数据。

建议使用现有 `tests/test_rpc_integration.py`、`test_monitor_integration.py` 增补双路径回归，同时编写性能基准脚本对比CPU占用、延迟与吞吐，确保新能力可控且降级可靠。

