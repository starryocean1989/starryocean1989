# 协程上限移除记录

## 修改日期
2025-10-26

## 修改目的
移除所有硬编码的协程/连接数上限，让系统展现真实瓶颈，用于验证以下假设：
- 8进程×数千协程无法让CPU进入压力区间 ≠ CPU瓶颈
- 16进程×少量协程能让CPU进入压力区间 = 单进程存在隐性并发上限

## 修改清单

### 1. LoadBalancer - 多进程异步模型协程上限
**文件**: `backend/infrastructure/data_module_vnpy/load_balancer/load_balancer.py`

**修改位置**: 第2202行
```python
# 修改前
self._max_coroutines = 1000

# 修改后
self._max_coroutines = 999999  # 🔧 移除协程上限，让系统展现真实瓶颈
```

**影响**: 动态协程数调整不再受1000的硬上限限制

---

### 2. ServerPoolManager - 每进程协程上限
**文件**: `backend/infrastructure/data_module_vnpy/load_balancer/load_balancer.py`

**修改位置**: 第4798行
```python
# 修改前
self.max_coroutines_per_process = 50

# 修改后
self.max_coroutines_per_process = 999999  # 🔧 移除协程上限，让系统展现真实瓶颈
```

**影响**: 服务器池测速时每个进程不再受50协程限制

---

### 3. TDX连接池 - 主连接数上限（关键！）
**文件**: `backend/infrastructure/tdx_asyncio/async_connection_pool.py`

**修改位置**: 第35行
```python
# 修改前
max_primary_connections: int = 38  # 主连接数

# 修改后
max_primary_connections: int = 999999  # 🔧 移除连接数上限，让系统展现真实瓶颈
```

**影响**:
- **这是最关键的修改！**
- TDX连接池的 `asyncio.Semaphore` 不再限制为38
- 每个进程可以创建更多的并发连接
- 真实瓶颈将从TDX连接池转移到其他地方（网络/磁盘/CPU）

---

### 4. 两段式Worker - 连接数上限
**文件**: `backend/infrastructure/data_module_vnpy/data_acquisition/data_acquisition.py`

**修改位置**: 第1836行
```python
# 修改前
connections_per_worker=30,

# 修改后
connections_per_worker=999999,  # 🔧 移除连接上限，让系统展现真实瓶颈
```

**影响**: 两段式下载worker不再受30连接限制

---

### 5. 标准异步Worker - 连接数上限
**文件**: `backend/infrastructure/data_module_vnpy/data_acquisition/data_acquisition.py`

**修改位置**: 第2527行
```python
# 修改前
connections_per_worker=30,

# 修改后
connections_per_worker=999999,  # 🔧 移除连接上限，让系统展现真实瓶颈
```

**影响**: 标准异步worker不再受30连接限制

---

### 6. IPO下载Worker - 连接数上限
**文件**: `backend/infrastructure/data_module_vnpy/data_acquisition/data_acquisition.py`

**修改位置**: 第3144行
```python
# 修改前
connections_per_worker=30,

# 修改后
connections_per_worker=999999,  # 🔧 移除连接上限，让系统展现真实瓶颈
```

**影响**: IPO下载worker不再受30连接限制

---

### 7. 传统模式 - 每进程连接数
**文件**: `backend/infrastructure/data_module_vnpy/data_acquisition/data_acquisition.py`

**修改位置**: 第3561行
```python
# 修改前
optimal_processes = math.ceil(len(available_servers) / 30)

# 修改后
connections_per_process = 200  # 从30提升到200
optimal_processes = max(1, math.ceil(len(available_servers) / connections_per_process))
self.async_connections_per_process = min(connections_per_process, len(available_servers) // self.num_processes + 1)
```

**影响**: 传统模式下每进程从30连接提升到200连接

---

## 预期效果

### 修改前的瓶颈链
```
8进程 × 1000协程配置
  ↓
实际受限于: TDX连接池Semaphore(38)
  ↓
真实并发: 8 × 38 = 304个
  ↓
现象: CPU未满载，大量协程等待semaphore
```

### 修改后的预期
```
8进程 × 无上限协程
  ↓
TDX连接池无限制 → 创建数千个连接
  ↓
真实瓶颈将转移到:
  - 网络带宽饱和
  - 磁盘IO队列满
  - CPU调度开销
  - 内存压力
  - 或其他隐性限制
```

### 对比测试建议

| 配置 | 修改前预期 | 修改后预期 |
|------|-----------|-----------|
| 8进程×1000协程 | CPU 40%，受限于38连接 | CPU 70%+，展现真实瓶颈 |
| 16进程×1000协程 | CPU 60%，受限于608连接 | CPU 90%+，可能内存/磁盘瓶颈 |
| 32进程×1000协程 | CPU 80%，受限于1216连接 | CPU/内存/磁盘均达极限 |

---

## 验证方法

### 1. 观察日志
查看worker启动日志，确认实际连接数：
```
异步Worker 0 启动，PID: xxxxx，连接数: 999999
```

如果看到999999，说明上限已移除。

### 2. 监控系统资源
运行下载任务，观察：
- ✅ CPU使用率是否显著提升
- ✅ 内存占用是否增加
- ✅ 磁盘IO是否饱和
- ✅ 网络带宽是否达到上限

### 3. 采样协程等待时间
在 `data_acquisition.py` 的异步函数中添加日志：
```python
async def download_task(client, ...):
    wait_start = time.time()
    async with semaphore:  # 可能在这里等待
        wait_end = time.time()
        if wait_end - wait_start > 0.1:
            logger.debug(f"协程等待semaphore: {wait_end - wait_start:.2f}秒")
```

如果修改有效，等待时间应显著减少。

---

## 注意事项

### ⚠️ 潜在风险

1. **内存消耗**: 数千个协程可能占用GB级内存
2. **网络连接数**: 可能超过操作系统限制（Windows默认5000，Linux可调整）
3. **TDX服务器限制**: 服务器可能限制单IP连接数，导致连接被拒绝
4. **事件循环性能**: asyncio在极高并发下可能性能下降

### 🔧 回滚方案

如果系统不稳定，恢复原值：
```python
# LoadBalancer
self._max_coroutines = 1000
self.max_coroutines_per_process = 50

# TDX连接池
max_primary_connections: int = 38

# Worker函数
connections_per_worker=30
```

---

## 结论

通过移除所有硬编码上限，系统现在可以：
1. ✅ **展现真实瓶颈** - 不再被人为限制掩盖
2. ✅ **验证假设** - 确认是否存在单进程隐性并发上限
3. ✅ **指导优化** - 基于真实瓶颈数据做出正确决策

**下一步**: 运行压测，对比8进程vs16进程vs32进程的性能表现，结合 `采样文档.md` 的监控方案，找出真正的系统瓶颈。

